# AGENTS.md

## 开发与沟通约定

- 开发由当前主代理独立完成，不新增子代理或委派；用户另有明确要求时再调整。
- 先用产品语言说明：用户现在怎么操作、看到什么；本次改变什么；改完怎么操作、看到什么。多用动词，技术名词只在影响判断时解释。
- 实质性改动前，先与用户对齐目的、改动范围、可见结果和验证方式，获得确认后再实施。不能把认可一个目标当成认可未说明的执行环境或方案；方向发生变化时先重新对齐。已确认且范围兼容的工作不重复请示。
- 临时 HTML 只用于让用户判断改后的样子。做到能看清并选择即可；只修会误导判断的问题，不为原型本身持续打磨。用户认可后进入正式实现。

## 协作协议：完成标准先于实现

人定什么叫好。机器在改动空间里搜。

完成标准必须在动手之前按用户要求和可观察结果写出来，先冻结判定条件再实现。不得为迁就已经写出的代码而放宽标准。

### 三个角色

- **校验**：动手前写出可执行的完成标准（要跑的命令、用户必须能看见的现象）。实现开始后，实现方不能自行改判定条件；用户明确修订需求时，按下述修订规则同步更新。
- **实现**：只对已有标准改代码。可以跑检查，不能改断言让自己过。
- **编排**：看哪些失败算数、下一步做什么、何时交给人。测试绿了但真实路径没出现，不算完。

三个角色是主代理分阶段承担的职责，不要求启动其他代理。实现阶段不能倒改用来停手的标准；产品判断由用户裁决。

### 需求修订与连续推进

- **当前指令优先于过期记录**：重要行动前核对当前用户要求、有效授权和任务记录。用户明确修订同一任务时，同步更新受影响的目标、完成标准和交付内容并继续，不因旧记录重复确认；实现方不得为迁就已有代码自行放宽验收。文档只记录状态，不能自行授予权限或覆盖当前用户要求及更高优先级约束。
- **已授权范围内连续推进**：只有出现需要用户裁决的产品取舍、实质性范围变化或缺少必要授权时才暂停，不把内部步骤切换变成审批点。已批准的流程不能代替无关外部操作、破坏性操作或特权操作所需的授权。

### 流程

1. 需求进来，先写完成标准再写代码。需求模糊时，先问清「什么现象出现时算它确实更好了」。
2. 每条标准标明谁检查：
   - **机器**：`npm run typecheck` / `npm test` / `npm run build` / 针对 `shared/protocol.ts` 的契约测试。实现自己跑到全绿才交付，未绿继续改，不拿半成品占用用户判断。
   - **人**：体验、观感、设计意图等机器判不了的。交付时单独列出，请用户裁决。
3. 用户说「太慢」「不好用」时，先追问成可观测现象（数值、行为、截图特征），再写成检查。能写成命令的写成命令，其余标「人」。
4. 实质性任务的完成标准存 `docs/evals/YYYYMMDD-<任务名>.md`，commit message 引用该文件。

### 完成标准格式

```markdown
# 任务: <一句话，用户能观察到的结果>

## 完成标准
- [ ] 1. <可执行标准> — 谁检查: npm test
- [ ] 2. <可执行标准> — 谁检查: 人

## 边界与不做
- <明确的非目标>
```

## 上下文管理默认行为

无需用户指示，默认执行：

- **定点探索**：由主代理按当前问题读取调用链和相关文件，及时记录结论，不为探索扩大上下文或新增子代理。
- **状态分工明确**：`docs/STATUS.md` 记录项目当前进度；任务验收文档记录完成标准与证据；`docs/NOTES.md` 记录续接所需的技术结论；开发日志记录方向与原因。同一任务不在多处独立维护当前进度，其他文档需要引用进度时链接到状态入口。
- **先外置再丢失**：每完成一个子任务，立即把关键结论、改动文件清单、未决技术问题写入 `docs/NOTES.md`——不等上下文压缩，压缩随时可能发生。同一问题更新已有结论，不反复追加状态快照。
- **阶段结束整理状态**：当前状态保留已确认事实、未决问题和下一步，替换过期描述；完成后保留结果及验收证据链接。原始完成标准和实测证据留在验收文档，用户修订标准时保留修订依据；重要失败原因进入开发日志，不随状态压缩删除。
- **压缩后回溯**：需要早期细节时先读 `docs/NOTES.md`；仍缺失再查 `.kimi-code/wiki/fold-archive/` 里的压缩前轨迹归档（由 PreCompact hook 自动落盘）。

## 经验沉淀闭环（Codex 任务收尾）

- 每个实质性开发任务完成相关验证后、最终交付前，由**当前 Codex 主代理**执行下面的收尾步骤，无需用户另说“复盘”。任务中止或未验证时，只保留必要记录，不把未完成结果写成有效经验。
- 复用唯一经验库 [`.kimi-code/wiki/index.md`](.kimi-code/wiki/index.md)。目录名是历史遗留，Codex 可直接维护；本流程不启动 Kimi、不派其他代理、不等待会话退出，也不保证应用关闭后仍会运行。

### 收尾步骤

1. **筛选**：只提炼本任务中有证据的失败教训、关键原因和适用条件。若最终代码、测试或已有文档已足以解释，不新增经验；没有新发现就到此结束，不为每个任务凑文档。
2. **核对**：定点查经验索引及相关 pattern、历史提案，避免重复和重提已拒绝建议。用当前任务的实际操作、完整错误、验证结果及用户反馈核对结论；旧总结只作线索，不自动扫描历史会话或把局部成功推广成整体成功。
3. **记录**：写入或局部更新 `patterns/`，说明现象、原因、方法、适用条件、验证与来源。区分事实、推断和未验证部分；证据链接到项目验收记录，不复制敏感轨迹。更新 `index.md`、在 `logs.md` 记一行。纠正已有经验时保留修订依据，不静默抹去反例。
4. **提案**：值得推广且现有规则或 skill 尚未覆盖时，向 `proposals/` 写至多一条原子提案，具体格式与裁决流程见 [提案说明](.kimi-code/wiki/proposal-workflow.md)。**形成提案不等于获准修改规则或安装 skill**；用户当前已明确要求的修改按原授权执行，不再伪装成待审提案。
5. **交付**：说明本轮新增或修订了什么经验，给出待审提案链接。用户决定是否升级；已有待审提案不阻塞其他已授权开发，也不在每轮重复催审。

- 日常实现只按已生效的规则和 skills 工作；复盘、定点纠错和用户裁决时才读取相关 wiki，不把整个经验库默认注入每次任务。
- 历史 Kimi 复盘接线仅作为独立入口保留，配置或可用性不决定 Codex 收尾流程是否执行。

## 开发日志

- 实质性工作日写开发日志，存 `docs/devlog/YYYYMMDD-NN-<标题>.md`（首篇：20260903-01）。
- 记什么：**方向上的改动、为什么改、最后成果如何**——重大设计转向与功能设计必记（含被实测推翻的方案及其原因），代码细节不记。
- 文风：阮一峰式——短句短段、一句一个判断、事实先行、克制观点（"我认为/我的判断是"可少量出现）、编号小节、不吹嘘、结尾给数字与下一步。

## 设计取向（UI/UX）

- 参考对象优先优质开源项目与一线产品（如 tldraw、ChatGPT/Kimi 侧边栏），消化其做法而非照搬。
- 美学基调：克制、简约，同时给人安全和可依赖的感觉。克制 = 信息分层，默认只露摘要，细节收进折叠；可依赖 = 每个动作有名字、耗时、结果状态，用户始终知道 Agent 在做什么。

## 设计改动：先判断，再对照，再出 HTML

界面、命名、层级、空状态、动效要人挑时，默认这条，不先改产品：

1. **先写出这一步真正要判断的 1–3 件事。** 例如这一屏是定名册，不是定字号。
2. **用这件事去搜 Will's S**（Notion，工作区 Will's S Design Note），打开对上的页，对照正文判断。禁止跳过 Notion 凭感觉开写。
3. **做成临时 HTML，同一条用户路径并排。** 选项不超过三，推荐项标出来。说明文字浅于方案本身。等人挑。
4. **人点头后再落地。**

HTML 本身也遵守：并排比较、先留白再收、标签能省则省。逐步做好，不一次堆满。
