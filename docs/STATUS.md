# 项目进度

更新：2026-09-18。本次汇总提交收录 9 月 17–18 日的翻译可靠性、P0 恢复和 Jev 显示加速代码、测试与验收文档，并更新 GitHub README。下文历史记录中的“未提交”指对应验收当时的状态；本次提交不改变 P0 未结项及真人语音待验收的结论。工程检查与提交范围见[汇总记录](evals/20260918-repository-publish.md)。

## 当前工作

技能快速复用正在集成进主线（2026-09-19，隔离 worktree `codex/skill-loop-integration`，从 main `c57edb9` 合并 `52b593e`，**合并进行中、未提交/推/加载日常扩展**；详见[集成验收](evals/20260919-skill-loop-integration.md)）。本轮修复与核验：①本树测试环境补回 arm64 rolldown 原生绑定（未改 package/lock），`npm run check` 退出 0（223 文件 / 2290 项 + 2 项规模、边界/两端类型/构建通过）。②路由根因是候选概率把「是这份做法」与「材料齐全可执行」混在一起；已拆出**独立澄清判断**（`clarify_i`，门限 .85）产出 `needs_input`，**执行门限 .9 未动、澄清路径永不执行**。真实 Jev：calibration 29/30、holdout 20/22、受影响组 12/12，未匹配样本 0 误自动匹配；保留 2 条业务正例约 907ms 撞 900ms deadline 的安全回退（不放宽 deadline）。③补 T02 `task_view` 与技能执行兼容回归（自动/手动回放 running→idle、绑定真实 runId/页面、零模型调用）。④**已修独立 Review 三项**（来源 `52b593e`）：敏感输入不再经技能运行进入公开事件/面板历史/诊断（含 ProgramStep 子步骤与失败文本，用本次材料逐个替换；执行值仍正确，见 canary 正反例）；自动学习只在「整条要求被这份做法完整交付」的真实 Jev 判断通过后才生成**可自动复用**的候选（`learnedOutputChecked`，契约被重新示范改动作废，判断概率限制有限 0..1）；本地模板只在槽位带引号或模板内有明确终止边界时确定性直达，尾部动作/条件交语义判断，**带引号的换材料零模型路径保留**。相应地，未带引号的自由文本不再本地直达。⑤真实隔离无头侧栏验收本轮 **BLOCKED**：受管沙箱内 headless Chrome for Testing 启动即 SIGABRT（未绕开权限），首次任务→候选保存→自动/手动换材料→停止→改口未复验。原 `52b593e` 部分验收结论（自动复用 1.816 秒、手动 1.686 秒、改口约 77.6 秒、program-first 未达 40% 默认关闭）保留为历史证据，不代表当前未提交状态已加载。不改变原 P0、真人语音或正式发布门槛。

日常试用已按用户决定启用（2026-09-18）：日常扩展已重载加载新运行时，任务模型从耗尽周限（429，两天后重置）的 opencode-go/deepseek-flash 切为 `minimax-cn/MiniMax-M3`，`~/.sideagent/config.json` 的 `displaySteerFastPath` 打开（父开关 displayFastPath 原已开）。真实面板验证通过：SW 存活、面板模型显示 MiniMax-M3、最小真实任务端到端回答正确；证据 `out/enablement/daily-trial-1789743968156/`（evidence.json + 截图），验证脚本 `scripts/acceptance/daily-enablement-check.mjs`。试用观察项：MiniMax 的概括粘行/命名行格式习惯、真实显示修改的准确度与回退体验；新任务模型待 deepseek 周限恢复后可再裁决。此前 Ticket 7 修复见下。

Ticket 7「显示修改不得改变未指定的属性」已修复并验证（2026-09-18，已连同前两轮未提交成果一并提交为可回退点 `2bdc618`）：反例重建确认执行层省略字段本就保留原值（display 分支条件赋值），缺陷在模型面——工具说明与插话契约补上「只提交本次要求改变的字段、省略即保持现状」；另发现 MiniMax 以原任务「不要修改网页」为由整体跳过显示修改，契约再补「最新明确修改要求对其指明属性优先于任务早期限制」。真实配对（任务模型按用户指示从耗尽周限的 opencode-go/deepseek-flash 换为 minimax-cn/MiniMax-M3，仅验收环境变量、日常配置未改）：原始反例 pair-0-off 与欠执行反例 pair-1-off 均全项通过，20 臂无任何未要求属性被改；`npm run check` 退出 0（215 文件 2061+2 项）。剩余失败均为 MiniMax 行为/习惯类别（summary 粘行超 40 字、缺命名行、该 display 却 translate、晚到交付 flush 污染下一臂窗口致 deliveryIdentity 假阳性），逐一定位并保留证据，未放宽任何验收标准；配对窗口归属缝隙记为验收台已知缺口。见[属性域修复](evals/20260918-display-attribute-scoping.md)。

上轮两项未通过项已定位并处理（2026-09-18，未提交、未重载日常扩展、新开关仍关）：①提示词预算按用户裁决上调至 16,000，`npm run check` 全绿（215 文件 2056+2 项，边界/类型/构建通过）；根因是 09-16 起余量仅 2 字符，09-18 接入提交的 P0 恢复指引 +170 越线。②Jev 关闭臂插话回退路径注入「补充而非替换、原任务仍欠交付」契约（session.ts STEER_CONTRACT_NOTE，随 session.steer 载荷进 Pi，销账仍按整条载荷精确匹配；执行闸门报错同步补句），真实全量逐臂隔离配对中关闭臂原任务段落数/概括交付 10/10（修复前 3/10），开启臂 10/10 无回归，6 边界 0 失败；验收脚本新增 `--pairs/--skip-boundaries` 分批抽查，正式门槛仍 10 对+边界，部分范围不置 passed。全量配对退出 1、不宣称完整通过：pair-0 关闭臂模型擅自把仅译文切成双语（字体与原任务答案都对），基线偶发越改显示参数，非契约引入，保持未处理。见[预算与契约](evals/20260918-prompt-budget-and-steer-contract.md)；验收脚本效率分层的三档用法同文。

Jev 运行中显示修改的独立 review 五项已修复并留证（2026-09-18，基于 `867f60b`，本轮未提交、未加载日常扩展、未启用新开关）：刷新使本条修改失效而非回主模型重做，拒绝的要求不留在恢复输入；显示成功但 Pi 交接失败时保留旧写入阻断，接管/交还只恢复交接；晚到核验不再声称取消的任务继续；提示按已核验/未核验/未知区分。原四个反例先复现失败，正式扩充为 10 项通过；独立验收器 13 项通过，另相关 216 项、P0 162 项、规模 2 项和构建通过。真实隔离浏览器刷新/取消 2/2 通过（Jev 响应和读回时机受控，实际任务模型与浏览器）；当时关闭臂漏交原任务与提示词超预算两项遗留已由上段后续轮处理。证据、验收器修正和中间失败见[review 五项修复](evals/20260918-jev-display-review-fixes.md)；此前[六票交付与配对](evals/20260918-jev-display-steering.md)保留为历史，旧判定器结果不作当前完整正确性证明。

Jev 显示操作已接入并在日常环境启用（2026-09-18）：已有译文的宋体/双语/仅译文可直达原工具，1秒超时或不确定回原流程，原字体暂走原模型。真实侧栏10次配对零错误页面/缺失回执，9次直达，中位页面变更2604→771ms（降低70.4%）；取消、刷新、超时、局部范围及混合请求完成检查。范围回退曾误调用整页工具，已增加run绑定阻断并复测。日常扩展与native已重载、连接确认。全量普通测试保留一项既有提示词长度预算失败，相关检查通过。见[接入验收、启停与原始证据](evals/20260918-jev-display-integration.md)。

P0 复测裁决已落盘（2026-09-17）：保留未知写入的安全停写行为，但不将未完成任务改成通过。程序步骤编号、文字恢复/请求幂等和损坏条目隔离已有实机正证据；继续补接收持久化与范围明确的恢复能力，不结项、不进入 P1。见[裁决、证据与后续完成标准](evals/20260917-p0-retest-adjudication.md)；已有修复的工程和实机记录见[修复验收](evals/20260917-p0-acceptance-fixes.md)。

原复测结果保持：主复测 **2 PASS / 3 FAIL**，文字入口 **3 PASS / 2 FAIL**，其余分别 7 项 NOT_RUN；损坏条目变体 **17/17 检查通过**、1 个场景 PASS，其余 11 项 NOT_RUN。三份原报告已重新离线校验，均 errors=[]，分别汇总为 FAIL / FAIL / NOT_RUN，不能拼成整套通过。证据在 `out/acceptance/p0-local-agent-retest-20260917/`；首轮11/12报告完整保留。原失败场景仍未完成，已记录的重复保存/错页保存为零不代表业务成功。

新增优先缺陷：首次文字 host-restart 的原始 trace 已发 accepted 回执，首条模型输出前重启却丢了 runId。当前 Pi 的 appendCustomEntry 在尚无 assistant 消息时可能只写内存；本轮无模型最小复核得到 memoryHasCheckpoint=true、fileExists=false。必须先让接收回执之前的任务与必要附件真实落盘，并保留该即时重启反例；等待 checkpointDurable 后注入是另一个边界，不能消除该缺陷。P0.1 重新打开，P0.4–P0.5 保持未勾选。随后补低风险当前状态核对与有范围、可取消的一次确认续接；换页面或笼统确认不放行未知提交。上述新修复尚未实施，本轮仅改文档、复核存储行为与已有报告，零产品模型调用，未动日常扩展、预算、遗留进程或原证据，未提交发布。

两段修复已实施并继续加固（2026-09-18，未提交）：`ConversationStore` 在会话建立时用 SDK 自带 header 创建并接管 Pi 会话文件；新任务的 accepted 边界改成单条 `sideagent-task-acceptance-v1`，一次 append 同时保存检查点和必要附件，避免“附件已写而任务 rejected”或“检查点已写但附件丢失”的半提交。`TaskResultBook` 保留旧 unknown，以新的 satisfied 核对/重设项通过 `supersededBy` 解除对应阻塞，不改写旧证据。`confirm_blocked_write` 仅支持低风险 `fill`：先读当前状态；需要重设时把确认绑定到任务、要求版本、tab、**documentId**、对象和参数，用户允许时再次读取同一 document，允许后到实际 fill 之间若同 URL 刷新/换 document，扩展执行闸门仍会明确 `not_executed`，不会制造新的未知写入。原 `fill` 参数只在账本保存 **SHA-256 valueHash**，不保存字段明文；“当前已经满足、无需写入”只允许稳定同一对象 + 原参数 hash 一致，模型重定位、改值、旧 `@ref` 或旧版本无 hash 都必须取得用户确认。save/send/pay/delete 不走此路。P0 专项 **160/160**、普通回归 **209 文件 / 1970 项**、类型、构建和 **189 个生产文件**边界通过；规模测试单独复跑 **2/2** 通过。此前一次 `npm run check` 在长串行后的规模阶段触发既有 5 秒超时，该失败保留为环境波动证据，不写成整条命令 exit 0。当前源码的四份实机模板已重新生成到 `out/acceptance/p0-chat-retest-20260918/`，指纹 `894c5936dabe…`，均为 NOT_RUN / errors=[]。真实模型复测尚未运行，需用户明确授权实际模型和本次预算；后续由当前 Chat 直接通过 DevSpace 执行，不再转交开发 Agent。P0 仍不结项、不进入 P1。见[裁决与实现](evals/20260917-p0-retest-adjudication.md)。

翻译 review 已核实并修复（2026-09-17）：注入前标签页解析失败准确记为未执行；每次字体恢复后释放样式基线，网站在两次操作间增删 style 均按本轮状态恢复。定点回归和隔离生产扩展验证通过，见[复现与验收](evals/20260917-page-translation-review.md)。未重载日常扩展、未提交发布。

翻译可靠性本地修复（2026-09-17）：页内异常保留真实原因及未写入事实；动态段落不再拖垮整批，持续无进展有界退出；专用宋体/原字体与双语切换可逆，字体请求不重译。OpenHands真实隔离侧栏+当前模型完成183段，约7.2秒首批、92秒整轮，宋体约3秒落页；动态后来内容单独计为待译。类型/边界/33项定点与生产浏览器路径通过。原截图底层注入触发原因未留存，未重载日常扩展、未提交发布。见[原因、失败记录与最终证据](evals/20260917-page-translation-reliability.md)。


Voice 多要求调度已实现并加载（2026-09-16）：独立要求登记、2个根任务/8个待办准入、同页等待、按名称修改取消及跨任务结果回原语音已接通；页面准备阶段也占名额。真实三任务验证新页并行、同页后续启动、三份正确结果及阶跃音频，实际侧栏显示排队。额外修复分类把背景条件拆成动作、首声前插话丢结果、超过32条待播静默丢弃。相关506项及新增计划31项、类型/构建/边界通过；空闲检查后19:13重载native并重新连接。见[实施证据与边界](evals/20260916-voice-multi-request.md)。精确续播、整体控制和恢复体验为后续项；未提交发布。

输入区精简已接入并加载（2026-09-16）：普通回执按任务/请求收纳，异常保持可见；诊断收进输入选项，模型轻量显示。明确可恢复的新请求可携原文/选区/附件转交新会话，旧记录缺上下文时不伪造按钮。见[验收](evals/20260916-composer-cleanup.md)。

克制动效已接入并加载（2026-09-16）：右上角状态即时更新并短淡入，角色列表锚点展开/更快关闭，侧栏过程自然高度折叠；快速反向操作与减少动态检查通过，原角色/光球/正文保持。见[动效验收](evals/20260916-motion-polish.md)。

Voice 多要求架构检查已完成（2026-09-16）：实测忙时三个独立要求均未排队、跨会话结果不回原语音、40条待播只留32条；跨会话执行准入没有统一上限。页面保护、连续纠正和控制优先检查通过。新增基线及相关92项通过；这些结果是现状刻画，不是多任务功能通过。本轮只增测试与设计文档，日常运行未改。实施顺序为要求登记/结果回传一起接通，再接有界准入与页面等待，见[检查证据](evals/20260916-voice-load-boundaries.md)、[实施设计](voice-multi-request-design.md)。

Voice 插入新任务后原回答漏播已修复并加载（2026-09-16）：原任务仍有效时，新输入的澄清不再使其回答过期。先复现丢播再修复，300 项语音回归与 Agent 类型通过；真实阶跃 TTS 验证两种到达顺序都产生原答案音频。空闲状态重载后 native 进程已重启连接。独立新任务仍走另开会话澄清，后续处理明确“同时”请求的任务接入。见[原因与证据](evals/20260916-voice-task-interjection.md)。

Voice输入边界已按全栈方案修改并加载（2026-09-16）：声音先进入识别，页面资料异步按原片段补齐；分类与任务执行完成信号分开，快速三段的迟到转写保留并排序。自动化先抓到三段丢第一段，再验证修复；真实隔离扩展+阶跃识别+任务模型+网页读取得到正确开放时间和票价。相关411项、类型、构建与模块边界通过，确认无运行任务后加载并核对后台脚本。见[架构图](voice-architecture.md)、[全栈证据](evals/20260916-voice-input-boundaries.md)。接下来做附和后的自然续播与等待反馈；未提交发布。

Voice连续对话改造推进中（2026-09-16）：队列、停播、未听完全文记忆、未完成句续接与附和已做候选修复。两次真人失败分别暴露分段续接/完整性误判，以及旧闲聊跨轮启动任务导致新读页答案丢弃；后者已有旧条件失败→修复通过的生产回归，358项语音测试、类型和6种真实模型表达通过。确认无运行任务后已再次重载，包含停播捷径；真人整体体验仍未通过。精确断句续播、后台网页整链路和声学延迟仍未完成。详见[完整范围、失败及证据](evals/20260916-voice-conversation-flow.md)，不以局部通过代替整套验收。

Voice 交互验收推进中（2026-09-16）：真人“别说了”需重复两三次，插话体验未通过。第二次收音观察支持启动阈值过严的假设，最小调整已获认可并加载日常侧栏（已核对运行脚本）；受控回归、类型、构建及真实模型合成语音/噪声检查通过，等待修复后真人一次停声与回声误停对比。另有停播意图重试12.5秒的独立延迟。用户同意沿现有阶跃链路依次改善听见、停住、接话和任务并行，不接入GPT-Live/Gemini Live。见[诊断与修复](evals/20260916-voice-input-diagnosis.md)、[验收标准](evals/20260916-voice-interaction-contract.md)。未提交发布。

EverOS 日常积累已启用（2026-09-16）：用户改为“先启用、边用边判断”。常驻服务从启用后的新完成任务读取已有记录，真实页面验收任务已自动整理并落盘；停启、重启不重复导入与错误恢复检查通过。日常浏览器无需重载，生成内容暂不影响操作或覆盖明确记忆。使用、关闭方式和证据见[启用记录](evals/20260916-everos-enable.md)。

光标统一状态已接入（2026-09-16）：本页读取/等待/动作结果与跨页角色集中在右上角；目标旁只显示授权确认，普通光标名牌隐藏。生产bundle受控路径与相关测试通过，空闲后已重载日常扩展，未提交/发布。见[接入验收](evals/20260916-unified-cursor.md)。

明确记忆已完成本地修复（2026-09-16）：自然表达保存/更新/忘记、临时覆盖与按需取用已通过真实隔离扩展；纯记忆请求不顺手提交，原报名任务补邮箱后可继续。EverOS 早期三次任务未生成技能，后续按用户新决定先开启积累，状态见上。相关检查通过；全量回归保留一项并发侧栏 CSS 断言失败。明确记忆修复当轮未重载日常扩展、未提交/发布，见[实现与验收](evals/20260916-memory-implementation.md)；早期[组件对照](evals/20260916-memory-options.md)保留为判断依据。

跨页入口已合入统一状态并加载（2026-09-16）：轻量角色入口，多角色展开分别跳转；修复工作页迁移遗留等待状态。后台竞态与生产bundle受控交互通过，当时因任务运行未重载，现随统一状态加载。见[验收](evals/20260916-cross-page-entry.md)。

回应区已选定并接入阅读优先（2026-09-16）：正文行距与标题层级对齐已选原型，原光球与折叠过程保留，回答末尾提供复制和继续追问。生产构建的受控交互通过，日常侧栏已刷新且连接正常；来源仍为真实 Markdown 链接，未加入原型中的模拟摘录。未提交/发布，见[接入验收](evals/20260916-reading-priority.md)。

暖纸色与回应区已落地并重载日常扩展（2026-09-16）：双斜杠 Logo、浅砂气泡；保留原光球与状态标记，工具/协作详情收进过程，回答独立呈现。实际模型读页回答、受控交互、窄屏/深色、类型/构建及相关测试通过，原生侧栏已打开。最终观感待用户判断，未提交/发布，见[本轮验收](evals/20260916-paper-response.md)。



语音修改已取消强制读回（2026-09-16）：用户亲测翻译、双语/仅译文、宋体成功，但修改被连续复述确认三次；已核对7轮原始转写/回执并改成一次送达。真实模型与运行中任务验证两条修改无需确认即可改变网页；原页面/附件/控制身份和具体危险动作保护保留，终止任务仍沿用读回。当前语音会话未重载，真人听感待复测，见[语音直接修改验收](evals/20260916-voice-direct-steering.md)。

划词阅读已完成本地实现和验证（2026-09-16）：统一浮层、独立无工具阅读请求、多轮追问、停止/重试/关闭恢复，以及完整侧栏交接；修复侧栏启动切走阅读与首次回复前交接未落盘。真实隔离扩展+模型及受控异常路径通过，观感待用户判断。本轮未主动重载日常扩展、未提交/发布，见[验收与截图](evals/20260916-selection-reading.md)。

整页翻译真实长文故障已修复（2026-09-16）：同页复现定位为段落翻译的可选思考耗尽输出预算，继而被误记成未知写入并锁死。已改直接生成、准确记录失败事实、保留部分结果恢复，并弱化侧栏滚动条；用户原 Asterisk 页面真实侧栏+模型验证 88/88 段、仅译文，约 5.8 秒开始显示、整轮 82 秒。默认双语和语音转写的既有功能保持，真人麦克风待测；未重载日常扩展、未提交/发布。见[故障与修复验收](evals/20260916-page-translation-failure.md)，早期短页证据保留在[原验收](evals/20260916-page-translation.md)。

连续插话稳定性已修复并重载日常扩展（2026-09-16）：补充同轮处理、控制优先、暂停/故障时未读要求不丢或复活，团队更新失败的成员停止旧任务。真实隔离扩展+模型9项路径通过，普通回归、类型与边界检查通过；未提交/发布，真人语音另验。原失败、复核与最终证据见[专项验收](evals/20260915-continuous-steering.md)。

主执行光球四类状态标记已按确认方案落地并重载：手形、完成勾号、失败提示、停止方块；停止事件进入历史，避免回看误显示完成。真实文本任务完成标记及受控前端状态/回放检查通过，见[标记验收](evals/20260915-orb-markers.md)。

主执行光球已修复并重载本地扩展，动效已按用户指定 thinking-orbs 源仓库原预设校正（移除自定调速与附加效果）：真实运行中思考、执行、等待分别有不同动效，完成后静止；接管/恢复、失败、停止、历史和减少动态经隔离前端检查通过。普通单测、类型、构建及模块边界检查通过，见[光球验收与演示](evals/20260915-orb-state-fix.md)。未提交/发布。

并行助手呈现已修复并重载本地扩展：默认摘要显示职责、固定身份和真实等待/交付状态，分工说明正式送达；真实两助手汇总、创建/执行/光标同名、切会话回放、窄面板和短任务检查通过。普通单测、类型、构建及模块边界检查通过；自主拆分与真人语音未测。本轮未提交，见[实现验收与前后截图](evals/20260915-cast-implementation.md)；原始问题保留在[诊断](evals/20260915-cast-diagnosis.md)。

人机交互入口已落地：新会话给可编辑起点，诊断收起并明确只记录；移除写死的语音状态，保留真实语音阶段。真实前端检查了建议不自动发送、失败原文重试、键盘操作、引用/附件保留与切会话草稿；修复本轮发现的慢恢复草稿覆盖问题。规则与原文落点见[人机协作约定](human-ai-contract.md)，证据见[交互验收](evals/20260914-human-ai-interaction.md)。日常扩展未重载；预览使用模拟传输，不等于真人语音验收。

仓库清理已完成：语音计划与文本比较、授权/请求识别、浏览器回退判定改为单点维护，退役配色代码删除，现有行为保持。整体工程检查及真实隔离浏览器填写、点击、取消、切页检查通过；独立复核无新增缺陷，保留项有明确依据。日常扩展未重载，真人语音待验结论不变。见[清理验收](evals/20260914-code-sloppiness.md)。

已修复语音问候重复回应与文字先到时丢音：正式答案只播一次，闲聊不自动读页，空白候选不会吞掉等待中的答案。已重载日常扩展，合成问候经真实 native host 验证一次回答、有音频、无读页/任务确认/页面介绍；1,662 单测、类型与依赖检查通过。真人麦克风待用户验收，监控继续暂停。延迟及测试中出现的服务/工具失败见[修复验收](evals/20260913-voice-greeting-fix.md)。

已量清语音首声等待：说一句话到听见第一声实测 4.9–7.7 秒，其中**两段串行的模型往返占 5.2 秒**（意图分类 1.7–2.7s、主 Agent 首字 2.5s）。三条本地优化路径都用真实调用验过并被数据否决：分类耗时≈供应商下限（削提示词、加 reasoning 都无效）、池内更快的模型不是更慢就是判断出错、TTS 提前建连只值 270ms。见[首声验收](evals/20260913-voice-first-audio-latency.md)。

首声已改善并提交推送：**闲聊、寒暄、算数改走一条独立的最小请求直接出正文**，其余（控制、页面问答、依赖事实的问询、动手委托）走与改前逐字同形的分类请求加既有派发。实测问候 6.8→3.8 秒、7.0→2.8 秒，算数 5.4→3.4 秒，reply 分支 9 个样本中位 **3432ms**（该口径不含脚本先送的 500ms 静音与真人停句时间，≤4 秒须以真人路径实测为准，不能引用合成口径宣布）；控制句首次实测 3.3 秒。**页面问答与基线持平（5.2–6.4 秒），未达成 ≤5 秒线**，按不达成记录，待裁决。防编事实改为失败关闭：由程序判定整句是否属于无事实类别，不依赖模型自律。首声目标由 ≤2 秒修订为 ≤4 秒（2.8 秒是链路地板，用户已同意）。外部复核指出的两点代码事实（`sendUserMessage()` 提前改状态、未执行的交付参数会被提前发成交付流）已核对属实，并行方案据此降级为备选。见[合并验收](evals/20260914-voice-single-proposal.md)。

用户已认可宋体、回答层次及设置方案。已实现阅读外观并重载本地扩展，原生验证保存、重开、恢复默认及实际长短回答。普通单测通过，规模检查复跑仍超时，原因未定位；不宣称全套检查通过。见[阅读外观验收](evals/20260913-reading-settings.md)及[使用说明](reading-appearance.md)。

仓库可维护性整理已完成并重载验证：模型选择器、无工具语音模型调用独立成模块；Node授权账本归回Agent；生产依赖方向加入自动检查。统一check通过（1598普通+2规模）；最后绝对路径反例补充后13项边界与类型检查通过。模型菜单和正常消息原生通过。检查与原生验证见[本轮验收](evals/20260913-repository-maintainability.md)。开发入口见[架构与维护](architecture.md)。

## 已验证的产品状态

| 范围 | 当前依据 |
|---|---|
| 文字执行与交付 | 修复有效答案后的空响应、链接误吞和重复呈现；读页、打开链接、两页比较通过[原生验收](evals/20260913-dogfood-fixes.md) |
| 纠正与控制 | 复合确认/否定接回原待办，查询/空轮保留上下文；中止后新任务、非成员页交还、切页不串任务、跨页提示清理通过[同类缺陷验收](evals/20260913-interaction-loop-audit.md) |
| 会话、记忆与授权 | 会话切换/草稿隔离已原生验证；记忆与技能各保留独立存储；授权参数、作用域、一次使用及取消失效有[闭环证据](evals/20260913-architecture-closures.md) |
| 已加载版本 | 阅读外观最终构建已重载，见[本轮验收](evals/20260913-reading-settings.md) |

## 未通过与下一步

- 真人语音：原确认循环已修，但没有第二次真人麦克风重测；声学首轮失败保留，不能以文字/合成输入替代。见[语音专项](evals/20260913-voice-confirm-loop.md)。
- 功能承诺：技能新示范到复用、部分重连/重启组合仍未完整原生通过；逐项以[功能清单](evals/20260913-feature-claims.md)与[操作记录](evals/20260913-feature-journeys.md)为准。
- 发布：真人、独立留出集等门槛未齐；不得把候选称为正式版。历史live第三轮达到145/150，不代表当前构建已重跑发布评测。见[发布验收](evals/20260911-pr02-pr07-release-converge.md)。
- 语音首声（2026-09-14 校准口径后）：**问候按"你说完 → 听见"约 3.9–5.8 秒**（此前报 3.4 秒用的是"应用收到说完了"起点，少算了约半秒；两次运行间波动很大，单次样本不足以定论）。运行中喊停的回执 3.3 秒、播出「任务已暂停，页面现在归你。」。**测试台曾把"缺应答"误报成产品缺陷，已更正**。见[尺子与停住验收](evals/20260914-voice-stop-and-ruler.md)。
- 语音首声（旧口径）：闲聊/算数已达标（中位 3432ms），**页面问答与基线持平未改善**，≤5 秒线未达成，待裁决是否按“回到基线波动带”结项。控制句只有一次样本。真人麦克风仍待用户。见[合并验收](evals/20260914-voice-single-proposal.md)。
- 当前顺序：补以上真实路径；不新增产品功能、不用测试通过保证全产品零缺陷。

## 历史与续接

旧状态全文保存在[整理前快照](history/20260913-status-snapshot.md)，包含当时未加载、旧分支、旧测试数字，均不是当前结论。续接原因见[NOTES](NOTES.md)，具体检查结果留在对应eval，设计变化留在devlog。

## Pi + Stagehand 接入（2026-09-13）

已将官方 Playwright 兼容层接入现有 Pi/browser_run 控制链。隔离真实 Chrome 已通过填写、取消、纠正后保留邮箱、切页不串任务，并补验点击、trial不点击、多匹配不写和macOS全选。真实面板+deepseek-flash模型的填写、正常停止按钮、改名保留邮箱和运行中切页四项已通过，新版已加载；真人麦克风留待亲测。无需Browserbase Key。见[接入记录](evals/20260913-stagehand-control-integration.md)。先前SDK独立探针为历史证据，见[探针结果](evals/20260913-stagehand-pi-probe.md)。
