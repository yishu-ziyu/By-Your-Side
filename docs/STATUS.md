# 项目进度

## 当前：两个杠杆都生效——单动作 2 轮、总耗时中位数 13.5s → 5.3s

2026-09-11。用户追问"为什么改动之后总耗时反而增加了"（更正为：中位数 8.8s vs 8.85s，是 3 次抽样误导），
随后要求把两个真杠杆改掉并做对照。标准与全部证据见[本轮验收](evals/20260911-two-levers.md)。

做了什么。① 新任务发出前，运行时先对用户当前页做一次只读 `snapshot`（4s 超时，无 tabId/扩展未连接/超时/空页面
都静默降级为不注入），观察按不可信页面内容包裹后附在消息尾部；运行中插话不预注入。②
`send_user_message(kind="finding")` 的工具结果带 `terminate:true`，命中 SDK 批次早停规则，round 在交付轮结束；
`ack` 不带（开场应答不能掐断任务），被拒绝的交付也不带。提示词同步加一条"消息已带新鲜观察就直接动手"，
并按"删重复不删规则"压回 15,000 字符以内（14,973）。

结果（同一条真实单动作 case「暂停视频」，真模型 MiniMax-M3 + 隔离 headless Chrome，改前 3 次 / 改后 5 次）：
轮数中位 4 → 2；总耗时中位 13,471ms → 5,332ms；首个页面动作中位 7,474ms → 2,715ms；改后 5/5 恰好交付一次、
页面真的暂停。trace 里的两处结构变化：模型的 `snapshot` 轮被 5–31ms 的运行时读取替代；交付轮 `turn_end` 之后
1–4ms 就是 `agent_end`，改前 986–1740ms 的纯文本收尾轮消失。

机器检查：153 文件 1335 项、typecheck、build 全过（新增预观察 5 项、交付 `terminate` 1 项，改写 1 项按时序）；
`merged-tools-run` 7/7、`run-status-ui` 10/10 复跑通过。中途一次全量测试的 2 个 QuickJS `interrupted` 失败经单跑/复跑
判定为并发抖动，不是本次改动。

未跑 / 待裁决：① 预注入的 token 成本（每次新任务最多 12k 字符页面观察）只测了收益侧；② 多步任务探针 3 次里 1 次
"做完但没交付"，改前基线同样出现过 1/3，属既有缺口，需单独查；③ 真实扩展里的人眼观感由用户裁决。
本轮未提交、未推送、未重载用户扩展。

## 上一步：快而果断硬指标 + 模型面/用户面收敛（用户 2026-09-11 批准的三件事）

2026-09-11。按 claude 会话批准的三件事推进：①把"快而果断"定成硬指标；②砍模型看到的面；③砍用户看到的面。标准与全部结果见[本轮验收](evals/20260911-fast-and-lean.md)。

模型面（已完成）：标签页五个工具合成 `tabs`（list/active/open/switch/close），标注两个合成 `mark`（画 / `clear:true` 清）；协作工具 post/await_message/list_workers/stop_worker 只在有 worker 时挂载（`spawn_worker`、`take_tab` 常驻——后者管跨会话页面所有权，且 S1 的「写能力不完整时 JS 一并拒绝」闸门依赖它）；系统提示词 17,418 → 14,993 字符（删掉代码已守住的重复规则，新增单动作 2 轮预算）。Lead 无 worker 时模型可见工具 32 → 23，工具描述+参数 schema 少 26%。RPC 名、控制闸门、账本语义未动。真实扩展路径 7/7 PASS（`scripts/acceptance/merged-tools-run.mts`）。

用户面（已完成，观感待人裁）：像素格等待态整体移除，运行中只剩一条状态行「[光球] 正在做什么 · 耗时」，思考块与工具 chip 默认收起、点开可见；结束后落定「查看执行过程 · 耗时」。真构建面板 + 隔离 headless Chrome 10/10 PASS，截图 running/done/expanded（`scripts/acceptance/run-status-ui.mts`）。

硬指标（未达标，如实记录）：同一条真实单动作 case（暂停视频，真模型 + 隔离浏览器，各 3 次）模型调用 4/4/5 → 5/4/4。改后结构是「观察 1 + 动作 1 + 交付 1 + SDK 收尾 1」，交付恰好一次（3/3）、首个动作 2.6–5.0s，但"≤2 轮"没做到。原因见[开发日志](devlog/20260911-02-模型面收窄与单动作轮数.md)：模型没有按提示把观察+动作折进 `browser_run`；交付后的 SDK 收尾轮在工具层取消不了。（这两个杠杆已在上面一节完成。）

机器检查：152 文件 1326 项、typecheck、build 全过（新增工具面测试 7 项；合并后按模型名改写 12+1 项旧断言、删除像素格相位测试，理由见验收附录）。本轮未提交、未推送、未重载用户扩展。

## 数据层：第三步之一 批量翻页（`fetch` 的 `pages`）

2026-09-11。`fetch` 新增 `pages:{from,to,step?}`：`url`（或 POST body）里用 `{page}` 占位符，一次调用顺序取多页（上限 20），每页原样落盘 `~/.sideagent/downloads/<基础名>-p<页码>.<扩展名>`，回执只给每页状态/字节/路径 + 第一页 300 字预览；非 2xx 标 `(not ok)`、网络错误写原因，不抛错也不冒充数据；缺占位符/非法范围/超页数在发请求前拒绝。单页 `fetch` 行为不变。

机器检查：151 文件 1326 项、typecheck、build 全过（新增 `agent/test/fetch-pages.test.ts` 9 项）。真实路径（隔离 headless Chrome + 真实扩展构建，GitHub issues 公开接口 `per_page=5&page={page}` 取 3 页）：一次调用 1,880ms，三个文件落盘且三页 issue 号互不重复，回执 1,232 字符（三页数据约 105KB 在磁盘），真实 404 逐页标 `(not ok)`，批量里私网仍被守卫拒绝且不发请求。见[本轮标准与证据](evals/20260911-fetch-pages.md)。

真实运行修掉一个**既有**缺陷：`redactCredentialText` 的词级字符集含 `/`，把路径/URL 按分隔符拼成长串，`https://api.github.com/repos/…` 会被隐成 `https://api.github.[redacted]`（抓数据的 URL 直接不可用）。已去掉 `/`、删除对产品自己回执的二次脱敏，补回归；[原验收已追加修订](evals/20260911-untrusted-and-snapshot.md)。

未决：① 接近 20 页/慢站点的超时行为未测；② 批量里单页网络中断的恢复只有单测。

下一步：`download`（浏览器原生下载）→ 声明式批处理 `act` → offscreen 连接。本轮未提交、未推送、未重载扩展。

## 并行：仓库复查与死代码清理

2026-09-11。全仓库复查：修掉四个真缺陷——观察候选 accept 不消费（反复问同一件事）、`fetch.pages` 超大范围页码循环无上限（挂死伴随进程）、`network` 的 `limit` 非数值时筛选失控、`ExperienceStore` 单个坏 JSON 拖垮全部经验读取；另按编译器 TS6133/6196 删死代码与未用形参（+11/−30），保留 `TOOL_ICONS` 等未决项。151 文件 1330 项、typecheck、build 全过；fetch/network/批量翻页三条真实路径 harness 复跑通过，数字同量级。见[验收](evals/20260911-repo-hardening.md)。

## 前一步：数据层第二步 `network`（被动看页面调了哪些接口）

2026-09-11。新只读工具 `network{urlContains?, types?, limit?, tabId?, clear?}`：扩展一旦持有调试器就开启 CDP Network 域，按标签页环形缓冲（容量 300，跨导航保留，内存态）；默认只看 xhr/fetch，可按 URL 子串过滤、取最近 N 条、清空重录；展示前隐去 URL 凭据（user:pass@、敏感查询值），回执过不可信边界。`navigate` / `open_tab` 现在先 attach 再加载，加载期的接口调用也进缓冲；attach 失败（DevTools 占用）不影响导航本身。提示词把「先 network 看页面自己的接口，再 fetch 取」写成一条。

机器检查：150 文件 1315 项、typecheck、build 全过。真实路径（隔离 headless Chrome + 真实扩展构建，B 站视频页）：`navigate`→`snapshot` 后 `network` 看到 101 条真实请求、14 条 `api.bilibili.com` 调用；从记录里取一个页面自己调过的 URL，`fetch` 直接取回 HTTP 200 / `code:0`；默认过滤、`urlContains`、`clear` 均按预期。真实运行修掉一个真缺陷：`data:image/png;base64,…` 伪请求把整段 base64 写进上下文，已改为只记 http(s)、展示 URL 截 240 字符。见[本轮标准与证据](evals/20260911-network.md)。

未决：① 调试器分离后缓冲保留、SW 重启即空只有代码/单测级证据；② 用户开着 DevTools 的分支未真验；③ `navigate`/`open_tab` 提前 attach 后，页面顶部黄条可能比之前更早出现，体验变化未人验；④ `Network.enable` 的额外开销未测。

下一步：批量翻页 / `download` → 声明式批处理 `act` → offscreen 连接。本轮未提交、未推送、未重载扩展。

## 前一步：数据层第一步 `fetch` 完成（含真实站点证据）
2026-09-11。新工具 `fetch{url, method?, headers?, body?, savePath?}`：扩展带登录态发请求（Cookie 不出扩展），只允许 http(s) + GET/POST，本地/私网地址与 Cookie 覆盖一律拒绝；响应上限 512 KiB，大响应由伴随进程落盘 `~/.sideagent/downloads/`，回执只给状态行、路径、字节数与 300 字预览；进上下文的正文过不可信边界与凭据隐去。提示词加了一句：批量抓数据先试页面自己的 JSON 接口，别拿 DOM 硬刮。

机器检查：148 文件 1277 项、typecheck、build 全过。实现中修掉一个真缺陷：`savePath` 带 `/` 时首版仍用原串，形成路径穿越（已改为只取 basename）。**真实站点样本已补跑**（隔离 headless Chrome + 真实构建扩展）：B 站视频页、Hacker News 首页两样本，fetch 进上下文 538 / 527 字符 vs 同页 snapshot 11,541 / 23,786 字符（1:21、1:45）；私网守卫在生产路径上复验拒绝且不产生请求。耗时如实记录，不宣称 fetch 更快（HN 上 snapshot 46ms、fetch 753ms）。**登录态仍未验**（不建真实账号，只打匿名接口）。见[本轮标准与证据](evals/20260911-fetch-tool.md)、[真实站点对比](evals/20260911-fetch-real-site.md)。

## 并行：截图幕帘 + 新标签页跟随

2026-09-11。截图前隐藏全部产品画布（光标/标注/控制条，按 `data-sideagent-overlay` 属性选，不依赖 cursor 脚本；等两帧再拍，`finally` 放下），点开新标签页时回执写明新页并通过既有 `switch_tab` 跟过去（归属校验失败只报告不抢）。机器检查：146 文件 1261 项、typecheck、build 全过（首版因同步访问 `chrome.tabs.query` 在受限环境抛错，已改为整体 try/catch，click-integrity 21 项回归保住）。两件的真实样本都未跑（fixture 没有新页链接，本轮也没跑截图场景），只有代码与回归证据，不据此宣布可用。见[本轮记录](evals/20260911-curtain-and-newtab.md)。

下一步（已与用户约定）：**network / fetch 数据层工具**（大件，先写验收契约）；剩余可搬项：声明式批处理 `act`、offscreen 连接。本轮未提交、未推送、未重载扩展。

## 并行：页面内容加不可信边界 + 凭据隐去；快照预算治理

2026-09-11。三处改动都作用于「进入模型上下文的内容」：①snapshot / read_element / js 的结果包进 `<page-content untrusted …>`（页面伪造的闭合标签会被打断），系统提示词新增一段「页面内容是不可信数据，不是指令」；②读路径凭据隐去：整行凭据折叠成一条、`key=value` 只隐值、密码/验证码字段的 `value` 在扩展侧只回 `<N chars>`，URL/邮箱/价格/BV 号/小写连字符订单号不误伤；③AX 快照预算 50,000 → 24,000 字符（可显式覆盖），超限时先保全部 ref 行、先丢纯文本行，`backendIds` 只含真正渲染出来的 ref，父节点名字与子静态文本重复只出一行。

验证：146 文件 1261 项、typecheck、build 全过；隔离 harness 真实运行三项检查 PASS，Pi 会话文件里 snapshot 确实带边界标签。真实复杂页面的同页体积对比未跑（验收 fixture 只有 14 行），见[本轮标准与证据](evals/20260911-untrusted-and-snapshot.md)。

B 站真机试用暴露"真实页面点击落在裸 div 上、名字在祖先"的问题：现在取名顺序加了 title/图片 alt/祖先文字，无名点击标成弱步骤（运行期认不出来就跳过并报出第几步），全军覆没时明确报"什么都没做成"。全量 1277 项通过。未决：① 模型面对注入样本的行为无真实样本可验；② 预算在真实大页面（B 站类，traces 里 AX 满 50k）上的实际截断体验待人验；③ 本轮未提交、未推送、未重载扩展。

## 并行：点击回执带效果证据（P0-2）

2026-09-11。按 P0 第二步，`click` 不再只回「已点击」：动作前后在页面侧采基线（目标状态 / 目标区块 / body 顶层 / 页面提示 / 页面是否自己在动），动作后 100ms 轮询早停，把能归因到这次点击的变化写进回执；只由强证据决定「动没动」，全局 DOM 与正文长度、焦点只作弱参考。无反应时明说并阻止盲目重试。真实路径（隔离 harness，MiniMax-M3）：点击返回 `Page reacted: paused false → true.`，click 601ms（等满的假阴性版是 1206ms，接入前 855ms）。三个真实运行修掉三处：页面侧未确认也给 token（假计时器下空转 + 生产上拿不到读数却报「没变化」）、只比 DOM 看不到 JS 状态变化（原生媒体控件）、元素身份按点击落点解析比按定位串重找更接近真实接收者。见[本轮标准与证据](evals/20260911-action-evidence.md)。

未决：① 无反应时最坏等满 600ms，是否收到 400ms 未测；② fill / type_text / press_key / hover / scroll 未接入；③ 「页面自己在动」的真实站点样本（弹幕/行情）未验，现有证据均来自合成单测；④ 本轮未提交、未推送、未重载扩展。

## 并行：账本下沉到执行事实（P0-1）

2026-09-11。按架构评审 P0 第一步，把任务结果账本从「模型先调 record_task_results」改为由真实执行事实派生：观察和写操作都不再需要预先登记，账本项在 `tool_start` 按真实调用生成（同工具唯一的未定位项在执行时改绑实际目标；已写明目标的登记项不静默漂移），未知/重复/接管约束不变，`record_task_results` 保留为可选计划工具。

机器检查：142 文件 1204 项、typecheck、build 全过（新增 11 项；4 条旧断言按新契约改写，原断言、理由与替代反例见验收附录）。真实路径（隔离 harness + MiniMax-M3，同 case 跑 3 次）：页面侧检查通过，**记账调用 0 次**（A/B 基线平均 3.33 次/任务、3.17 个纯登记轮），最终样本 6 轮 / 12.2s / 交付一次。三次真实运行暴露并修掉两处提示词耦合：投影把交付绑定在 `resultState` 上（自动账本下 `satisfied` 会在只读收尾前提前到达），以及改该句时误删「已交付即结束」，模型一度重复交付 8 次。见[本轮标准与证据](evals/20260911-turn-economy-ledger.md)。

未决：① `correction` / `pause` / `cancel` 等验收 case 仍带「必须先登记」的旧断言，未跑，属验证体系后续；② 轮数/耗时不是同场景对照，不宣称提速，可比的只有记账调用 0 次；③ 本轮未提交、未推送、未重载扩展。

## 并行：动效统一成一套语法；执行面板光球已落地

2026-09-11 凌晨。用户认可 Motion Language 方向后落地四条动作 + 一个深色缺陷。定三档时长（120 / 240 / 360ms）和三条曲线（swift / spring / glide），旧别名 `--spring-bounce`、`--spring-fluid` 统一指向它们，不再各自写值；7 个可点控件的按压力度收敛到 `scale(0.95)`（发送按钮原来 0.86，在 30px 圆上像被捏扁），按下走 quick+swift、松手回弹走 move+spring；模型选择器展开改成从被点按钮的中点长出、列表项只在展开那一次依次落位；工具步骤完成从"一帧替换图标"改成等待态收束退出 + 完成图标从同一点展开（这条产品里原本没有）；页面定位高亮从"500ms 内连消退一起播完"改成出现→保持→消退三段，操作完成的标记也会消退。顺带修掉深色模式下发送按钮浅底白箭头看不见（写死 `#fff`，而深色下 `--text-primary` 是浅色）。验证：typecheck、137 文件 1139 项（新增 12 项 motion 契约测试）、build 全过，扩展已重载，01 与 03 在真实渲染里实测生效。见[本轮标准与证据](evals/20260911-motion-language.md)。

05 执行面板光球按定稿的方案 B 落地：`thinking-orbs` v0.3.1（MIT）原样 vendor 进 `src/vendor/`，三个身份挂到三处——思考块 composing、工具 chip solving、记忆回执 connecting。只有正在跑的球占 rAF，跑完定格一帧；折叠、工具结束、历史回放、系统"减少动态"都不转。第一版直接用 `--text-secondary` 上色，球还要乘引擎给的 alpha，被稀释到几乎看不见，改用专用 token `--orb-ink`（深色模式自动反相）。验证：typecheck、139 文件 1160 项、build 全过，扩展已重载；用真引擎 + 产品 CSS 做的隔离渲染确认三个球形态正确、间隔 700ms 两张截图逐帧在变。真实任务里的观感待人裁。

用户问运行中的蓝色方块留不留。判断**保留但缩小**：像素格承载光球没有的两样信息——实时耗时（`处理中 · 4.0s`）和当前动作名，删掉就把这两样一起删了。5×5 / 47px → 3×3 / 16px（格子 7+3 → 4+2px），相位延迟跟着换坐标。真实面板页里用产品 CSS 渲染对照：旧 47×47、新 16×16，整块高度 205 → 197px，见 `evals/20260911-motion-language/exec-panel-loader-shrink.png`。typecheck、44 项定点测试、build 全过，扩展已重载，`dist` 里确认是 9 格 + `pixelDelay(i,3)`。

未决：① 04 / 05 / 06 的视觉都需要真实任务触发才能看，本轮没跑（会消耗额度并在用户会话留记录）；② `details` 完成收起（`run.root.open = false`）仍是瞬时的；③ chip 的图标位让给了光球，工具图标（`TOOL_ICONS`）留着没删，是否要同时保留两个待定；④ 球比原图标大 2–3px，chip 行高约 26→28.4px、记忆回执约 17→21.6px，真实消息流里的密度待人眼判断；⑤ 缩小后的等待态是否还看得出"还在跑"待人裁；⑥ chip 的 solving 球定格后（16px）像几粒散点，放大到 20px / 线宽 1.3 会密一些，要不要动待定（`evals/20260911-motion-language/exec-panel-orb-size-ab.png`）。

## 并行：示范录制第一刀已落地，编译成脚本未开始

2026-09-11。用户确认「技能」的做法：**用户亲手做一遍 → 编译成脚本 → 第二次起确定执行**，参照美团 Tabbit 的脚本，差别在脚本从哪来（Tabbit 由一句话生成，本条由真机做成功的一遍编译）。同时确认两条产品决定：脚本默认不显示代码（默认只讲步骤与完成情况，想看再点开）；调用入口要有。

隔离浏览器验收 **24/24 通过**（含编译成脚本那一刀）（`node scripts/acceptance/demo-record-run.mjs`，合成输入），跑出并修掉两个真缺陷：示范页没有任务绑定时步骤被全丢；收盘先删会话导致最后一批步骤丢失（含「提交表单」）。两条都补了单测。本轮落地「示范 → 步骤记录」：顶栏「看我做」按钮、页面侧录制（点击/输入/回车/提交）、background 内存缓冲、面板步骤列表。三条硬规矩写进 `shared/demo-record.ts`：只记语义锚点不记坐标与 DOM 路径；密码/卡号/验证码只记「填了」不记内容；示范期间写类工具被硬闸门拒绝（页面归用户）。证据：`extension/test/demo-record.test.ts` 10 项 + `extension/test/demo-session.test.ts` 5 项通过，typecheck 与 build 通过，`dist/content-record.js` 已产出。**写类工具在示范期间被拒**只有代码与单测证据：隔离实例没有 agent 连接，发不出真实 tool_call，浏览器行为证据待补。**人手验收未做**（合成输入不等于人手）。见[本轮标准](evals/20260911-skill-from-demo.md)。

编译成脚本（第二刀）已完成并通过隔离验收：面板把示范 + 一句话意图交给伴随进程，确定性编译成技能（人话步骤 / 完成凭证 / 折叠脚本），落 `~/.sideagent/skills/`，带版本与忘记。修订/回退/可能过期已实现并有单测（1261 项全绿）：重新示范同一技能 → 版本 +1 且旧版归档、换站点不覆盖；回到上一版；这次不太对攒线索；连续 3 次"页面变了"标记可能过期。真机验证到"已更新（第 2 版）"与逐次回退；**未定位**：rollback/note 的 `skill_result` 回执到不了面板（list 正常），已用本地消息 + 定时刷新兜底。技能怎么持续优化与修订的设计已冻结（证据累积、版本只在用户确认的修订或重新示范时抬、连续 3 次"页面变了"标记可能过期）：见[本文](devlog/20260911-01-技能的持续优化与修订.md)。**第三刀已在用户真浏览器里跑通**：真的点「看我做」→ 真的点页面链接 → 编译 → 「照上次那样跑」，结果 `ok:true / 5 步 / 1.4 秒`，页面真的从 Issues 切到 Labels，运行窗口内没有任何模型活动。真机还抓出并修掉四个缺陷（示范跨页断、生成脚本的引号转义导致页面语法错误、凭证取到裸标签、跨导航的无名残影步）。见[验收](evals/20260911-skill-from-demo.md)第三节；第 5、6、9 条验收仍需要用户给一个真实站点和它的第二份材料。

## 历史：侧栏历史写爆扩展存储已修；光标状态层人裁可用

2026-09-10 晚—09-11 凌晨。用户报侧栏显示「未连接」。排查结论：**后台 service worker 没有运行**，因此没有进程去拉伴随进程；面板与后台之间的重连本身是好的（实测停掉后台 10 秒内自己回来）。查后台异常找到真故障：`chrome.storage.local` 被 `history:<会话 id>` 键写满（10,485,715 / 10,485,760 字节 = 99.9996%），后台持续抛 `Resource::kQuotaBytes quota exceeded`，草稿、教学模式、标注动效、token 全部静默写不进去。

用户批准方案并先手工清空（释放 9.99 MB，备份 `~/.sideagent/backups/panel-history-2026-09-10T15-46-13.jsonl`）。已修：单会话落盘按 UTF-8 字节截到 256 KB；只保留最近 8 个会话的键；只剩 status 的会话不落盘（服务端给每个会话都推状态，一次 idle 就写一个键）；写失败打日志、不再抛未处理异常；启动恢复失败也照常连接（`transport.start()` 移出 `.then()`）。验证：136 文件 1127 项、类型检查、构建通过；扩展已重载且运行代码与构建逐字节一致；实测由 119 键 / 555 KB 收敛到 8 键 / 60 KB（连续 40 秒稳定）。见[本轮标准与证据](evals/20260910-panel-history-quota.md)。

光标状态层第二轮此前已通过机器检查；用户跑完真机任务后反馈「还可以」，人裁记为可用。遮挡细节（是否压住 logo/正文、字号是否够）未逐项确认。见[光标验收](evals/20260910-cursor-status.md)。

未决：测试套件在并发负载下会抖动（单项单跑 3 次全过、全量单跑绿；与构建并发、或与同工作区的并行工作同时跑时出现 2–3 项失败，已见 `extension/test/click-integrity.test.ts` 的 B1），不是本轮改动引入，未逐项复现。同一工作区另有并行工作：23:47–00:13 出现 orb 设计预览与截图（未跟踪，本轮未提交）。

## 历史：正常使用采集已上线；被自己打断的半句不再丢失

2026-09-10 晚。第一批真实数据（19:18–19:22，14 轮）证明采集链路可用，并指出一个具体缺陷：没等回答就接着说时，上一轮的转写晚到，被整段当成旧内容丢掉（14 轮丢 2 轮，且都是长句前半段），助手只收到碎片，看起来答非所问。已修：被打断那一轮的迟到半句（2.5 秒内）并进当前轮，旧音频与旧工具调用仍旧丢弃。全量 1103 项、typecheck、build 通过，扩展已重载，宿主已跑新代码。仍需用下一批真实数据核对丢轮比例是否下降。见[本轮标准](evals/20260910-voice-late-transcript-merge.md)。

## 历史：语音采集改为正常使用中自动进行

2026-09-10：用户改方向——不做手动诊断，改为日常使用语音时自动收集数据，用真实数据判断问题出在哪一层，并加一个一秒钟的“这条不对”标记。已实现：正常语音会话自动记录 C1（实际上行字节）、旧轮过滤前的原始转写、转发文字、界面文字、时序与缺口，并保存 C0（连续收音）为 WAV，落在 `~/.sideagent/voice-capture/`（音频保留 14 天或 2GB，先到为准，`npm run capture:clear` 一键清空）。语音行为按定点测试保持不变（照常路由、回答、steer），日常使用无差别与标记手感仍待人验收。主代理复查发现并修掉了落盘接线未接上的真缺陷。验证：typecheck/build 与 27 文件 216 项通过；扩展已重载。识别退化仍未定位，语音未判修复。见[本轮标准](evals/20260910-voice-capture-normal-mode.md)与[失败修订](tasks/20260910-voice-capture/lessons.md)。

## 历史：语音诊断第1步（手动诊断记录链，保留可用）

2026-09-10 用户同意Pro评审后的诊断方向并要求逐步实施。本轮只增加可见、手动开启的音频/转写取证与服务端禁动作；先打通一条记录，再做同源三路真人对照。识别退化尚未定位，语音未判修复。见[本轮标准](evals/20260910-voice-diagnostic-capture.md)。既有光标与其他工作区修改保留。

## 当前：光标状态层已上线，待用户体验判断

2026-09-10 22:00 补修：真机 21:29 那次"点了发送被拦成待确认"，系统却把它记成"已执行"、账本判完成，模型自己起疑后又读页面才敢确认。已改为：被拦下的点击回执按"未执行"上报，写作项转"结果未定"（只能靠页面证据解除），只读项退回待做，重新登记也洗不回待做。代价是"结果未定"期间同一轮的其它写操作会被暂停；用户点取消时页面没有新证据，这一轮会卡到下一轮重新开账。全量 136 文件 1122 项、类型检查、构建通过，扩展已重载且运行代码与构建 hash 一致；22:32 宿主已重连（ego lite 与 Dia），对之后的每一轮生效，Dia 那个 9 月 9 日的旧宿主一并清掉。真实路径未跑。见[本轮标准](evals/20260910-held-click-not-executed.md)。

2026-09-10 21:40 补修：真机确认"待确认"名牌在确认后不消失（ChatGPT 发送按钮那次）。根因是拿住态结束时只摘双键、不撤标注锚框；已改为拿住态结束即撤掉本次锚框，页面上其他任务标注不受影响。定向检查、状态层检查、全量 1117 项、类型检查、构建全绿，扩展已重载且运行代码与构建 hash 一致。同段日志里另发现两处待裁决问题（取消会清掉整页标注、held click 被记成已执行）记在[本轮验收](evals/20260910-held-confirm-mark-cleanup.md)的"未决"。

2026-09-10 晚：第二轮光标可视化已按用户确认的样态实现并重载——等待/读页面挂在光标名牌上（"正在等模型响应 · 已等 N 秒" / "正在读这个页面"），完成停 1.5 秒自动收并回角落，失败停在原地显示"这一步没做成"直到下一轮或接管，它在别的标签页干活而你看的是当前页时，右上角出现可点胶囊，点了切过去；字号按用户选择缩到 11px / 9.5px。机器证据：14 项定点单测、隔离浏览器 PASS（含深浅底、拿住双键不被状态覆盖）、第一轮回归 PASS、全量 136 文件 1117 项、typecheck/build 通过；正式扩展已重载且运行代码与构建 hash 一致。真实网页可辨认度、是否挡内容、胶囊跳转手感待人验收。见[本轮标准与证据](evals/20260910-cursor-status.md)。

## 历史：光标可见性第一轮已生效（用户已确认可用）

2026-09-10：用户批准 P0，已增强光标为 44px、加入实际动作名牌和持续目标边框，修正反馈顺序并移除额外动画等待。用户看录屏确认第一轮可用；第二轮等待/阅读/跨页状态层见上条。见[第一轮标准、录屏与证据](evals/20260910-cursor-visibility.md)。既有语音暂停结论保持有效。

## 最新：真人反馈识别退化，停止修改并交接GPT 6 Pro

2026-09-10 15:51用户明确表示识别变差、体验不满意，要求当前全部内容commit并push，交由GPT 6 Pro指导。本次保存工作快照，不代表产品验收通过；此前“待试用”已被本次负反馈取代。已停止继续修改产品。入口：[交接与失败证据](tasks/20260910-voice-review/handoff-gpt6-pro.md)。

## 人声检测：体验未通过，已停止修改

用户已反馈识别退化，取代此前“待试用”状态。已有检测样本及合成收音检查只证明触发与提交，未验证真实麦克风输入到语音识别结果的完整路径。后续产品修改按接手会话的新指令决定；范围及原始证据见[人声检测验收](evals/20260910-voice-speech-detection.md)。

## 任务中连续语音对话：整体验收未通过，随语音工作交接

先接话、后台结果排队和闲聊期间保留待接收委托已实现。较早版本的成功样本不能证明最终版本；最新先接话样本之后遇到服务过载，日常侧栏完整语音路径尚未验收通过。回放、时延与失败证据见[本轮验收](evals/20260910-live-dialogue.md)，后续统一从顶部交接入口续接。

更新：2026-09-10。本页是当前进度入口，原始标准与实测证据留在对应验收文件。提交记录以 Git 为准；提交不改变未通过或暂停的验收结论。

开发维护：用户已批准的规范精简已完成，确认条件、验收纠错和记录触发已调整，收尾细节改为按需读取。仅涉及文档，规则与链接检查见[本轮验收](evals/20260910-working-rules-simplification.md)。本轮未提交推送。

历史阶段记录：较早的语音根因修改、测试及试用门槛修订保留在[原验收](evals/20260909-voice-turn-recovery-and-readiness.md)。以下标为“前轮”的内容是历史证据摘要，不覆盖顶部当前裁决。

## 当前：暂停5秒文字收尾任务，验收对象待重新对齐

用户在澄清5秒的含义后指出：侧栏文字显示一直正常，复杂任务生成长内容超过5秒合理。Codex确认“完整文字交付+运行结束”的指标混入正常输出耗时，未准确对应原始问题，已暂停推进并中断Grok。原始问题仍是简单操作等待及追加要求接续，尚未重新批准下一项验收对象。

已完成的有限结果保留：候选改善原生控件状态返回，14项定点/typecheck/build通过；单例正确完成，长串无效读取消失，但结果齐备到SDK停止仍24.063秒，未达旧5秒标准。完整正文计时另有格式匹配缺口，未作正式验收。正式产品源码与A一致，候选未回接、未重载、未提交。详细记录见[原验收定义](evals/20260910-task-completion-tail.md)及任务目录；此次暂停不改判候选通过。

## 前轮：登记与目标绑定比较完成，保留 A

2026-09-10：用户明确批准解决登记/绑定的额外往返，并指定Codex规划与评审、DeepSeek实现及A/B测试。主代理已冻结[本次标准](evals/20260910-registration-binding-ab.md)和[执行委派](tasks/20260910-binding-ab/assignment.md)。A为当前完整工作区；B比较唯一pending项的执行时绑定；C比较动作携带原待办编号的原子绑定。均先通过防错绑/unknown/防重放边界，再做18次同M3/medium交错对照。

DeepSeek已完成隔离候选和离线修复，见[报告](tasks/20260910-binding-ab/report.md)。其报告B/C全量各1061项通过、类型检查和构建通过；最后仅测量脚本改动，13项定点复跑通过，[摘要](tasks/20260910-binding-ab/checks.log)由执行者抄录，非独立复跑证据。主代理静态评审已退回并复查身份/持久化及统计入口问题，记录见[产品评审](tasks/20260910-binding-ab/review-r1.md)与[脚本评审](tasks/20260910-binding-ab/review-harness.md)。

用户已确认主代理在宿主启动[DeepSeek脚本](tasks/20260910-binding-ab/run-ab.mjs)，分析和修正继续由DeepSeek执行。首批因隔离目录无.git而启动失败，原记录保留；修复后有效批次20260910T045813-60319完成18次真实对照。用户在测试过程中明确改用单一指标：六次全部正确完成的平均耗时，任一次真实失败即不达标。

最终A 6/6成功、平均83.701秒；B 5/6、C 5/6均不达标。Codex复核原始JSON与源码无漂移，按指标保留A。B/C不回接，正式产品未重载或提交。结论与证据见[选型结果](tasks/20260910-binding-ab/decision.md)。

## 前轮：浏览器状态工具与组合执行已修复，整体时延仍未通过

2026-09-10：用户同意调研判断并要求优先向ego-lite学习、先改善工具环境。当前M3与medium未调整；已扩展read_element的原生状态读取、类型明确的expect条件和有界等待，修复browser_run子步骤经异步进度队列导致正确登记被误拒的接线问题。工具和程序继续共用权限/目标/回执。

机器检查：128文件1024项、类型检查、构建通过；真实Chrome7项状态/失败/换文档反例通过。最终自主M3任务暂停一次并读对三条评论，使用新状态检查、0次JS探查，9轮29.561秒。旧环境部分基线10轮21.762秒；不能称整体提速。中间的慢样本和错误都保留在[本轮验收](evals/20260910-browser-environment-state.md)。

12:05正式扩展重载，native进程和面板重连，在线后台/侧栏与构建hash一致。未提交推送。当前交付是工具环境首轮修复；日常流畅性、登记流程多余往返、改口接续和同run旧播报仍待处理。既有R3未通过项不变。总体取舍依据仍为[系统调研](research/20260910-agent-responsiveness/report.md)。

## 前轮：第三轮独立验收未通过，R3仍缺操作与证据绑定

原主代理独立复跑旧无关标题反例7/7、实现者正例6/6、定点98项均通过。但新增三个真实隔离Chrome反例均错误解除unknown，随后重复新增，DOM/服务端均为2条：隐藏旧内容（7/10）、无关区域更新（5/8）、同tabId换文档（6/9）。三个反例均为产品断言失败，没有基础设施异常。

判定：A4/A5/A8仍未满足，退回修正窄恢复路径的证据契约；不能把前后文字差异当作本次操作证明。原标准未改。详细原因、源码与返工要求见[第三轮报告](evals/20260909-write-receipt-loss-independent-review-r3.md)，原实现者报告保留。真实模型/侧栏A9未跑，产品源码未改，未提交推送或重载用户扩展。

## 前轮：写入回执丢失独立验收未通过

2026-09-09：原主代理独立复跑固定浏览器15/15与定点82项，均通过。但新增真实Chrome反例确认：写入后返回明确unknown的非超时错误，账本误变blocked，重试后页面/服务端均从1条变2条。执行事实因SDK与RPC编号无映射而丢失，后续错误文本兜底误判。晚到回执无生产回接、页面核查无恢复入口、重复投递无执行器去重也未满足标准。

判定：退回实现者修复，未整体验收通过。权威结果与R1—R4返工项见[独立验收报告](evals/20260909-write-receipt-loss-independent-review.md)。原冻结标准不变；A5/A9真实模型/侧栏等未跑项明确留待机制修复后验证。实现者原997项/typecheck/build报告保留在[自测报告](evals/20260909-write-receipt-loss-fixed.json)及开发日志13，不再用作整体闭环结论。本轮未改产品、提交、推送或重载用户扩展。

## 开发维护：Codex 任务收尾经验沉淀

2026-09-09：用户明确目的为当前 Codex 开发中的经验沉淀，并批准以任务收尾约定落地。当前主代理已更新规范与 [经验库入口](../.kimi-code/wiki/index.md)，用本次“验收入口错位”完成提炼、核对、记录和待审；下一步由用户判断是否采纳 [模板入口字段提案](../.kimi-code/wiki/proposals/20260909-acceptance-entry-field.md)。不依赖 Kimi 退出事件，未新增后台机制或启用 skill。验证与范围见 [本轮验收](evals/20260909-codex-experience-closeout.md)。

旧 Kimi 接线的实测、候选纠错及工程检查保留在 [前轮记录](evals/20260909-wiki-consolidation-repair.md)，不将其当成当前 Codex 路径的验收证据。原 probe-scripts 提案仍待审。

## 本次任务已结束：S2 真人失败后的机制修复

2026-09-09 21:09 真人改口后没有圈画；21:14 用户要求停止逐案例修补，追查通用机制。已修复三处连接：默认 AX 快照给内容引用；同一节点传入绘制层，正文滚动后不扩大到容器；明确未执行的定位失败可以依据新观察恢复，不误当成撤销授权。

原并排卡片改口、不同正文/图片/嵌套内容的定位失败恢复均有真实 M3 + 隔离浏览器结果；同名标题、文字范围、图片、Shadow DOM 和滚动后锚定的直接工具检查通过。最终 125 文件 991 项测试、类型检查、构建、diff 通过。扩展已重载，实际 background 与最终构建 hash 一致。真人日常体验尚未通过；未提交推送，未进入 TTS。证据和中途失败见[机制验收](evals/20260909-observation-action-recovery.md)。

收尾：按用户要求结束本次任务，不继续开发或追加测试。现有改动留在工作区，未提交、未推送。后续若继续，从本节及机制验收记录接续；待确认的是日常真实使用的可靠性与等待体验，不重做已通过检查，也不把机器通过记作真人验收。技术原因和失败教训已由代码、测试、验收文档及开发日志覆盖，本次不重复新建经验或提案。

此前 S2 接续的旧回执隔离、暂停恢复、unknown 不重放等标准继续有效，见[原标准](evals/20260909-s2-lifecycle.md)与[分阶段结果](evals/20260909-harness-s2-results.json)。用户要求按改动定点检查，稳定后一次全量；每次产品改动后由主代理重载并确认生效。

## 前轮：S1 语音与文字使用同一浏览器能力（机器检查完成，待用户检查）

2026-09-09：正常页面委托与空闲纠正进入同一有工具会话，圈画保留手绘轮廓和轻微抖动。禁用mark后，JS与browser_run不能绕过；正常权限下JS实际读写与动态禁用/恢复已独立通过。

主代理最终115文件938项测试、typecheck/build/diff通过。最新隔离浏览器disabled 9项、正常JS 11项、保留措辞圈画11项通过，源码hash一致，截图已检查。原19次套件的4个失败、1个disabled假阳性及后续修复均保留，未伪记为最终hash的19次全绿。见[冻结标准与S1结果](evals/20260909-harness-capability-continuity.md)及[结果台账](evals/20260909-harness-s1-results.json)。

当前停在S1待用户实际检查。未重载用户扩展、提交或推送；合成麦克风/CDP隔离路径不代称真人或已加载扩展验收。S2生命周期和TTS/时延没有进入本轮。用户的reachable-models与全部既有未提交改动已保留。

## 前轮：显式用户消息交付实验（机器验收通过，体验被用户否决后进入S1）

2026-09-09 17:22：Grok/Kimi/Anti Gravity 已停笔，Boss 独立终检通过 886 项测试、类型检查、构建和 31 项真实侧栏/模型对话检查。任务结果、追问、纠正、语音重开及纯文字任务均形成正式交付，语音与界面正文相同，播放回执绑定同一 id。最终源码 hash 一致。见[冻结标准与终检](evals/20260909-explicit-user-delivery.md)。

本轮真实模型没有主动调用 send_user_message；两个任务通过有限补交，三个追问通过同一任务模型组织回答，共新增 5 次调用。三次追问首音频等待约 12、19、22 秒。内容一致性通过，交流速度和自然听感尚未由人接受。未重载用户扩展、提交或推送。

前一停点：2026-09-09 15:50 用户要求暂停表达修补、先读Grok Bot重建仓库。已暂停AG-SPEECH-R2并保留所有代码。自动恢复/底栏局部真实验收通过；完整连续对话回归仍有Markdown原文播报与指代不明确的失败，整体不算完成。最新证据见[调研](research/20260909-grok-message-delivery.md)、[恢复验收](evals/20260909-voice-auto-recovery.md)和[接收回应/底栏验收](evals/20260909-voice-ack-composer-fit.md)。未提交、推送或重载用户扩展。

## 前轮：语音连续对话第一轮

本地实现与Boss独立工程验收完成，尚未重载用户扩展、未提交或推送。当前已提交基线仍为40993d5。

任务最终文字结果与最近原话进入同会话语音上下文；内容指代不再直接当成缺会话名；有结果时生成发现短答，保留范围限制；暂停/终止/错误回执不被旧报告覆盖。同会话关闭重开语音可继续追问，不重播旧结果或重做动作。

Boss独立13项机制测试、全量788项、typecheck/build/diff通过。隔离Chrome+真实M3+Step语音的读邮箱、追问、纠正、重开继续8项机器检查通过，逐句检查没有再次篡改实体类型。首两次真实失败及修复记录保留。详见[完成标准与结果](evals/20260909-voice-conversation-continuity.md)。

真人麦克风、用户已加载扩展路径和自然听感待验收。本轮不是跨进程或跨天长期记忆交付；输入为合成语音，不能当作真人耳麦验收。

## 已有产品基础

| 能力 | 已有结果 | 未解决的边界 |
|---|---|---|
| 文字浏览器操作 | 导航、读页、截图、点击、填写与组合执行已落地 | 复杂任务仍需核对真实结果 |
| 多会话 | 独立任务、历史、草稿、附件和页面集合；切换不停止其他会话 | 不能把跨会话读取当作共享任务上下文 |
| 多成员协作 | 主动分工、同页字段填写、用户接管和页面移交有真实验证 | 短表单样本未证明更快 |
| 显式记忆 | 保存、跨会话使用、修改与忘记已有真实侧栏验证 | 完整退出Chrome后的组合验收、不同Chrome配置隔离仍待补 |
| 纠正经验 | 明确纠正可整理为待验证做法，再用于同站点新任务 | 尚未证明总体错误率下降；未接EverOS或自动技能升级 |

以上为项目已有验证记录，本次文档整理没有重新执行这些路径。依据：[使用说明](../README.md)、[会话与协作日志](devlog/20260908-01-会话独立运行与同页协作.md)、[记忆日志](devlog/20260908-03-跨会话记忆A版落地.md)、[经验日志](devlog/20260908-04-让纠正进入下一次网页任务.md)。

## 本轮语音工作

| 阶段 | 当前结果 | 完成判断 |
|---|---|---|
| P0 基线 | 文字和合成语音均得到预算800、升序699/799、单次启动 | 阶段通过 |
| P1 统一入口与回执 | 请求身份、去重、落盘、查询、侧栏重载及预算回归通过 | 阶段自动部分通过 |
| P2 启动与意图分流 | 真人筛选成功；结构化路由已实现 | 新校验和低延迟分类配置实验144/144通过，已采用；真实三轮仍继续 |
| P3 暂停、恢复、终止 | 强化后的真实双成员完整路径一轮通过，17项检查，含主动写探针和控制期间观察 | 总方案要求的三轮连续回归及真人控制闭环未完成 |
| P4 页面资料与主动状态 | 图片数字7391、移除后的选区海风进入实际表单；主动结束播报通过一轮 | 更多状态与三轮回归未完成 |
| P5 指定会话与复合指令 | 指定目标暂停并保存600通过；原话与回执双归属已实现 | 已加控制版本、持久计划与逐步结果；真实完整复合路径待复验 |
| P6 真人耳麦 | 用户确认筛选、对话、追问、插话立即停播 | 暂停/人工改页/恢复/终止闭环和20轮时延统计未完成 |
| 新增：语音查看页面 | 独立当前页只读授权，核对文档身份与可见区域；真实双页观察已通过 | 已重载；图像独有标记与同URL重载故障正在复验 |

详细结果见[验收台账](evals/20260909-voice-dispatch-results.md)，固定门槛见[总方案](evals/20260908-voice-dispatch-parity-plan.md)。

## 当前问题

| 问题 | 已知事实 | 状态 |
|---|---|---|
| 完整听写后没有回复 | 两次真人记录都有完整转写，随后出现空识别；重说后成功 | 已修候选空轮丢请求；真实生产采音空轮恢复通过，原请求只路由一次。不能据此断言原真人录音的触发原因 |
| 语音持续重连 | 重试额度会被过旧的健康连接时间反复清零 | 已重载；截止时间、待播放结果及有限重试有测试，真实故障复验仍待补；原始断线原因仍未知 |
| 停播后仍显示处理中 | 无音频的ready没有退出thinking | 已加载；状态区分识别、理解、读页、等待控制和准备回答 |
| 修改后继续漏动作 | 指定会话只保存预算，没有恢复 | 已加遗漏检查与分类修复，真实复测未做 |
| 口头说正在确认 | 真人“你确认一下”得到口头答复，尚无相应实际查看证据 | 需按只读观察路径复核，不能认定已确认 |

## 检查、加载与交接

当前本地最近全量89个测试文件、732项通过，typecheck/build通过；分类请求采用已通过144次实验的无推理设置。生产WebAudio图停播20轮P95上界5.33ms，包含一个渲染周期，不代表真人耳机端测量。初次生产采音回答约5.56秒，整体4秒目标仍未通过。

用户已明确交回浏览器用于自动测试，扩展已重载。真实空轮恢复、当前页观察及强化控制各有证据，详见[评审修复标准](evals/20260909-voice-reliability-r1-r5.md)。未完成项仍为连续三轮、部分故障/复合计划/重启组合，以及真人控制闭环与时延。

本轮继续完成自动验收，随后请用户做真人路径；新修复不沿用上一次开发快照的推送授权。云端原评审入口见[评审问题](reviews/20260909-voice-review-brief.md)。

---

## 待办（另一条线的记录写在这里，避免被覆盖）：技能／观察 UI 的设计优化

**已按用户选定的方案一实现（2026-09-11 05:05）**：一个入口「知识」抽屉，技能／记忆分段；技能行默认只露「名字 + 事实行 + 一个主按钮」，凭证／步骤／脚本／重新示范／不太对／回退／忘掉收进一次展开；观察开关压到分段行右侧（打开时显示「已记 N 段」）；过期的技能事实行标「过期」、主按钮仍是「照跑」，点下去就地确认（不用原生对话框——那会把面板卡住）。原型见 [docs/previews/knowledge-drawer.html](previews/knowledge-drawer.html)（含改前／方案一／方案二／记忆分区四栏、深色切换）。

隔离无头实例里已渲染真面板核对：抽屉、分段、观察行、空态都对；**带真实技能数据的列表（跑过几次、过期标记、候选卡）还需在真机上由用户看一眼**。全量 151 文件 1326 项通过。
