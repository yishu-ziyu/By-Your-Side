# By Your Side 复盘：版本情况与问题清单

调查日期：2026-09-26　调查对象：`main@0c40421`（与 origin/main 相同）　只读调查，没有改项目、没有碰 issue、没有 push。

## 一句话结论

项目从来没有发过版本：版本号停在 0.1.0，一次都没改过。当前的实际状态是：“只装扩展”这条主路径已经在隔离环境里跑通，但 09-25 的修复都还没装进日常 Chrome，真人也还没试用。09-25 定下的下一步是“先做错误分析”，这件事还没开始。文档里有一批过期记录：两个已经修好的 issue 还开着；STATUS 写着“未推送”“CI 未运行”，实际都已经做了；④ 工作流页上显示未完成的步骤，其实已经完成。还有两个现存问题会让“测试全绿”的说法站不住：在当前 main 上 `npm test` 的退出码是 1；在 macOS 上用干净的 `npm ci` 装完依赖，单元测试根本启动不了。

## 一、版本情况

| 项 | 实际情况 | 证据 |
|---|---|---|
| 扩展 / agent 包版本 | 都是 0.1.0 | `extension/package.json:3`、`agent/package.json:3`、`extension/manifest.json:4` |
| 根 package.json | 名字是 `sideagent`，没有 `version` 字段 | `package.json:2` |
| 协议 / 宿主版本 | `PROTOCOL_VERSION = 1`、`STORAGE_SCHEMA_VERSION = 1`、`HOST_VERSION = "0.1.0"` | `shared/protocol.ts:17,19,21` |
| Git tag / Release | 0 个 tag，GitHub 上没有 release | `git tag` 为空 |
| 分支 | 本地 main = origin/main = `0c40421`（2026-09-25）；远端只有 main | `git rev-parse origin/main HEAD` |
| 历史 | 193 个提交，时间从 2026-09-03（`8b7364c`）到 2026-09-25 | `git rev-list --count HEAD` |
| Issue / PR | 开着的 issue 3 个（#1、#2、#4），没有关闭的 issue；PR #3、#5 状态是 closed 但没有 merged，它们的提交通过集成分支进了 main | `gh-axi issue list --state all`、`gh-axi pr list --state all`、`git merge-base --is-ancestor` |

版本上的不一致有这几处：

1. **扩展版本号有 5 处写死成字面量 `"0.1.0"`**（`extension/src/background/uplink.ts:207,243,292`、`extension/src/inproc/browser-host.ts:103`、`agent/src/host-core.ts:135`），没有从 manifest 读取。以后只改 manifest 的话，握手时上报的版本就会和实际版本对不上。
2. **命名有三套**：产品名叫 “By Your Side”（manifest），npm 包叫 `@sideagent/*`，本机宿主叫 `com.sideagent.host`，配置目录是 `~/.sideagent/`。09-07 改名时只改了仓库名和产品名（`docs/devlog/20260907-03-...`）。
3. **0.1.0 从来没有变过**，也没有任何发布门槛通过的记录。09-11 的发布收敛卡片（`docs/tasks/20260911-release-converge/ISSUE_CARDS.md`，最后更新于 09-13）全部没有勾选；STATUS 开头写的也是“尚无完整发布通过结论”。

## 二、我做了什么

- 通读了 `docs/STATUS.md`、`ROADMAP.md`、`NOTES.md`、`docs/work/`（④ 工作流、09-23 交接）、`docs/tasks/` 目录、09-19 到 09-25 的主要 `docs/evals/`、最近的 `docs/devlog/`，还有 `docs/reviews/`、`docs/diagnostics/` 的目录。`docs/diagnostics/` 下全是 09-09 的产物，没有新的待办。
- 用 `gh-axi` 读了 issue #1、#2、#4，PR #3、#5，以及 CI 运行记录。
- 在一次性 worktree 里执行了下面这些命令（结果都是当次运行得到的）：

| 命令 | 退出码 | 结果 |
|---|---|---|
| `npm ci` | 0 | 装了 294 个包 |
| `npm run test:unit`（刚执行完 `npm ci` 时） | **1** | vitest 启动失败：`Cannot find native binding`，缺 `@rolldown/binding-darwin-arm64` |
| `npm install --no-save @rolldown/binding-darwin-arm64@1.2.7` 之后再跑 `npm run test:unit` | **1** | 284 个文件、3059 项**全部通过**，但有 1 个未处理的 rejection：`uplink.ts:46 chrome.storage.onChanged` 是 undefined，来源是 `extension/test/delivery-receipt.test.ts` |
| 单独跑 `npx vitest run extension/test/delivery-receipt.test.ts` 两次 | 1、1 | 两次都是 6 项通过、1 个 error，稳定复现 |
| `npm run typecheck` | 0 | 通过 |
| `npm run check:docs` | 0 | 574 份文档，0 错误 |
| `npm run check:architecture` | 0 | 273 个生产文件通过 |
| `npm run test:scale` | 0 | 2 项通过 |
| `npm run build`（只写 worktree 里的 dist，不影响日常 Chrome） | 0 | 构建完成 |
| `node scripts/lint-changed.mjs --all` | 0 | 没有新违规；baseline 里还留着 4335 条旧违规 |

没有跑真实路径、日常 Chrome 或语音相关的验收，因为这些需要日常浏览器、模型凭据和真人参与，不在只读范围内。

## 三、问题清单

### 类别 1：进行中（已经开工，还没收尾）

| # | 问题（一句话） | 证据 | 最后动过 |
|---|---|---|---|
| 1.1 | “任务核心搬进扩展”（④）：隔离环境里已经通过，但 09-25 的多项修复还没加载进日常 Chrome 复验；备用模型只在协议层验证过，没在真实故障下切换过；真人还没试用 | `docs/evals/20260925-pure-extension-daily.md` 第 7、8 条未勾；`docs/evals/20260924-core-into-extension.md:12-13` | 09-25 `f0b8760` |
| 1.2 | 语音插话停声：停声本身已达标（6/6 在 0 ms 内停下），但插话后的新问题只有 3/6 得到回答，没回答的 3 次都是 StepFun 取消慢或卡住；附和词会不会误停、真人手感都还没验 | `docs/evals/20260925-voice-barge-in.md:24-28,60` | 09-25 `3b53de4` |
| 1.3 | “没做完的一行”依赖模型自己填 `unfinished`，有 1 次模型没配合；回答淡入的手感等人来判断 | `docs/evals/20260925-unfinished-line-and-answer-reveal.md:15`；STATUS 表第 2 行 | 09-25 `93a37b9` |
| 1.4 | 对标 Sitegeist：文件卡片下载已经做好，但网页文件在侧栏内预览没做；“没做却说做了”的纠正在真实路径上一次都没触发过；`browser_run` 在扩展里还出现过 `process is not defined`；“只在复制任务里要求逐字一致”靠词表判断，词表以外的说法不受保护 | `docs/evals/20260925-sitegeist-parity.md` 第 4、6 条；`docs/evals/20260925-artifacts-files.md` 第 5 条 | 09-25 `e7389ea` |
| 1.5 | “执行过页面脚本后必须重读页面才能核对‘没保存’”：只有单元测试覆盖，真实路径上 8 条任务都没用到脚本，规则没被触发过 | `docs/evals/20260925-unit-suite-failures.md` 末段 | 09-25 `c3b79be` |
| 1.6 | 用户点选元素：文字主会话已经完成；语音、刷新/接管、多会话、iframe/Shadow DOM 负例都没跑 | `docs/evals/20260923-user-points-element.md:66-68` | 09-24 `2793c42` |
| 1.7 | 新配色、语音人设“罗宾式”的口吻：实现已完成，等人来判断观感和试听；罗宾式人设下圈画不稳，原因是服务端断句 | `docs/evals/20260925-claude-aligned-palette.md:21`；`docs/evals/20260924-voice-persona.md:10-11` | 09-25 `75d3f83` / 09-24 |
| 1.8 | 语音服务卡住：产品这边已经改成约 22 秒发现并请用户重说，但卡住的根源在 StepFun（傍晚 6 次里卡住 4 次，下午 10 次里 6 次），没有解决 | `docs/evals/20260925-pure-extension-daily.md` 失败与修正记录 | 09-25 `f0b8760` |

### 类别 2：未解决（已知，但还没开工或已经停滞）

**当前代码上确认存在的问题（我今天复现了）：**

| # | 问题（一句话） | 证据 | 最后动过 |
|---|---|---|---|
| 2.1 | **`npm test` 和 `npm run check` 在当前 main 上的退出码是 1**：3059 项全部通过，但 `delivery-receipt.test.ts` 的 chrome 替身缺少 `storage.onChanged`，vitest 把未处理的 rejection 算成失败。09-25 修 30 个旧失败时补了 `resume-entry` 的同类替身，这一处漏了 | 本次运行两次都复现；原因来自 `de381b0`（`Uplink.start` 新增了 onChanged 监听） | 09-24 `de381b0` |
| 2.2 | **在 macOS 上用干净的 `npm ci` 装完依赖，单元测试启动不了**：`package-lock.json` 里没有 `@rolldown/binding-darwin-arm64`（lock 里有 darwin-x64，没有 arm64）。④ 工作流页只记了手动补装的办法，lock 文件一直没修 | 本次运行已复现；`docs/work/20260924-core-into-extension.md` 风险第 6 条 | 09-24 `dfa5202` |
| 2.3 | CI 从来不跑类型检查和单元测试：`docs.yml` 只查文档，`e2e.yml` 只跑隔离验收，而且只能手动触发。它的注释写着“Linux 第一次变绿后改成 push/PR 触发”，09-23 已经跑绿了一次，但触发方式一直没改。所以 2.1 这种问题 CI 发现不了 | `.github/workflows/e2e.yml:2-4`；运行 `35880574337` 成功 | 09-23 `517376c` |
| 2.4 | CAP-02 的 `stopSessionPageEvents` 仍然没有调用方：会话停止时不会释放 filechooser 拦截等页面事件。`armFreshNetworkCapture`、`pageEventLedger` 同样没接线；`VoiceService` 的 `onSpokenAck`/`recordSpokenAck` 已经没人用 | 在 `extension/src agent/src shared` 下 grep，只有定义所在的文件（`extension/src/background/page-events.ts:514`）；`docs/evals/20260923-repo-cleanup.md:68-71,123-127` | 09-23 `1e3ae6f` |

**文档里记着、代码里没有进展的问题：**

| # | 问题（一句话） | 证据 | 最后动过 |
|---|---|---|---|
| 2.5 | **错误分析（09-25 定的下一步第 1 项）还没开始**：之后的 4 个提交都是修测试、修规则，仓库里找不到查看页，也没有 30 条记录的判定结果 | `docs/ROADMAP.md` 第 7 条；`98e283a` 之后的提交是 `4dfecc1`、`c3b79be`、`380c692`、`0c40421` | 09-25 `98e283a` |
| 2.6 | 语音服务对比实验（下一步第 2 项）没开始；ROADMAP 已经把语音稳定性定为发布阻断项 | `docs/STATUS.md` 下一步第 2 项 | 09-25 |
| 2.7 | “陌生人在干净电脑上自己安装、配置模型、完成第一个任务”从来没验证过；`install-host.mjs` 只适配 macOS，而且依赖 Node 和仓库路径（如果走纯扩展路线，这一点可能就不重要了） | `docs/ROADMAP.md` 第 5、7 条；`docs/evals/20260923-user-points-element.md:70-76` | 09-25 |
| 2.8 | “即时”还没做到：翻译约 17–20 秒，圈画 6–51 秒；“多快才算即时”还没定 | `docs/devlog/20260925-01-...`；`docs/evals/20260925-pure-extension-daily.md` 表格 | 09-25 |
| 2.9 | 改口后要改写同一个字段时，会被旧的成功回执挡住，`confirm_blocked_write` 又没有可恢复的状态；中断任务在“保留的标签页丢了 + 伴随进程重启”之后，“继续原任务”点了没有反应；接管请求没得到确认（真人路径没复核） | `docs/evals/20260922-computer-use-product-path.md:112-128` | 09-22 `8754a57` |
| 2.10 | 页面下载（C3）：CDP 路径被 Chrome 拒绝，改用 `chrome.downloads` 需要申请整个浏览器的下载权限，这个权限决定一直没做。09-25 新增的文件卡片只能下载助手自己生成的文件，替代不了页面上的下载 | `docs/evals/20260923-ci-gate-failures.md` 第 4 节；`docs/evals/20260922-browser-capability-integration-v2.md:65` | 09-23 `f3f93ce` |
| 2.11 | Browser v2 其他缺口：S6 的 Jev 真实对照因为预算没定而 BLOCKED；OOPIF、waitForURL、S3–S7、C6 富文本粘贴都没跑；C01 的“fetch 被拒后改为打开页面”在真实模型上没跑到 | `docs/evals/20260922-browser-capability-integration-v2.md:64-66,135-148`；`docs/evals/20260923-ci-gate-failures.md` 第 2 节 | 09-23 |
| 2.12 | 圈画结果核验：中文目标仍然会升级到主模型复核，多出来的耗时没解决 | `docs/STATUS.md` 表中“圈画结果核验”行 | 09-24 |
| 2.13 | 语音识别和断句：逗号处会被切成两轮（服务端问题，参数实验已经否定改参数的方案），还出现过“不要点它”识别成“要点它”、整段吞字 | `docs/evals/20260925-pure-extension-daily.md` 失败记录 | 09-25 |
| 2.14 | 09-19 产品复核票组 T01–T06：代码在 09-20 修了，但所有勾选框都是空的，外部最终裁决一直没更新；T07/T08 暂停 | `docs/evals/20260919-product-review-repair.md:5-10,77-79` | 09-20 `2365838` |
| 2.15 | Issue #1 稳定化追踪：A1–A6 一条都没勾，从 09-07 起就没动过；之后方向已经变成 ROADMAP 第 7 条（错误分析优先），这个 issue 实际上停滞了 | `gh-axi issue view 1`；`docs/evals/20260907-stability-foundation.md:15-20` | 09-07 `ddc5a8f` |
| 2.16 | 按决定暂停的方向：显式记忆、技能复用、EverOS、多会话协作（保持现状，不删） | `docs/ROADMAP.md` 第 7 条 | 09-25 |
| 2.17 | 发布：从来没通过 `release:verify` 这套门槛，版本号没动过，没有 tag；lint 还有 4335 条旧违规靠 baseline 放行 | 见第一节；本次 `lint-changed --all` | 09-13 |

### 类别 3：看起来已经解决，但文档还写着没解决（过期记录）

每一条都按代码或远端的实际状态核实过；“证据支持”一栏说明证据站在哪一边。

| # | 过期记录 | 实际情况 | 证据支持 | 最后动过 |
|---|---|---|---|---|
| 3.1 | **Issue #2 还开着**（模型能力标签靠名称猜测） | 修复 `6606e81` 已经在 main 里；`models.ts:146` 对任何输入都返回 `{tier:"unknown", tag:null}` | 代码：已修 | issue 09-07；代码 09-07 |
| 3.2 | **Issue #4 还开着**（断线发送会清空正文） | 修复 `72fe08d` 已经在 main 里；`main.ts:3623` 的 `send()` 会返回成功或失败，`sendInput()` 失败时直接 return，保留正文和引用，并提示“这条没有发出去”（`main.ts:4363,4403`） | 代码：已修。C6 真实扩展证据写在 `docs/evals/20260907-send-delivery-reliability.md:69`，C1–C7 的勾选框没有更新 | 09-08 |
| 3.3 | 两份标准文档的勾选框都是空的：B1–B6（`20260907-stability-foundation.md:46-51`）、C1–C7（`20260907-send-delivery-reliability.md:24-30`） | 两个修复都已交付，devlog `20260907-01/02` 有记录 | 代码与 devlog：已交付 | 09-07/08 |
| 3.4 | STATUS“验收遗留”一行写着“**未推送**” | origin/main = HEAD，`db0ad34` 已经在远端 | git：已推送 | 09-25 |
| 3.5 | STATUS“文档管理”一行写着“**远端 CI 未运行**”；`ci-gate-failures.md` 末尾写着“Linux 试跑还没有运行” | Documentation 工作流 2 小时前成功；E2E 在 2 天前先失败一次，随后成功（`35880574337`） | GitHub Actions：已运行 | 09-23/25 |
| 3.6 | STATUS“Browser v2”一行写着“审计/恢复**红灯仍未关闭**”；v2 记录第 4 条同样这么写 | 这两条（task-goals、task-recovery-matrix）在 09-25 已经处理（`4dfecc1`、`c3b79be`），本次单元测试 0 条失败。Browser v2 的其他缺口仍然存在，见 2.10、2.11 | 代码与测试：红灯已关 | 09-23 |
| 3.7 | `docs/work/20260924-core-into-extension.md` 上 4c、4e、4f、4g 都没勾，还写着“未提交、未加载日常” | `host.ts` 已经删掉（`extension/src/inproc/` 下没有这个文件），`entry-contract.test.ts` 里已经没有 `it.fails`，`check:architecture` 本次通过，相关提交全部在 main 里。4g 只在协议层通过，这一点属实 | 代码：4c/4e/4f 已完成；4g 部分完成 | 09-24 `dfa5202` |
| 3.8 | STATUS 第 1 行把“复制任务 155 秒”列为缺口 | 同一页 ④ 那一行写着：只装扩展、用阶跃时，中位耗时是 26 秒。155 秒是日常 Chrome 加智谱、修复前的数据，日常环境还没重新测 | 两份记录都不假，但第 1 行已经过时，应该改成“日常环境待复测” | 09-25 |
| 3.9 | NOTES 表中“最近会话如何续接”仍然指向 09-23 的交接文档，里面写着“实验分支已跑通” | 实验分支已经并进 main（`de381b0`），09-24、09-25 的方向决定取代了那份交接 | git：已过时 | 09-24 `2793c42` |
| 3.10 | STATUS“真实路径基础设施”一行写着“全量单元测试 3059 项全部通过” | 3059 项确实都通过了，但命令的退出码是 1（见 2.1）。“测试全绿”这个说法不成立 | 我今天的运行结果：部分不实 | 09-25 |
| 3.11 | STATUS“仍需解决”里还写着“技能复用……不能自动关闭” | ROADMAP 第 7 条已经把技能复用暂停了。两份文档的口径不一致，不算真正的矛盾 | 文档之间不一致 | 09-25 |

另外有一处流程上的小问题：`380c692` 修改了模型能看到的提示词（验证动作时先用这个动作自己的回执），提交类型写的是 `refactor`，却没有同步文档，也没有验收记录，和 AGENTS.md“改功能必须同步文档”的要求不符。

## 四、建议（不是路线图）

- **可以直接交付的两处小修复**（修法明确，都是可逆的工程卫生问题）：
  1. 在 `extension/test/delivery-receipt.test.ts` 的 chrome 替身里补上 `storage.onChanged`，让 `npm test` 的退出码回到 0（验收：`npx vitest run extension/test/delivery-receipt.test.ts` 没有 Errors、退出码 0）；
  2. 把 `@rolldown/binding-darwin-arm64` 补进 lock 文件（验收：删掉 `node_modules`，重新 `npm ci`，然后 `npm run test:unit` 能启动）。
  如果要把本任务升级为交付任务，这两项最适合先做。
- **文档对账**：按类别 3 更新 STATUS 的 3.4、3.5、3.6、3.8、3.10，④ 工作流页，以及 NOTES 的续接指针，再关闭 issue #2、#4。关闭 issue 属于对外写操作，需要船长点头。
- 错误分析（2.5）和“把 09-25 的修复加载进日常 Chrome”（1.1）是很多“待人判断”项的共同前提：人手感、观感、日常复测都要等这两件事做完。

## 五、只有船长能回答的问题

1. **Issue #2、#4**：代码已经修好，要不要关？关的时候需不需要先补一次日常 Chrome 复验？
2. **Issue #1（稳定化追踪）**：它已经被 09-25 的“错误分析优先”取代了，要关掉、改写，还是继续留着当总账？09-19 的 T01–T06 票组要不要正式验收收口？
3. **日常 Chrome 什么时候加载 09-25 的构建？** 本机宿主清单目前处于停用状态（`...disabled-purext-20260925`）。这一步决定日常复测、真人试用、手感判断等一批“待人”项什么时候能开始。
4. **页面下载**：要不要申请 `chrome.downloads` 权限（覆盖整个浏览器的下载权限）来补上 C3？还是继续只用“助手生成文件”的方式？
5. **Jev 真实对照（S6）的预算**：给不给预算？是 0 还是某个上限？
6. **什么时候定版本号**：出第一个 tag 或 release 需要满足什么条件？命名要不要统一成 By Your Side（包名、宿主名、配置目录现在都叫 sideagent）？
