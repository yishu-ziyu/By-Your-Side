# 让纠正进入下一次网页任务

2026-09-08

## 1. 第一轮只做一条完整路径

用户希望经历、偏好和经验连起来。我们先让一次明确纠正进入下一次任务。

默认导出只得到当前页20条。用户要求全部200条。后台把纠正和网页结果整理成待验证做法。新会话在改版页面上导出347条，并核对数量。

## 2. 留下事实，再整理做法

任务记录独立保存。结束不等于成功，模型自称完成也不能替代网页结果。

后台整理使用现有模型，没有浏览器工具。每条做法必须引用实际的用户纠正和网页观察。用户可以在已有记忆抽屉里修改或忘记。

第一轮先从明确纠正开始。成功轨迹自动提炼、技能升级和EverOS接入留在后续。

## 3. 实测改变了实现

模型会合并原文片段。引用校验拒绝后，改为短连续引用。

后台模型等待曾排在任务落盘前面。两条队列分开后，新任务的事实可以继续保存。

用户切换活动页、A占用页面和截图等待，都暴露了验收边界。测试使用明确目标网址和独立正式面板，产品也按明确网址选择经验。

## 4. 当前结果

601项测试、类型检查和构建通过。真实模型主路径7项通过，下载CSV另核实347条。原会话与接管回归17项通过。

这说明记录、整理和再次使用已接通，还没有证明总体错误率下降。下一步由用户判断来源与待验证提示是否清楚，再决定是否扩展成功经验和EverOS。
