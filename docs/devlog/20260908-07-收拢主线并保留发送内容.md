# 收拢主线并保留发送内容

## 1. 用户决定

保留现有功能和研究文档，排除三个本地工具目录。最终只保留 main 和 feat/session-management，两条分支同步。

## 2. 合并时的适配

旧断线发送修复早于多会话和图片附件。合并时保留新会话逻辑、附件和历史计时，失败回执携带原会话身份。

用户切换会话后，另一条同号消息不会被误标未送达。返回原会话时可以看到失败状态并重试。重试不会清空正在输入的新内容。

手绘标注改为读取当前会话的模式，避免被默认会话覆盖。

## 3. 验证

相关 30 项测试通过。真实浏览器两种标注模式、保留输入、失败回执和重试等 8 项检查通过。未跑全量测试。

旧分支通过真实合并保留提交历史，待推送相同版本后删除分支名。本地工具目录只忽略，不删除。
