# 麦克风授权需要独立页面

## 1. 真实路径暴露缺口

用户点击语音后看到了权限错误。Chrome 的权限状态仍是等待询问，不是拒绝。侧栏没有把首次授权弹窗展示出来。

## 2. 补上恢复入口

权限错误时提供“开启麦克风”。独立页面让用户完成浏览器授权，拿到麦克风后立即释放。用户回到侧栏再开始语音。

## 3. 当前结果

8 个相关测试、类型检查和构建通过，扩展已重载。最后的实际授权和语音听说仍待用户操作验证。
