# 把经验沉淀接到当前任务收尾

## 1. 前轮入口选错

前轮修好了旧 Kimi 复盘接线。用户指出，当前开发发生在 Codex，这并没有解决他的目的。

问题在实施前就已经发生：验收围绕旧实现展开，没有先确认用户要在哪个入口得到结果。

## 2. 先确认，再改范围内的文件

用户确认了目的，又批准了具体范围：当前主代理在任务验证后整理经验，复用仓库经验库，值得复用的建议交给用户审核。

这次把上述步骤写入 AGENTS.md，入口说明改为宿主无关的项目经验库。历史目录名保留，当前 Codex 收尾直接维护它。

## 3. 用本次教训验证流程

当前主代理记录了“旧入口验收通过不等于当前入口可用”的经验，并提出在验收模板增加使用入口字段的建议。已获授权的目的对齐要求直接写进规范；额外模板变更保持待审。

这轮新增 1 条经验、1 条待审提案，安装 skill 0 个，产品代码改动 0 处。只验证文档事实、引用及差异，不重跑产品测试。证据见 [本轮验收](../evals/20260909-codex-experience-closeout.md)，下一步由用户判断模板字段是否值得加入。
