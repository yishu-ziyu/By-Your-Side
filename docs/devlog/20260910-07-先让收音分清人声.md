# 先让收音分清人声

## 1. 只改第一处

用户反馈没有说“嗯”，侧栏却反复记录。日志显示新的短录音被识别成一个字。旧检测把持续80毫秒的音量当作开口，非人声噪声也能触发。

用户选择先修人声检测，试用后再决定是否需要其他处理。

## 2. 在本地判断人声

增加随扩展打包的检测模型。它只判断声音，不改转写内容，也不屏蔽“嗯”。保留开口前声音，轻声仍能进入原识别流程。

## 3. 用能看到的结果交付

正式扩展页面检查9类样本。中文、低音量短句和三个短词各触发一次，静音与三类噪声未触发。正式收音链路也以合成输入验证到一次提交。页面定时器喂样本受后台限速影响，检查器改用逐帧回执；未改样本或断言。

1050项测试通过。下一步交给用户日常试用。若误触改善且不漏听，本轮到此为止，不自动扩展第二、第三处。

[验收与未验证部分](../evals/20260910-voice-speech-detection.md)。

## 4. 真人验收未通过

15:51用户报告识别变差并要求停止继续修改、commit/push交给GPT 6 Pro。当前样本只证明触发和提交，没有验证真人转写准确率。这是本次验证边界的缺口。保存失败截图与诊断日志，不能将本次结果写成体验改善。
