# 任务: 清理仓库中已确认的重复规则与退役实现，保持现有产品行为

## 语音计划去重完成标准（第一步）

- [x] 1. 两个语音计划入口共用请求组装、预算、重试与校验流程；不新增通用策略框架、不只搬文件。— 谁检查: 主代理阅读实际 diff，独立 DeepSeek 复核
- [x] 2. 两入口的提示词、输入上下文、模型参数、失败重试与错误结果保持原行为；保留诊断前缀、requestId、尝试数和耗时语义。— 谁检查: 生产入口定点测试、改前/改后对照
- [x] 3. 计划取消、超时、无效候选与供应商失败不增加调用；闲聊直答仍单次请求，无计划回退；受影响的旧会话行为通过回归。— 谁检查: 定点测试、普通单测
- [x] 4. 类型和生产依赖边界检查通过，改动与未验证范围如实记录。— 谁检查: npm run typecheck、npm run check:architecture、主代理

## 第一步边界与不做

- 保持现有产品行为与两个公开入口，保留旧入口调用方；不改提示词、意图白名单、授权、页面归属、TTS、任务派发。
- 不修改供应商依赖与配置，不安装扫描工具，不重构 UI，不重载日常扩展或发布。
- 这是规则去重，不是语音延迟优化。真实模型波动与真人麦克风体验不由本次离线证据证明。

## 改前观察

- 起点: main / b83c9bc，工作树干净。
- 本地一次性文本扫描: agent/src、extension/src、shared 下 163 个生产 .ts 文件，排除 vendor 和 .d.ts，共 33,934 物理行；这是定位线索，不是 sloppiness 分数。
- 扫描连续 8 个去空白/整行注释后的非空行，人工核对发现 voice-model.ts 两套计划调用循环重复。文本扫描不能等同 AST 重复率或论文 Erosion。
- 大文件入口及其职责边界已有 docs/architecture.md 描述，不能凭文件长度继续机械拆分。
- 基线: npm run check:architecture 通过（167 个生产文件，包含其自身扫描支持的其他扩展名）。
- 基线: npx vitest run agent/test/voice-model.test.ts agent/test/voice-classifier-deadline.test.ts agent/test/voice-single-proposal.test.ts，3 文件 / 47 项通过。

## 第一步结果

- 生产规则从两套循环合为 `runVoicePlan` 一套。没有新文件/依赖/策略回调；两个公开入口保留自身返回值和诊断口径，闲聊直答、编辑判定及只读观察未改。
- 生产文件 347 → 310 物理行（+87/-124，净减37）；新增契约测试 272 行 / 25 项。测试增长用于锁住两个已有入口的外部契约，不计为生产复杂度改善，也不以总净行数作为成败。
- 未来若调整重试提示，只需修改 voice-model.ts 内的 VOICE_INTENT_RETRY_HINT；请求预算、payload 与校验也只有一个落点。VOICE_PLAN_PROMPT 原本就是 VOICE_INTENT_PROMPT 的别名，本次生产统一引用后者。
- 执行者先在旧生产实现上通过新增25项契约断言，再重构；类型标注随后修正。重构后定点4文件72项、语音25文件291项、类型与边界通过。
- 主代理从 `git show b83c9bc:agent/src/voice-model.ts` 与当前源码各自 bundle，以相同假模型应答运行两个生产入口：成功、请求异常重试/耗尽、供应商 error/aborted 重试/耗尽、候选拒绝重试/耗尽 × 上下文提供/省略，共32组，请求参数、诊断及结果逐项一致。固定 Date.now 消除测试时间差；此差分不证明真实供应商或计时器表现，超时由定点测试另验。原始检查脚本/结果位于本机临时目录 `/tmp/bys-slop-20260914/`，长期回归使用仓库中的契约测试。
- 主代理普通回归: `npm run test:unit`，188文件 / 1,732项通过。
- 主代理发现新增跨入口字节比较包含实时 timestamp；用每次调用递增1ms的时钟确定复现1项失败。语义相同仍可能因跨毫秒而失败，需固定该用例时钟，不删除请求比较或修改产品。
- 独立 DeepSeek 复核未发现生产新缺陷；指出上述时间戳竞争。其只读环境运行 Vitest 时被临时目录 EPERM 阻断，记为未运行，不算测试通过。主代理与执行者已有运行证据覆盖超时和取消。
- 一轮定点修正：仅在跨入口比较用例内固定 Date.now，保留完整请求与次数断言，沿用 afterEach 恢复；修正一处测试注释来源。修正后25项契约测试通过；同一递增时钟反例由1项失败变为1项通过；两端类型检查通过。生产代码未再变化，因此复用此前1,732项普通回归与32组差分结果。
- 主代理验收：实际diff已检查，重复流程确实只有一份；未采用外部报告中错误的提示词文件/常量引用，以实际源码为准。审查、执行、复核与修正的收据均核实为 opencode_go / deepseek-flash，完成报告不代替验收。
- 未跑：规模测试（未改存储/数据规模路径）、扩展构建（未改扩展或共享源码）、真实模型/麦克风与浏览器验收。本次仅验语音计划代码契约，不据此更新产品语音延迟/声学结论。未提交、推送或重载扩展。
- 已检查 git diff --check 和本轮文档引用。代码、测试注释及本验收已能解释本次教训，不另建经验文档。

## 第一步候选判断（续作见下）

- `extension/src/background/state.ts` 的共享页不安全工具清单与 `shared/control.ts` 的 WRITE_TOOLS 当前相近。审查建议派生集合，主代理未采用：控制闸门与共享页直写许可是不同规则，未来新增工具未必适合自动关联；静态相似不证明当前有授权漏洞。若新增工具，应分别验证两种约束。
- `agent/src/consent-ticket.ts` 与 `task-dispatcher.ts` 的 canonical 参数序列化是确定的重复候选；后续整理需固定授权参数hash与落盘请求指纹，验证旧收据重载后不重复执行。本轮保留以限定改动为语音计划规则。
- `extension/src/background/exec/input.ts` 有六处相同的 debugger 回退判定；可考虑同模块谓词，但每处错误包装及已执行/未执行语义不同，不能把整个 catch 机械合并。本轮不改变浏览器执行路径。
- 未按文件行数拆入口，也未删除仅凭搜索无引用的保留项；未建立论文指标分数或硬阈值。


## 2026-09-14 续作：修正任务范围

用户指出“处理仓库”被主代理自行缩为一处整理。原目标不变；小步只作为内部执行方式，不再作为停止条件。本次会完成所有已确认、能保持行为的候选，并给其余候选明确的保留依据。

### 完成标准

- [x] 5. 授权与请求去重共用同一序列化规则，已有授权hash和磁盘收据fingerprint兼容，重载/并发不重复执行，变参仍拒绝。— 谁检查: 改前固定样例、定点行为测试、独立复核、主代理
- [x] 6. 浏览器六处不可用判定共用一处私有谓词，原错误分类与包装保持，点击不可重复；真实隔离浏览器填写/点击/取消/切页路径通过。— 谁检查: 定点测试、headless验收、主代理
- [x] 7. 语音去标点规则复用现有实现，保留确认函数的空值兜底；当前配色保持，删除仅被过时测试引用的退役配色算法。— 谁检查: 生产引用核对、改前颜色固定样例、语音与配色测试、独立复核
- [x] 8. 所有候选有明确处理结论；整体普通回归、涉及存储的规模测试、类型、依赖边界与扩展构建通过。— 谁检查: 对应命令、主代理最终diff审查

### 续作边界

- 沿用未提交的语音去重成果，保留并发修改；不改模型/授权策略/页面控制权/配色或产品交互，不提交推送、不重载日常扩展。
- 新增授权：无。上述清理属于用户原始仓库整理要求；真实验证使用现有隔离headless宿主、本地表单，不操作用户日常浏览器或真人麦克风。
- 不合并语义不同的工具清单，不删除既有明确保留的工具图标设计待定项；这两项不是默认留给用户的一轮新任务。

### 追加观察

- 配色候选经主代理核对需要修正：页面光标与侧栏均已调用 palette.ts 的名册配色，cursor-visual.ts 的顺序配色函数只被 overlay.test.ts 引用；palette.ts 的五色数组也仅有测试引用。这里可以删除退役实现，而无需选择新的配色方向。
- 当前实际配色基线：main/空ID=#2f6fed，wiki=#000000，worker-1=#ff6a00，worker-2=#000000，worker-3=#ff3347，worker-4=#8656f6，worker-5=#9a6737。新增检查针对这个现行规则，原测试中的“五色轮换”是未调用旧函数的测试，不能冒充产品行为要求。
- normalizeSpeech 已是 voice-receipt.ts 的公开纯函数，conversation-manager.ts 已导入该模块；voice-confirm.ts 的同样替换需保留 text ?? "" 的旧容错，不改变确认/拒绝正则。

### 续作结果

- 请求识别：抽出一个17行的 Agent 内 canonical-value.ts，两个实际调用方共用；不合并各自的授权/请求业务。定点13项及授权直接消费者17项通过。旧实现采集的5个授权hash/请求fingerprint字面量经独立复核用旧函数体重新计算，完全一致；嵌套键重排不重执行、数组换序及变参冲突被拒，旧 finished/pending 磁盘收据都不重执行。
- 浏览器错误判定：六处同一正则改用私有 isDebuggerUnavailable，catch、错误文字和执行事实包装保持。新增3项从生产click出发的AX ref检查；5文件58项通过。执行者短暂将谓词改为恒真时，失效/遮挡两个反例失败，还原后通过；主代理确认最终源码恢复为原正则。
- 语音文本比较：confirmation与conversation manager复用现有normalizeSpeech，保留确认函数的null/undefined兜底；没有新建文本工具模块或修改确认规则。相关原行为回归通过。
- 退役配色：删除cursor-visual中仅测试调用的顺序配色函数及两份没有生产调用的五色数组。当前名册配色、尺寸、描边、样式均未变。原两项退役规则测试换成一项捕获现行生产配色的固定样例，保留其他有效断言；主代理5文件基线73项、清理后72项通过。独立复核按现有名册映射另算8个颜色全部一致。
- 整体 `npm run check` 退出码0：168生产文件依赖边界、两端类型、188文件/1,739项普通回归、2项规模测试、扩展构建全部通过。未降低规模门槛或删除有效失败。
- 真实隔离浏览器：`npx tsx scripts/acceptance/stagehand-control.mts --headless` 退出码0，完整浏览器链路[结果](../../out/acceptance/stagehand-control-2026-09-14T08-59-50-533Z/result.json) passed=true。填写姓名/邮箱后预览只点击一次，trial未点击，多匹配未写入，全选正确；取消后无新增调用/写入，切页后仍只改原表单、另一页为空，过期任务不写入。主代理查看[改后页面](../../out/acceptance/stagehand-control-2026-09-14T08-59-50-533Z/02-corrected-a.png)并核对状态，姓名李明、邮箱保留、未提交。该证据使用生产构建、现有Stagehand兼容层及本地表单，排除模型/麦克风；debugger故障注入仍由隔离夹具证明，不能冒充真实故障实测。
- 独立DeepSeek续作复核：四个改动范围无生产新缺陷或需修正项；确认旧指纹及现行配色基线有效。复核者未重跑测试，运行结果以上述主代理/执行者证据为准。两执行者和独立复核的实际路由均核实为opencode_go/deepseek-flash。
- 同口径生产TS盘点：163→164文件，33,934→33,891物理行。新增模块用于两个现有调用方；行数只作改动说明，不作为质量分数或完成门槛。
- 本次已确认、可保持行为的候选均已处理。没有全仓重排/拆大文件，没有新增依赖或扫描框架；改动留在本地，未提交、推送或重载日常扩展。真人语音、模型波动与既有发布待验项保持原状态。
- 主代理复查diff、引用及候选处置；本次原因已由测试与验收说明，不另增经验模板。按用户纠正更新了全局汇报规则：技术明细留文档，内部步骤不代替任务完成。

### 保留项及依据

- WRITE_TOOLS与SHARED_UNSAFE_TOOLS：控制闸门与共享页直写许可职责不同，内容相近不足以自动合并；没有证据表明现实现有漏洞。本次保持两种语义与现有测试。
- TOOL_ICONS：既有[硬化验收](20260911-repo-hardening.md)明确列为开放设计决策的保留项，当前未产生用户可见变化，不在代码清理中替用户选择工具行图标方案。
- READ_POST_CAPABILITIES：空的政策说明锚点保持；它不参与运行判断，POST是否需确认仍由classifyToolEffect实际分支决定，不能把该常量当作额外安全检查。
- 其余大入口沿现有[架构职责](../architecture.md)保留。本次没有凭文件长就制造转发层，也不将所有表面相似的代码认作同一规则。
