# 任务：按云端评审修复语音正确性，再完成真实验证

2026-09-09，用户明确同意：空识别后自动接回非操作回答；当前页面先看可见区域，必要时再扩大；其余可执行的工程修复、判断和测试一并完成。实现由当前主代理独立推进。

## 完成标准（本轮实现前冻结）
- [ ] R1：停声音立即发生，候选输入不抹除上一有效问题；空轮后闲聊可恢复，观察重新取得有效证据；未接受写指令不能自动复活，已接受动作不能重做。异步诊断记录原轮次及取消原因。— 组合竞态测试、生产采音路径及人验
- [ ] R2：恢复后缺ASR、缺配置确认、缺回答均有明确截止时间；重连不得无限延长原等待；播放中断保留结果，接受或unknown只能查询原请求。模拟close/error/旧包/新输入排列，检查零重复执行。— 虚拟时钟与真实断线
- [ ] R3：以独立、受限观察授权读取固定当前页的可见区域；不依赖任务runId，不认领、激活或改变控制权，不污染任务定位引用；文字/图片返回实际tab与文档身份和采集时间。同URL重载与来源变化拒绝混合证据。— 真实扩展双页、暂停/终止/导航测试
- [ ] R4：模型候选覆盖完整分句并明确动作、参数、条件、背景/引用/否定归属；显式目标不能静默丢失；合法形状的错误候选应被拦下。每步目标、有效内容和执行资格进入oracle；保留原48句全部匹配要求。— 构造反例及真实模型3轮
- [ ] R5：计划有稳定身份、逐步回执及未执行步骤；按任务和控制版本检查，新的人工接管使旧计划后续恢复失效；计划重放不重新分类/执行；另开确认绑定稳定proposal，避免重复空会话。— 并发、磁盘/来源侧重载和Chrome
- [ ] 生产音频验收经过VoiceClient、AudioWorklet、检测器、播放器；暂停验证含主动写入与旧代次探针；来源侧回执经过协议→UI→持久化→重载；所有测试记录源码/包/模型/数据集指纹与清理结果。— 自动脚本
- [ ] typecheck、test、build、diff通过；原冻结A1–A10三次连续真实路径、真人三轮控制闭环及一次终止不降低。— 机器及人
- [ ] 时延仍从“说完”算到有效反馈，20轮P95≤4秒；20次检测到开口到实际停声P95≤200ms，另外记录真人开口到检测。只读/处理中不能冒充已接收。— 时序与人验

## 既定产品行为
- 自动恢复仅限非操作回答；未接受的网页写指令需要重新确认。
- 默认当前可见区域。不能把旧选区页或任务工作页当作当前页；扩大读取须有用户明确要求或后续明确范围。
- 保持当前音色和固定回执语义。缓存或独立TTS是后续性能方案，必须共享停播机制，不伪造供应商item身份。
- 主动消息不抢断当前回答，空闲时再播；先修正确性再优化延迟。

## 边界
- 不为让测试变绿放宽旧标准；不把合成PCM旁路当作真人采音通过。
- 云端意见是待验证输入。先复现代码缺陷，不把未录音的噪声假说写成根因。
- 浏览器仍在用户控制下；本地代码/测试先做。真实重载与交互测试前核对其当前使用状态，保留任务与页面，不借验收清理用户数据。
- 本轮默认先实现与验证，不以原开发快照推送当作当前全部改动的再次推送授权。

## 2026-09-09 用户调整：先进入本人试用
用户明确提出按必要性取舍：不必要的连续回归和20轮时延统计改由本人试用发现问题。本次据此调整进入个人试用的门槛，不改写上方原始标准及既有结果。
- 不再以三轮连续真实路径通过、20轮P95统计阻挡个人试用；这两项留作后续稳定性和性能量化，当前不宣称达标。
- 暂停/终止后零写入、旧动作拒绝、已接受操作不重复、目标归属等正确性仍须机器检查；发现未修复的此类缺陷不得以主观试用覆盖。
- 快慢、插话、连续交谈和真实任务体验由用户判定。用户试用未发现问题，可记为个人试用验收通过，不等同于原统计门槛通过或所有场景可靠。
- 最新控制路径 /tmp/ego-voice-control-1788930084105 的17项通过；最新typecheck、737项测试、build通过。此前供应商529失败记录保留。
- 停止循环调度，不再启动余下重复；让当前已启动子测试完成清理。未实施订阅池分配或自动切换模型，用户提出的候选池方向另行落实具体范围。
