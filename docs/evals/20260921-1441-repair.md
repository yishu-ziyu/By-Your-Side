# 任务：修复 9/21 14:41～14:49 试用暴露的五项实现缺陷

来源：[日志回溯](20260921-1441-log-review.md)。本记录是本轮修复的验收契约；各工作包的实测证据放在 [20260921-1441-repair/](20260921-1441-repair/) 下，由主代理汇总结论。

用户可见的目标：同类圈词任务不再因内部保存要求卡住；圈注结果能被核验器看到；部分交付正文不再夹带未核验的完成宣称；语音工具结果回传不再与服务端自动回复冲突；同一标签页并发读取不再自己触发“调试器占用”。

## 完成标准

- [ ] 1. 已固定的目标方案可以在保留全部用户要求编号的前提下，移除或替换没有 field 依赖且仍 pending 的内部 material 目标；任何 condition/field/answer 目标与已 satisfied 目标不能被改动或删除；修订经过同一 plan 阶段 Jev 审核并记录 reason — 谁检查: vitest `agent/test/task-goals.test.ts`、`agent/test/task-goal-tool.test.ts`
- [ ] 2. 原文材料超限时报错给出实际长度、上限和可行做法；空材料与超限是两条不同错误；上限仍为 8000 且 restore 校验一致 — 谁检查: vitest `agent/test/task-evidence*.test.ts`
- [ ] 3. condition 目标核验可让宿主自己按执行器给出的选择器读回全部命中元素（文本、可见性、位置、计算样式、数量，有界），并把这些证据与页面文本一起交给 Jev；模型不能替代提供证据 — 谁检查: vitest `agent/test/task-goal-tool.test.ts`、`extension/test/read-elements.test.ts`；真实页面圈注核验由人在加载后复验
- [ ] 4. `outcome=partial` 的 finding 在发送前经语义检查：正文不得把仍未核验的目标说成已完成；检查失败时拒绝并说明哪些目标未核验；如实报告“已执行但未确认”不被拒绝 — 谁检查: vitest `agent/test/user-delivery-runtime.test.ts` 及新增回归
- [ ] 5. 服务端 VAD 自动回复待启动期间，客户端不发送 `response.create`；工具结果仍恰好回传一次；自动回复未到达时有超时兜底不饿死；迟到保留的用户转写会转发给界面 — 谁检查: vitest `agent/test/realtime-voice-*.test.ts` 及新增回归
- [ ] 6. 同一标签页并发 `ensureAttached` 只发起一次 `chrome.debugger.attach`，其余调用复用同一 Promise；attach 失败时不留下假的已连接状态 — 谁检查: vitest `extension/test/debugger-attach.test.ts`
- [ ] 7. `npm run typecheck`、`npm run check:architecture`、受影响测试文件全部通过；未运行项明确列出 — 谁检查: checker 子代理 + 主代理
- [ ] 8. 用同一次 trace 的离线回放（[replay.mts](20260921-1441-log-review/replay.mts)）反查：原本断言“缺陷仍在”的检查应当失败 — 谁检查: 主代理

## 边界与不做

- 不放宽任何 Jev 门槛，不删除有效失败，不把材料上限从 8000 改大；单份超过上限的整篇原文本轮仍不可作为一份材料保存，记为已知限制。
- 不修改历史证据脚本 `20260921-1441-log-review/replay.mts` 与其输出。
- 不运行 `npm run build`、`install:host`、`reload:ext`、`accept:*`、`eval:*`；不重启正在运行的 native host。是否加载到日常由用户决定。
- 不提交、不 stash、不 checkout；工作树现有未提交改动全部保留。
- “词频最高”口径（是否排除虚词）是模型执行时的解释变化，不在本轮代码修复范围。
- 语音事故的唯一根因、当时听感与圈注视觉质量仍需真人复验，本轮只消除已确认的时序竞争路径。
