# 任务：保存 Realtime 直连浏览器工具之前的源码基线

用户要求先提交整个仓库当前版本，再接入现有浏览器工具供真人试用；满意后再次提交，不满意只撤回本次接入。

## 完成标准

- [x] 保存当前所有已跟踪改动以及未跟踪的项目源码、测试、文档和工具配置 — 检查者：主代理，Git 暂存清单。
- [x] 不包含依赖、构建产物、凭据及误生成的 `~/.cmuxterm/` 本机锁和会话账本 — 检查者：主代理，文件清单与凭据模式检查（命中均为 fast-task 文件名误报）。
- [ ] 接入后真实语音试用通过 — 检查者：用户；不属于基线提交的通过声明。

## 基线含义

提交前 HEAD 为 fcc59c2；包含前序任务的全部未提交产品工作。此提交只是恢复点，不声称现有缺陷已解决，不运行全量评测。基线 commit ID 在后续接入验收中记录。

回退时只撤回基线之后本次接入的改动；保留任何后续无关改动，并重新构建、加载恢复后的版本。Git 回退不撤销网页上已经发生的动作。
