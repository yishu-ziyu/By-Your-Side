# 任务：语音保持原说话人，操作请求不再长篇铺垫、迟迟不执行

## 真实反馈与已有证据

用户明确反馈「忽男忽女」，不是已确认的播放断续。原音色 `voice-tone-T3kZb9MwL2` 在当前凭据下可查询到；日常服务回显也确为该 ID。接口接受、返回音频并不能证明听感稳定；此前以此宣称音色恢复的验收不足。尚无证据证明两条回复路径使用了不同音色，不擅自换音色或更换模型。

用户要求切换标签页，21:44—21:46 的真实日志却记录两次 `read_page`，没有任务派发。读取耗时 14/5 毫秒，但首次工具结果 21:44:29.574 就绪，21:44:38.647 才发送，第二次等待约 9.4 秒。代码 `maybeFlush` 明确等待实际播放结束才能发送工具结果，造成额外串行等待。之后又发生长回复。证据：`out/acceptance/20260920-general-browser-trial/usage-214248/logs/`。

## 完成标准

- [ ] 工具结果不再被前导语播放阻塞；仍等服务端生成结束再请求后续回复，不虚报已播放或任务完成 — 谁检查：定点单测、真实 API 事件时间。
- [ ] 普通操作请求先实际派发，不以读当前页替代切换/导航等操作；默认只简短报结果或问一个必要问题 — 谁检查：真实语音回归，检查实际工具调用、回复及独立浏览器状态。
- [ ] 多轮普通回复与工具续答保持原说话人，不忽男忽女 — 谁检查：用户听感；API 音色回显只作辅助，不代替试听。

## 边界与不做

不改用户网页作为故障复现，不开启用户麦克风录音，不擅自重克隆、付费购买音色或切换到 2.5。保留原始失败日志。本轮真人任务已经曝光，只作回归案例。
