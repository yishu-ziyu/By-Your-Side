# 任务：精简输入区与回执，并让新会话选择携带原请求

- [x] 普通回执按任务/请求收纳，异常和待决策项可见；同请求更新不重复。
- [x] 新会话操作保留原文、页面引用及附件；双击不重复创建，原任务不变。
- [x] 模型轻量显示，诊断按需打开，输入与草稿行为保持。
- [x] 定点契约、浏览器交互和构建检查；记录实际加载范围。

历史回执没有原请求上下文时不伪造恢复按钮。已有记忆/语音/并发工作保留。

## 验证结果

- [生产侧栏受控交互](20260916-composer-cleanup/result.json)：普通回执按run/request收纳、同请求替换、queued转accepted归档、失败回到可见区域、诊断开关、模型轻量显示、双击只创建一次、跨会话发送完整原文/选区/附件通过。
- 任务分发、会话管理、回执文案与语音诊断定点测试通过；新增恢复上下文回放与busy请求跨会话执行检查，原任务未被中止。协议与解析50项通过，类型、构建、架构边界及本轮差异检查通过。
- 协议测试的首个正例缺PageContext必填title，被正确拒绝；补齐fixture后通过，未放宽校验。
- [截图](20260916-composer-cleanup/08-composer-cleanup.png)使用真实生产bundle、模拟后台消息；未冒充真实模型任务。旧harness在history重置后遗漏模型快照，补发hello后验证轻量模型入口可见。
- 日常扩展侧栏当时已关闭；[重载前状态](20260916-composer-cleanup/preload.json)确认198份会话控制快照均idle，随后重载成功。native host随扩展连接生命周期重启加载后端改动。未提交或商店发布。

## 行为边界与维护

只有后端明确标记可另开会话的被拒start回执才附带原请求。其他拒绝/未知结果不猜测恢复。保留附件与页面引用；新建成功后按创建请求ID发送到对应会话，双击复用同一promise，15秒无创建回执不自动发送。仅转交匹配的语音提案，原任务授权保持。历史回执没有完整输入时保留原始记录，不生成不完整快捷操作。

界面新增独立receipt-view模块；普通记录只合并呈现、不删除请求。更多菜单沿用原生popover，诊断原控件按需显示。未新增运行依赖。全局已有并行工作未覆盖。
