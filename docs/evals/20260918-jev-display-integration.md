# Jev 显示操作小范围接入

## 固定完成标准（用户已同意）

- [x] 已有译文的宋体、双语、仅译文走可关闭的Jev候选路径，原字体仍走原流程。
- [x] Jev不确定或超过1秒回原流程；取消、接管、页面更换不执行旧命令。
- [x] 真实侧栏入口产生正确页面、任务记录和正式完成回执；否定、混合、局部范围不误执行。
- [x] 完成10次配对操作，零错误操作，成功直达的页面变更中位耗时降低至少30%，回退能完成。
- [x] 达标后启用，不追加分类题库；未达标只修复具体失败。

## 实现范围

生产入口为原会话的新任务准备阶段：保留manager的持久化和runId；使用当前SDK已注册工具执行器，工具回执写入原任务账本，读回后调用原send_user_message交付。Jev只决定display参数，执行保留原工具闸门。

snapshot增加只读译文状态及实际DOM一致性检查；display携带观察到的translation document token，刷新或更换文档不能复用。API请求固定jev-1.13.0、单次1秒、无重试；不确定回原主模型。只用本地伴随进程的TYPESAFE_API_KEY或~/.sideagent/typesafe.env，不送进扩展。

启停：~/.sideagent/config.json 的displayFastPath=true/false；下次请求读取，无需改变模型设置。验收用SIDEAGENT_DISPLAY_FASTPATH覆盖开关，仅限测试进程。

## 证据

待真实侧栏配对验收。新增取消/同URL换文档/无译文/不确定/正常回执5条定点通过，旧snapshot身份2条通过。首次metadata读取对旧测试替身的异常隔离已修正；测试beforeEach返回mock函数被框架当清理钩子的错误已修正，不算产品问题。


## 真实侧栏验收结果

[10次配对结果](../../out/acceptance/jev-display-pilot-1789705411374/results.json)：20次操作均得到要求的页面结果和正式finding，9/10开启操作由Jev直达、主模型turn为0；1次不确定正常回原模型完成。按相同指令对应的成功直达配对计算，页面变更中位耗时2604ms→771ms，降低70.4%，达到约定30%门槛。原始事件及页面/侧栏截图同目录保留。

首次接线smoke中的一次Jev未命中属于正常回退；smoke未满10对而标passed=false，不冒充配对验收通过。随后完整配对是独立一轮，不挑选最佳单次拼结果。

## 范围与故障路径

[首次边界记录](../../out/acceptance/jev-display-safety-1789705605884/results.json)证实：人工注入Jev1秒超时后原模型完成；路由期间取消无晚到写入；同URL刷新取消旧命令；明确只问能力不修改页面。但局部标题请求回退后，原模型又尝试整页display并超时，按失败保留。

针对这个具体失败新增run绑定的工具闸门：Jev识别到局部/不确定范围时，本run的page_translation写动作被阻止，普通模型明确报告整页工具限制。不是只靠Jev拒绝直达后就认为安全。[范围复测](../../out/acceptance/jev-display-safety-1789705787383/results.json)通过：局部请求不改正文；混合“改宋体并告诉网页标题”走原模型完成两项要求；最新runId实际落在会话文件。新增单测确认此限制不会污染后续新run。

这些故障路径与10对速度测试分别留证，不称一份报告覆盖所有情况。

## 工程检查

- 类型、190个生产文件模块边界、扩展构建通过。
- 定点87项通过；范围闸门修复后的相关55项通过。
- 全量普通单测1976通过、1失败：既有SYSTEM_PROMPT长15168字符，超过15000门槛。本轮未修改prompt.ts或VOICE_PERSONALITY；保留原有P0提示词改动，不为本次功能删改其他工作。npm run check因此退出1，不宣称整体绿。
- 规模检查单独2/2通过，构建随后完成。
- 实际diff复核：运行时只增加一个可取消的准备状态，复用注册工具与回执/交付路径；工具身份和作用域保持代码控制，不新增另一套网页执行器。

## 已启用（2026-09-18 12:32）

- `~/.sideagent/config.json`的displayFastPath=true，保留原模型及其他字段。
- TypeSafe key只写入本机`~/.sideagent/typesafe.env`，权限0600，未进入Git、扩展或报告。
- 重载前读取日常native会话列表：201个会话、无running任务。
- 日常ChromeMain扩展已重载，loaded background.js的SHA-256与本次构建一致；native连接已恢复，会话数仍201。
- native进程在12:32:33重新启动，使用本项目agent/src/main.ts；无需用户手动操作。
- 关闭方式：将本地config.json的displayFastPath设false，下次请求立即走原流程。无需更换模型或撤销密钥。

已按固定标准结项小范围接入，不再添加离线分类题。首批仅覆盖已有译文的宋体/双语/仅译文；原字体与未命中的请求走原流程。本次未提交或发布远端版本。
