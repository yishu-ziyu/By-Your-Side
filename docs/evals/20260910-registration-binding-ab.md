# 任务：减少任务登记和目标绑定往返，同时保持执行约束

用户2026-09-10明确批准推进此问题，并指定Codex主代理规划、DeepSeek负责实现及A/B测试。此授权覆盖本任务的委派，替代本项目默认不委派约定；不自动修改全局规则。主代理负责冻结条件、评审证据与选型。实现方不能为了已有代码修改判定条件。

## 候选方案

A：当前工作区的真实完整快照，含所有既有未提交修复。不能用HEAD代替。
B：观察可先于任务登记；保留明确意图登记。未执行、无绑定证据、同工具唯一pending候选时，执行层将本次实际目标绑定到该项再作原执行检查；歧义须明确选择/登记，不默认挑第一项。
C：观察可先于任务登记；保留明确意图登记。动作携带原待办编号，在同一次调用内完成未执行待办的目标绑定与执行检查；多项同工具通过编号区分。编号是内部元数据，不向网页执行或正文泄漏。

B/C均不把任意工具返回成功当成用户目标成立，不取消结果未知/确认/接管保护。不是一律允许不登记写入，也不以关键词硬编码站点。允许发现更简单方案后向主代理提出，未经修订不偷换候选。

## 完成标准（实施前冻结）

### 用户修订：单一选型指标（2026-09-10，真实对照进行中）

用户明确要求给出清晰、量化、独立的单一指标，并以指标决定优劣。当前有效选型规则：**六次固定任务全部正确完成的平均耗时（totalMs，用户发出任务到完成，单位秒）**。正确性由独立fixture的真实页面状态/副作用计数核对；任一次真实任务失败，该候选不达标；全部通过者中平均耗时最低者胜出。若没有全通过者，则本批无达标方案，继续依据失败修正。基础设施或fixture自身失败须保留证据并修复重跑，不冒充产品成功或失败。模型轮次、登记往返、token与首动作时间继续保留用于诊断，不参与多维选型权衡。

这是用户对本任务选型规则的明确修订，覆盖下文第7项及旧20%/简化优先条款；其余执行安全、同条件对照、证据、回接验证标准不变。原标准保留以说明修订依据，不按实现结果倒改断言。

- [ ] 1. 观察不需要先登记观察计划；单工具和browser_run子步骤遵守同一约束。观察不修改页面、不认领无授权页面、不改变unknown。— 机器：DeepSeek生产模块及真实Chrome。
- [ ] 2. 绑定只作用于当前run、正确成员/页面、尚未执行的指定意图；未知、已执行、在途项不可重绑。B的多候选拒绝；C的未知/错误编号拒绝。— 机器：DeepSeek反例。
- [ ] 3. 必须在首次实际写入之前完成绑定和必要的持久化/身份记录；中断、晚回执、重复投递不生成第二次副作用。失效ref、换文档、接管、工具禁用仍在原执行层拒绝。— 机器：DeepSeek针对生产链路。
- [ ] 4. 同一目标可在执行前更换方法，原结果身份保留；旧satisfied/unknown不能被新登记洗掉。源码中目标检查不能仅删掉以使测试变绿。— 机器：DeepSeek。
- [ ] 5. 原有正例与仍有效的边界回归通过；此前“所有观察必须预登记”或“唯一未绑定项不能在执行时绑定”的断言，属于本次明确比较的行为变更，可在候选副本中修订，但必须在报告列出原断言、修订理由和替代反例。禁止改错绑、重复、unknown、当前意图等断言。既有R3失败独立保留，不冒称修复。— 机器：DeepSeek；评审：Codex。
- [ ] 6. 使用同一M3/medium、同一fixture起点、同一普通用户任务，不在任务里提示使用候选工具。至少3类场景各A/B/C两次（共18次），顺序交错，数据可复查。记录结果、纯登记/绑定轮次、模型轮次、首动作、总耗时、token和错误；基础设施失败单列，不能删慢样本。— 机器：DeepSeek。
- [ ] 7. 所有新错误目标、额外副作用、unknown重放、错误完成均为淘汰项。通过者先看登记/绑定往返是否实际减少，再看配对耗时和实现复杂度。不用小样本宣称P95；没有可靠收益则保留A并解释，不强行选赢家。— 评审：Codex依据冻结数据。
- [ ] 8. 选中方案回接现有工作区后，DeepSeek执行定点、typecheck/test/build和必要真实路径复验；主代理检查最终diff和证据。未获主代理选型前不改当前产品源码、不重载用户扩展、不提交推送。— 执行：DeepSeek；评审：Codex。

## 场景与指标

1. 暂停真实媒体，再读取前三条评论：paused真实值、暂停事件计数、评论随机值，不以助手说完成作判据。
2. 多个同名/同类型目标中的明确对象：选择目标并改变控件/字段，旁边对象不得改变；覆盖B歧义/C编号。
3. 延迟就绪的控件或结果：等待真实条件再操作/确认，不靠反复模型读页、不重复触发。

模块反例另覆盖旧run/其他成员/已绑定在途/unknown/已满足项、关闭与换文档、工具禁用、程序中断。

效率是探索性比较，不在此凭空规定秒数。若候选在同类任务反复比A慢20%以上且没有明确可修的基础设施原因，不作为默认上线候选。报告每条原始值，不只输出一个平均值。两候选均有效时优先更简单且减少重复登记的方案。

## 交付边界

只处理登记与绑定，不串入语音改口抢占、音色、UI布局、供应商切换、上下文截断或推理降级。完整历史和当前WIP必须保留。隔离副本来自当前工作区，禁止git reset/checkout覆盖。

## 本轮执行结果

18次真实对照完成，按用户修订的单一指标保留A：A 6/6、平均83.701秒；B/C各5/6不达标。主代理已复核原始JSON与源码无漂移。第6项真实对照及修订后的选型判定已完成；第8项候选回接不适用，正式产品保持A。本轮未把候选模块检查宣称为全部真实边界通过。

原始数据、失败与收尾证据见[主代理选型结论](../tasks/20260910-binding-ab/decision.md)和[完整实测](../tasks/20260910-binding-ab/report-measured.md)。
