# 任务：Realtime 滚动后可继续多轮，未知写入保留核查与恢复边界

基线 main@94b1782；保留已有未提交直连接入。用户授权直接修复、两条定点检查、加载日常；不提交，不跑全量测试。

## 完成标准

- [x] A：真实 TaskProgress、ConversationManager、BrowserAgentSession 和既有工具执行链中，scroll 成功 → 新 input 读取 → 新 input 切标签可继续；同 input 身份不变、状态 running/idle、有开始时间，不伪造目标完成 — 检查者：定点测试。
- [x] B：模拟 RPC 写入结果未知，后续只读核查可用，open/switch/close 与重复填写被阻止；新 input 保留原 run，无关回执不解除，原调用有效回执解除对应项 — 检查者：定点测试。
- [x] 类型检查通过、日常扩展重载、新 Native 与连接日志核对 — 检查者：主代理。
- [ ] 日常真人连续语音体验通过 — 检查者：用户。

## 故障与最小修复

核对 ~/.sideagent/agent.log 6623–6683：12:15:17 的 scroll 成功；12:15:30 snapshot、12:16:14 tabs 进入直连后被“上一项操作结果尚不确定”拒绝。前一个过期话轮取消不算故障。

控制权写工具清单把 scroll/hover 算进持久副作用，但结果登记排除它们，成功后仍把审计完整性设为 false。直连入口又在新 input 拒绝全部工具；status running 没有设置 startedAt，最终回到 none。

- 复用结果副作用分类，区分 scroll/hover 与持久写入；控制闸门不变。JS 等宿主明确判定的副作用必须登记原调用，不再被辅助工具排除清单吞掉。
- 新话轮遇到未决项时沿用原 run、原结果。只读工具走原执行器；写入仍走已有逐项执行保护。tabs 按实际 action 区分。
- 错误给出结果项与原调用编号；复用已有迟到回执及核验机制。读取本身不清除未知。历史总标志没有原调用信息时保留限制，只允许读取并要求人工核对，不猜测清锁、不改原始日志。
- status running 在已有 run 上记录 startedAt，结束仍由既有 finally 的 idle/user 收敛；不调用 Pi，不制造 agent_end 或目标 verified。

## 定点证据

`npx vitest run agent/test/realtime-multiturn-repair.test.ts`：修前 A/B 均在真实直连入口报同一拒绝；修后两项通过。RPC 边界为模拟，进度、工具注册、执行检查、生命周期为生产实现，不是手填全绿快照。测试另覆盖 JS 未决回执、历史缺失记录保持限制、执行前取消。

首次测试夹具把 scroll 参数写成 direction，并且模拟超时遗漏 executionFact，分别被参数校验拒绝及误记 not_executed；纠正夹具后才记录有效修前失败。首次类型检查发现测试回调缺类型，已修正。

## 边界与不做

- 不改模型、意图判断、语音播放，不增工具或平行账本；不做全量测试、泛化评测或提交。
- 不自动迁移/清除旧会话无逐项证据的全局异常。重启后的检查点允许只读核查并保留 interrupted 状态，写入仍须沿用原恢复流程。
- 不把工具结束当作用户目标全部满足；真实模型如何选工具、真人听感与日常多轮操作由用户试用确认。

## 加载与收尾

- 20:31 最终 A/B 通过；扩展类型检查已通过，测试类型修正后 Agent 类型检查通过。仅运行这两条定点测试，没有运行全量。
- `npm run build`、`npm run reload:ext` 通过，扩展 fnbjglhppbkgmjeehablkfilmmefjolo 重载。
- 旧 Native 40571/40572 退出，新 56824/56873 于20:31:19启动，加载本仓库 tsx 源码；agent.log 12:31:26.662Z 启动、12:31:26.663Z 面板连接。
- 最终差异检查通过。生产修改集中在五个文件，沿用既有分类、结果项、回执和状态消息，无新依赖、存储结构或模型调用；主要增长来自直连入口对核查与新任务的分支区分。既有未提交接入及其他文件保留，未 commit/push/stash/reset。
- 同类检查限定受影响路径：TaskProgress 两处审计缺口标记在容量溢出/未记录副作用时仍保留；未把缺证据一律改成成功。取消前拒绝有定点覆盖，真实执行中插话及真人模型多轮未另跑，交用户试用。
- 本次原因与边界已由代码、定点测试和此验收解释，不另建重复经验条目。
