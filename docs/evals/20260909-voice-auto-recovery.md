# 任务：可恢复的语音断连自动续接，不让用户反复点重试

派发前冻结，承接用户15:22截图与长期对话反馈。Boss独立Evaluator；执行者不改标准和Boss测试。

## 完成标准
- [x] 1. Native输入支持Chrome允许的较大合法帧：大于1MiB、低于64MiB的截图型JSON可被分块接收，随后小帧仍正常；输出仍限制1MiB，超过64MiB的输入头安全拒绝。— 谁检查：Boss独立stdio生产解码/传输测试与子进程验证
- [x] 2. 已开启语音后，侧栏Port断开或本地连接暂断立即显示恢复状态；连接恢复后同一用户会话自动恢复可收音/回答，无需点击重试。用户已有麦克风流可用时复用，不重复请求权限。— 谁检查：Boss生产VoiceClient+VoiceRelay故障注入、隔离Chrome实际UI事件
- [x] 3. 明确可恢复的上游网络/会话时限错误可以自动续接；权限拒绝、未配置凭据、模型不匹配等明确不可恢复错误不无限重试。恢复有有限次数和总时限，耗尽时给出实际原因与可操作入口。— 谁检查：Boss虚拟时钟/错误类型反例测试，真实语音重连
- [x] 4. 用户主动结束/切换会话/关闭侧栏后不偷偷恢复；旧连接音频与回调不得进入新连接；断连期间不缓存重发网页写指令或整段旧音频，已接受/未知结果只保留或查询原回执，不自动重做。— 谁检查：Boss独立事件序列测试
- [ ] 5. 真实连接故障注入后，下一句通过生产语音路径得到回答；保留断连类别和恢复结果的诊断，不记录敏感原文、音频或凭据。— 谁检查：Boss隔离浏览器/生产类/真实语音服务；人判断连续体验
- [ ] 6. Boss统一typecheck/test/build/diff通过；前轮连续对话、本轮接收回应与底栏修复仍保持正确。— 谁检查：Boss

## 边界与证据
- 用户当前浏览器由用户使用；不通过重载用户扩展制造故障，不操作其邮箱/X/YouTube。测试使用隔离环境。
- 现有记录确认15:22:34、15:23:30 native host重新启动；尚无该次帧大小日志，不声称大帧是唯一已证实触发原因。
- 官方协议：host→Chrome 1MiB；Chrome→host 64MiB：https://developer.chrome.com/docs/extensions/develop/concepts/native-messaging
- 不恢复未知网页写入，不承诺断网期间任务仍在执行，不把重连成功当作原任务完成。
- 保留本轮全部修改；不提交、推送或自动重载。执行者仅聚焦自测、直接消息交回，Boss独占最终验收。

## Boss独立结果（15:46，整体验收尚未完成）

- Native独立3测试与真实子进程通过，输入1,200,018字节分块JSON后继续处理小帧：`ego-native-frame-gr92ecwg/result.json`。
- 客户端7项、上游3项独立测试通过：静默预算、重复ready、麦克风复用后移除、主动停止、永久错误、29min时限、已握手后网络超时。
- 最终构建生产DOM在隔离Chrome下30种几何组合、8项按钮/Port恢复操作通过：`ego-composer-fit-hdjuXy/result.json`。背景Port和麦克风为测试替身。
- 生产VoiceClient检测/播放路径+VoiceService+真实Step自动重连后下一句回答“好的，随时可以继续聊。”，麦克风复用、同会话新voiceId：`ego-voice-recovery-NKkvJ7/result.json`。输入为合成PCM，播放完成模拟，不是人耳验收。
- 819项测试和构建通过；最终类型检查发现执行者测试类型错误，正在修复。连续对话回归有真实失败（见NOTES），第6项未通过，不能封板。
- 第5项机器故障注入路径通过；真人连续体验待用户裁决。
