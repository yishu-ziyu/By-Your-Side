# 任务：运行中说“预算改成八百”，原任务收到修改并按新条件继续

## 完成标准
- [ ] 运行中语音修改走现有 Pi steer，等待队列接收成功才生成成功回执；不 abort/reset/prompt，不新建会话。— 单测与真实执行
- [ ] 仅提交该语音轮的最终用户转写，模型工具不接受任意文字参数；转写晚到时等待，缺失/旧轮/重复工具不重复执行。— 协议测试
- [ ] 空闲、已结束、暂停、转写缺失、发送失败均明确拒绝，不能自动变成新任务。— 单测
- [ ] 进度查询和闲聊不触发修改；页面事实中的指令不能成为语音工具的指令内容。— 协议与真实上游测试
- [ ] 侧栏显示“已送达当前任务”的原话记录，语音只确认收到，不宣称网页修改已完成。— 浏览器
- [ ] 同一真实网页任务从1000预算运行中，通过原生音频“预算改成八百”修改后，后续筛选真实采用800，任务不中断重开。— 真实Step/Pi/Chrome路径
- [x] typecheck、测试、build、diff检查通过。— 机器

## 边界
- 首条闭环只实现调整正在运行的主任务。暂停/继续随后沿现有接管流程扩展，不把“先停一下”翻译成预算修改。
- 不开放语音新建任务，不增加网页工具权限，不修改既有接管/中止按钮。
- “送达”与“完成”分开；原生工具漏触发不能称修改成功。


## 实测推翻与修正
- 初版原生 steer_current_task 工具没有触发；语音却声称已改800。真实Pi原任务只启动1次，但网页budget=1000，结果仍含899。该轮判为失败，证据 `20260908-voice-steer-native-failure.json`。
- 改为应用主动路由：等待本轮Step最终转写，调用当前任务模型做无工具、无任务上下文的EDIT/NONE判断；EDIT才提交原话。成功/失败回执注入后才允许Step生成语音。旧轮或已结束语音的判定不得执行。
- 当前MiniMax-M3实测：预算修改EDIT；进度、计算、引用他人修改语句、暂停请求均NONE，5/5，耗时1157–1703ms。没有改变原任务的执行模型。
- 真实Step原生PCM先产生ASR，再由应用路由，最后生成语音已验证；无任务预算修改正确播报“没有正在执行的任务，修改未发送”。未先播放未经确认的成功回答。
- 新路由的完整网页复测被ChromeMain无窗口阻断（open_tab: No current window）；用户异步开窗问题待回复。不把初版失败或后台测试当作800筛选已经通过。

- 最终机器检查：78文件/655测试、typecheck、build、diff --check通过。仓库凭据扫描通过。新流程运行中网页验收未完成，不能勾选真实800筛选。
