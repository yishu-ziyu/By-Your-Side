# 17:32 日常试用：算术等待、语音未执行翻译

用户反馈：1+1等待过久；语音能回应问候、识别GitHub，但翻译请求后冗长解释并说没工具、提供建议，未完成期待的网页操作。

## 文字
[只读调用链诊断](20260921-1732-text-diagnosis.md)：真实任务从17:32:05.832到17:32:20.703，共14.87秒，最终回答1+1等于2。MiniMax M3三轮串行13.68秒，任务强制inspect→plan→回答；首轮输入19112 tokens。此会话模型与Native启动默认模型不同。

影子漏记是实现缺陷：真实侧栏发task_action(source:text)，在conversation-manager的user_message观察点之前返回；此前测试直接输入user_message，未覆盖真实侧栏协议。不是开关或Jev超时导致。本轮影子路由验收文字项失败。

## 语音（主代理核对）
原始证据：`/Users/mahaoxuan/.sideagent/agent.log`，`/Users/mahaoxuan/.sideagent/voice-capture/2026-09-21.jsonl`，`/Users/mahaoxuan/.sideagent/route-shadow/2026-09-21.jsonl`。
voiceId：86013f12-595c-4c86-9748-1f49f2b2d70f。

- 17:32:33.636 收到「你好呀，你能听到我说话吗？」；Jev建议chat，833ms。
- 17:32:42.784 收到「当前这个页面是英文的，你可以将它翻译成中文吗？」；Jev建议task，Choice概率0.98，846ms；pageChange Noul为0.57，两项不可混为同一置信度。
- 实际仅read_page一次，17:32:42.567调用，42.584完成（16ms），43.993回传；没有task_action/browser_request派发。
- 44.031创建工具续答，49.416生成结束，58.488关闭。没有新的翻译任务执行记录。
- 现行提示词已要求一句短话、操作先调任务工具，且提供task_action/browser_request；问题不等于缺少任务入口。运行表现是读页后直接作答，没有交给页面执行器。
- 用户关于冗长、声称没工具的听感作为反馈保存；日志未保存助手逐字口语，不能编写原话。当前保留转写为两条，不额外推断用户描述中的每句是否独立成轮。

## 下一步
1. 修复真实task_action文字入口的影子接线，用真实消息类型回归。
2. 简单问答不再强制走页面任务规划；这是行为改动，应以真实1+1路径为完成标准，避免只对这一句硬编码。
3. 语音明确页面操作不能以读页答话结束；需决定宿主如何使用路由判断约束实际派发。影子目前按授权只记录、不干预。
不新增泛化评测、不靠加一句提示词作为本次问题的解决证据。
