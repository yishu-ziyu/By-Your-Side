# Jev 显示修改：独立 review 五项修复

基线：`867f60b22b78cbb95f85d301db07f77890a586f9`。用户授权当前 Chat 直接修复；不委派其他模型，不改日常配置，不启用新增开关、不重载日常扩展、不推送。

## 完成标准

- [x] R1 页面实例变化后本条修改失效，不转主模型重放；释放本条登记，新请求仍可执行。— 机器：反例、真实隔离浏览器
- [x] R2 页面执行事实与交接状态分开；交接失败仍阻止旧计划写入，恢复只交接、不重做显示。— 机器：故障注入、恢复测试
- [x] R3 读回结束与回执交付时再核对任务身份/控制状态；取消后无“原任务继续”，已有执行事实不抹掉。— 机器：受控竞态、真实隔离浏览器
- [x] R4 已核验/未核验/未知分别记录；结束与取消提示不把未知或未执行要求说成成功。— 机器：组合场景测试
- [x] R5 独立验收器检验原任务结果、交付归属与实际重复写入；错误答案/缺失概括/错 run/重复实际操作必须失败；偶数中位数正确。— 机器：验收器反例与真实脚本；真实关闭臂漏答已被判失败，不表示整套产品验收通过
- [x] 定点回归、P0、类型、边界、构建及完整检查留存实际结果；既有失败不删、不降标准。— 机器；完整检查仍有已注明的既有失败

## 边界与证据

- 不新增显示能力，不调整 Jev 提示词/阈值/模型，不扩展权限或替换 Pi。
- 原 review 证据保留在 `out/review/jev-display-867f60b/`，新证据写独立目录；历史配对不得重新标为通过。
- TypeSafe 当前文档核对：`patterns/fan-out`、`confidence`。本轮不改 API，仅修正代码对执行事实与有效性的处理。
- 真人麦克风、TTS 听感与启用性能门槛不属于本轮结项。

## 实施与验证

### 运行时与回归

- 原四个独立反例在本轮修改前重新执行：4/4 失败，见 `out/review/jev-display-fixes-20260918/before.log`。没有删除失败要求。
- 正式纳入 `agent/test/jev-display-review-regressions.test.ts`，扩充为 10 项：包括刷新后的新请求、超时/不确定回退的刷新、交接失败后的原接管/交还恢复、成员交接期间取消、混合成功/未执行提示。
- 修改 `session.ts`、`conversation-manager.ts`、`task-progress.ts`。未知写入账本与原执行器保留；`TaskActionRejected` 的本次要求登记可以定向撤销，不删除已发生的执行事实。
- 最终定点 15 文件 216 项通过，P0 162 项通过，规模 2/2 通过，构建通过。见同目录 `final-targeted.log`、`p0.log`、`scale.log`、`final-build.log`。
- `npm run check` 退出 1：190 个生产文件边界与类型通过；普通测试 2053 通过、1 失败。唯一失败仍为 `tool-surface` 的既有提示词 15168 > 15000；本轮未改提示词和门槛。见 `check.log`。

### 真实刷新与取消

运行 `npx --no-install tsx scripts/acceptance/jev-display-steering.mts --headless --review-races`，退出 0，2/2 场景通过，证据：`out/acceptance/jev-display-steering-1789730475937/`。

实际隔离 Chrome、构建扩展、侧栏、生产 runtime 和当前任务模型；Jev 响应及读回到达时机由测试控制。不是 Jev 准确率/速度或真人语音测量。刷新场景旧请求 rejected、零显示调用，失效要求不留在 recoveryInput；取消场景保留已发生的 1 次写入，回执 failed，不称“原任务继续”。

### 验收器修正与反例

新增独立 `display-steering-oracle.mjs` 与测试。判定最终同 run 的原任务报告、4 个正文段落、完整非空且不超过 40 字的概括、固定页面四个主题的覆盖、真实请求回执、原页面、真实 RPC 执行与重复操作；此为固定 fixture 的可执行检查，不是任意文本的语义正确性证明。

只含 99 段/缺少概括/无关概括、其他 run 交付、未知写入、重复实际执行、失败交接都不能通过。允许同一 RPC 证据重放以及字体/模式分步操作。失败样本留在分母及配对记录中；成功页变更中位数和全部尝试总耗时分开，缺失页变更时间记 null。第一轮历史成对中位数的正确值为 -7247ms，不是 -7119ms；历史文件未改。

第一次新 smoke 的开启臂被验收器误判：它把同一任务的两次正确报告拼接，导致 count/summary 各出现两次。实读原始两份报告后修正为“最后一次原任务报告整份核对”，并补“较早正确、最后错误不能通过”的反例。原始 `out/acceptance/jev-display-steering-1789730534036/` 的退出 1 保留；最终验收器从原 events 补取真实回执，离线重评 2/2 通过，另存 `out/review/jev-display-fixes-20260918/smoke-regraded.json`，不冒充新实测。

### 最终脚本与全部中间尝试

1. `out/acceptance/jev-display-steering-1789730676875/`：完整 10 对/20 次 + 6 边界，旧同会话串行设置。开启臂 10/10，关闭臂 3/10，边界 6/6（当时边界尚未叠加原任务报告检查），整报告退出 1。失败包含漏原任务、未改显示及擅自沿用旧字体；同一会话中的前臂事实污染后臂，不能把此运行当作隔离公平对照或选择好的一半作性能结论。原始证据保留。
2. `out/acceptance/jev-display-steering-1789731376013/`：新建会话但复用页面，第二臂预置被原页面归属保护拒绝，退出 1。是测试台隔离不足，未放松产品所有权检查。
3. `out/acceptance/jev-display-steering-1789731631681/`：新建页面后，预置译文仍用 default 身份，造成误归属；真实执行被挡后走协调回退。修复测试调用携带本臂 conversationId，并把被拒绝的直达尝试与实际已核验直达区分。该轮关闭臂还漏原任务，原报告及退出 1 保留。
4. **最终脚本**：`out/acceptance/jev-display-steering-1789731763051/`，`identity-browser-smoke.log`。每臂新会话、独立等价网页，预置使用本臂身份；完整原事件和启动时 sourceDigests 保留。真实当前任务模型 + Jev，开启臂全部检查通过（页面变化 396ms，原任务段落数/概括、回执、归属和执行核验正确）；关闭臂页面正确但漏交原任务，验收器准确判 `paragraphCount=false, summary=false`，命令退出 1、`smokePassed=false`。这不是两臂通过，也不是最终 10 对验收。没有改提示词预算、替换模型、放宽成功标准或启用新增能力来消除失败。

未改变原用户任务，只要求原来的段落数与 40 字概括各用命名行输出以便检查；后续正式性能对照必须使用最终逐臂隔离脚本，不能引用中间同会话对照。

最终两文件专项回归 23/23（运行时 10 + 验收器 13）通过，见 `final-regressions.log`。新增开关仍未设置/关闭；工作留在本地未提交，不推送、不重载日常扩展。五个已确认代码/验收器缺陷修复完成，不宣称关闭臂原任务漏答、完整工程检查或真人语音已结项。
