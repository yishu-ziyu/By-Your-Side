# 任务：从文档入口能找到一致的当前状态、架构和验证边界

## 完成标准

- [x] STATUS 仅维护当前结论、未决问题、下一步及证据；旧状态移至历史入口 — 谁检查：主代理检查差异与链接
- [x] 安装、架构、语音说明符合本轮核对的源码；提示词、程序约束、实测结论分开 — 谁检查：主代理对照源码
- [x] 已加载版本、当前源码和本机配置不混用；保留用户体验失败与未验证项 — 谁检查：主代理对照配置、加载记录和反馈
- [x] 历史验收与失败证据不删；改动文件本地链接可解析；产品源码未被本任务改动 — 谁检查：链接扫描、快照与差异核对

## 边界与不做

仅整理文档。用户明确要求主代理直接完成。性能诊断让位于此次文档整理；不修产品、不跑产品测试、不重载、不发布，不替另一任务宣称验证完成。

## 依据

工作树 main，HEAD `3605347`；产品另有未提交改动。只读核对本机配置中的 model/generalBrowserLoop/displayFastPath/displaySteerFastPath（不记录凭据）、日常加载报告、语音反馈与当前装配代码。
历史页面保留原文内容，仅改相对链接并加历史标识。历史材料可包含已知矛盾，不能重新作为当前开工依据。

## 检查结果

- 已核对源码装配、任务入口的条件分流、语音工具/播放状态、记忆工具名和 program 运行器。协议中旧 `remember_user_preference`、EverOS 一概未接入、固定方法数量等陈旧描述已纠正。
- 本机配置三个实验开关均为 true；日常加载证据与未加载候选分开。整理期间纳入另一开发任务 22:01 交接：最新语音候选尚未加载、切标签回归失败、完成正文与 unverified 账本冲突、音色未解决。
- STATUS 从 44,170 字节压缩为约 5 KB，NOTES 从 41,548 字节压缩为约 3 KB。删减的是当前入口重复阅读量，不删除历史知识。八份历史快照逐份核对：除历史标识和相对链接调整，正文与写入前快照一致。
- 当前入口、专题说明、本次验收与八份新增历史快照的相对文件链接全部可解析；git diff --check 通过；原票组 CLAIMS 指向旧 STATUS 标题的锚点已修复。历史资料并未逐项重新验收，其当时结论明确标为历史。
- 产品源码/脚本/评测文件与本任务写入前哈希一致；原有未提交产品改动保留。未运行产品测试/构建，未重载、提交、推送或发布。

## 后续维护

同一事实在 STATUS 更新一次；架构保留实现边界，eval 保留各轮证据。新进展替换旧状态，相关历史不追加回当前入口。文档不授予外部操作权限。
