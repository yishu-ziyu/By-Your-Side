# 任务：明确的确认、纠正和控制得到正确推进，不陷入重复回应

## 完成标准

- [x] 1. 审查文字、语音、任务调度、交付重试、接管恢复、多会话、知识复用、授权入口的同类失败路径，记录覆盖边界 — 谁检查：主代理与独立审查；证据：源码路径及用例表。
- [x] 2. 每个确定缺陷先有具体输入序列/失败检查，再修复并检查相同路径；保留否定、过期、重复、跨任务反例 — 谁检查：定点测试与主代理。
- [x] 3. 修复涉及的可操作侧栏路径用原生电脑控制复验，真实语音未跑不得计为通过 — 谁检查：主代理；证据：原生页面/日志。
- [x] 4. 更新当前状态与未覆盖项，不用单测通过宣称全产品零缺陷 — 谁检查：主代理。

## 边界与不做

- 基于现有产品承诺及这次确认循环展开，不重写架构，不处理用户已降级的教学措辞。
- 保留已有大量未提交修改；无提交、推送或真实业务写入。
- 确认不应确认自身，取消不能变成批准；纠正仍须保存新约束与原上下文。

## 检查记录

本轮开始：已修复“是的，确认”复合肯定，但否定、澄清插话、重试及其他状态转换尚需独立检查。既有功能路径证据见 feature-journeys，避免把历史成功算作新反例通过。

## 已确定的同类缺陷与修复

| 路径 | 修前失败 | 修复与反例 |
|---|---|---|
| 控制确认的否定 | “不用了，谢谢”/“不，不用了”重新进入分类器并确认自己，4个定点失败 | 纯否定组合直接撤回；带“改南京/不保存”的新要求不吞掉，双重否定不误当撤回 |
| 另开会话询问 | “好的，另开会话”“算了，不用了”落回分类，3个定点失败 | 用同一肯定/否定规则；同意送原新任务，拒绝不新建 |
| 待确认生命周期 | 进度查询/系统只读恢复删待办，空白轮占用turn让确认失效，3个定点失败 | 查询保留，允许非相邻语音轮；新执行指令或新话题清除；旧连接不能消费新待办 |
| 接管恢复显示 | 成员A与活动B都显示条，恢复只清A；中止/断线同样遗漏 | 按会话实际展示范围清理；逐页队列读最新期望状态，旧A迟到/同页旧文案/其他会话均有反例 |

修订依据：旧测试把“跨一轮”一律当失效，现仅拒绝不晚于原要求的旧轮次，TTL、voiceId、runId、controlVersion、去重与原资料约束保留。旧“你好”新话题取消提案测试仍保留通过；没有一概让所有闲聊保留待办。源码字符串测试的单行格式断言改为入口存在，实际否定行为由manager+dispatcher用例覆盖。

## 证据与独立检查

- 初始全量：175文件，1519通过、1项task-dispatcher性能超时；该文件独立复跑7项通过。保留两次结果，不宣称初始全绿。
- 最终受影响语音/会话/控制条：24文件223项通过；类型检查与构建通过。
- 两位DeepSeek执行者分别完成复合否定与控制条初修，provider opencode_go/deepseek-flash经receipt核实；主代理补生命周期、并发消费及逐页绘制修复。
- 两次DeepSeek只读审查分别350/250秒到预算，没有完整报告，不算通过。独立原生子Agent接手，生产函数替身探针发现并再次核对修复，最终限定范围无阻断。主代理核对代码和最终用例。

## 原生续验（阶段记录，后续结果见下文）

- 原任务结束后说“不是苏州，是无锡，仍不要保存”：原页变无锡，甲1/乙1保持，未保存；真实回复8.1秒。trace为1789272969327-d235bdc5-319d-4c6d-9b41-e2801e3dc95b，run c1e657c0-b0d3-4321-b91e-825f45384465。
- 新会话只问不存在的“提交到服务器”按钮：3.6秒回答没有，页面无新事件；再说“是的，确认”：3.4秒说明没有待确认动作，没有执行凭空任务。文字回复偏长，未改口吻。
- 新会话输入框为空，原草稿恢复受控制器菜单取样时序影响尚未取得完整证据，不能算整条草稿隔离通过。
- 原生控制器部分截图仍停在旧画面；重建取图服务与AX会话后恢复操作。照片不作新的视觉通过证据，实际AX和任务日志分别记录。
- 已经原生点击扩展详情的“重新加载”，界面出现“已重新加载”。修后声学语音未重测。

## 覆盖边界

交付/重试、文字纠正、任务调度、语音输入与播放、控制接管、会话隔离、授权入口均经源码定点审查与现有全套回归；本轮重点反例见上表。现有记忆、技能、组合程序的原生证据仍引用feature-journeys，不伪称本轮逐项重跑。真实语音仍待补；修复后控制条原生路径已在下文补齐；产品总体零缺陷无法由本次有限输入证明。功能承诺完整验收余项仍保留在feature-claims。


### 原生验证带来的新增修复

实际任务A运行时切到新B页，接管后B出现“现在归你/交还”，点击交还无反应；查顶层handback_click确认按findSessionForTab找页面归属，B并非任务页而拒绝。改为只读取扩展内部已完成绘制的控制条owner；清理后getter失效，不接收页面提供的会话id。新增owner切换/清理测试，43项控制相关通过；独立复核无阻断。此项修前真人操作路径证据为05-other-page-held，修后续验仍在进行。


## 最终代码与主代理实测

新增两项同类根因已修：发送时页面没有落到执行器，导致首次操作随active漂移；旧aborted team与新任务未按runId隔离，导致新任务的控制按钮消失。前者补完整RPC与执行器参数链，后者补面板、background缓存、重开同步及历史消息身份。不能只改selector把旧状态隐藏，也不能只在读取里附tabId。

- 最终全量179文件1569项通过；两端类型检查、构建通过（final-suite/final-typecheck/final-build）。初始性能失败仍保留，未修改门槛。
- 默认目标：原生B发送后切A，B成都未保存，A零事件（08/09）；原B关闭后未误操作A、未新开页（10）。trace 1789276124076-8e70e377-1869-45f8-b6d8-44899b2313c6。
- 新任务控制：先接管中止，再同会话启动新等待任务，旧已中止卡消失，接管和中止按钮恢复可用（11），并实际接管成功。
- 非成员页交还：纯等待期间切handoff-final页接管，该页按钮实际恢复原任务，侧栏全队已恢复，原生截图无残留恢复条，页面仍零事件（12/13）。AX可能保留opacity隐藏的旧文案，不能只凭AX字符串断言视觉残留。
- 初次多步测试的步骤3本身会触发fixture保存，与“不保存”提示矛盾；不能把该保存算作模型擅自单击保存，之后改用纯等待用例隔离控制路径，原记录保留。
- 两项执行的DeepSeek代码worker分别521秒完成部分默认页链、400秒到期留下部分team接线；主代理补齐生产调用方。独立native子Agent多次审查并生产函数探针复核，无剩余确定阻断。另一次180秒DeepSeek审查也到期未交报告，与此前350/250秒一致，不计作审查通过。

侧栏重开/草稿恢复已由DeepSeek原生执行者与主代理补齐，见证据目录14-deepseek-ui.md。真人声学仍未重测。


## 最终追加检查与加载（13:44）

- 自然肯定尾词“确认吧”“好的，可以啊”“没问题，照做”现在仍消费原待办，带新约束不吞掉；“对了”不能误批。DeepSeek 执行和独立 DeepSeek 复核完成，主代理补“对了”反例；60项定点通过（ack-final-tests）。
- 跨页提示初修只检查外层 await，独立探针继续发现内部 ensureCursor 迟到重画、A清理误隐藏B两条确定失败。DeepSeek repair 在240秒截止时只留下两条红用例，主代理完成逐页 owner 与串行绘制，注入后重新读取最新意图；任务名牌同样修复。关闭目标页清理、点击目标与显示 owner 一致；独立 Agent 复核未发现确定阻断。
- 最后追加72项定点通过；类型检查首次暴露 nullable tabId，保留修复后成功日志。两端 typecheck、build 与 git diff --check 最终通过。全量179文件1569项是追加前的结果，不把它写成追加后全套重跑。
- 最终构建已通过原生扩展管理页重载。真实A页启动40秒只等待任务，切到B页：截图15能看到“正在另一个标签页工作”；25秒时点击中止，截图16提示消失、侧栏空闲、任务已终止，B表单事件0。不同 owner 的精确竞态由受控异步测试验证，不冒充真人碰巧触发竞态。
- 侧栏重开、原草稿恢复已通过。没有第二次真人麦克风试验；技能新示范到复用、网络重连故障及功能承诺表其他未验项仍保留，不宣称全产品零缺陷。无提交、推送或真实业务写入。
