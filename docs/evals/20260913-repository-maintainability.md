# 任务：整理仓库，让修改入口清楚、模块职责独立，现有用户操作保持一致

## 完成标准

- [x] 1. 盘点生产入口、模块依赖和历史资料，记录明确的维护热点及本轮边界 — 主代理检查实际引用。
- [x] 2. 将确定独立的功能从大入口提取为有明确输入/输出的模块，不复制实现、不引入新框架或依赖，不改变协议与控制身份语义 — 独立代码复核与回归。
- [x] 3. 当前状态集中在 STATUS；README 与开发导航指向真实模块和命令；历史状态保留可追溯 — 主代理核对链接及差异。
- [x] 4. 增加低负担的模块边界检查，能阻止跨宿主依赖倒置；定点、全量、类型与构建通过 — 机器检查。
- [x] 5. 重载后的真实侧栏验证受影响入口，保留未验项 — 原生电脑控制。

## 边界与不做

- 起点为 main 上已有大量未提交修复；这些是本轮基线，保留，不提交/推送、不重置。
- 不重写产品，不调整语音确认、任务状态、权限或页面绑定语义。
- 历史日志与验收证据不删除；不把迁目录、减行数本身算作架构改进。
- 大入口拆分以完整功能边界为单位，不用任意行数阈值或万能服务容器。


## 实际整理与边界

- 盘点生产Agent/扩展/共享模块，独立审查四个主要入口。侧栏入口3265行、background入口1680行、session1312行、manager819行是起点；不以行数本身判定质量。
- 模型选择器移到 model-picker.ts，独占modelState/modelQuery；入口只注入DOM和set_model出口，通过apply/update/reset/reposition同步回执与布局。main降至3031行。没有复制状态或新增框架。
- 语音编辑判定、整句分类、只读观察移到 voice-model.ts；session原方法保持，现取当前模型与provider头后转发。移出的密集代码改为可读结构；总源码行数增加，不宣称靠减行实现精简。
- 授权账本原放shared却依赖node:crypto；生产只有Agent使用，纯移动到agent/src/consent-ticket.ts，调用和测试改引用。共享展示协议不变。
- check:architecture使用现有esbuild解析运行依赖，已检查163个生产文件。Node/shared、Agent/扩展、扩展内宿主方向与生产导入测试都有反例；实际仓库图也接入npm test。类型引用、计算型动态路径、行为耦合和任意循环依赖不在此检查保证范围。
- README过期未实现清单改指STATUS；STATUS/NOTES整理为27/29行当前入口。整理前408/1675行完整归档到history，相对Markdown链接随位置调整；文档导航和架构修改落点已补。当前入口链接逐项存在。

## 检查记录

- 模型相关95项、语音相关25文件283项由执行Worker检查。两次执行receipt均核实opencode_go/deepseek-flash，不以completed_unverified代替主代理验收。
- 初次类型检查暴露新增边界脚本缺声明、新voice测试mock参数类型不完整；补声明并修mock后两端通过。失败和后续成功分别保留。
- 首次全量1599通过/1规模测试撞5秒timeout；原文件单独7项通过。不是产品行为断言失败，也不能把全量写为全绿。
- 将两项规模测试原样移到performance目录，npm test普通回归后顺序单worker运行规模测试。20k/200会话/1000次查询、50ms与20ms门槛、默认5秒超时不变。旧voice-dispatch faults入口补相同的顺序规模测试，避免搬文件导致覆盖减少。
- 最终npm run check通过：163个生产文件边界、两端类型检查、182文件1598项普通回归、1文件2项规模检查、扩展构建。见check-final.txt。隔离规模测试组织经独立只读复核，无确定阻断。
- 本轮未执行真人语音、发布live评测或voice-dispatch的真实故障注入；行为重构不沿用旧真人结果冒充新结果。

## 独立复核与原生入口

独立DeepSeek审查600秒达到预算，没有完整结论，不算通过。另一位只读子Agent接手限定源代码，确认模型选择初始化/回执、语音模型及取消预算、账本归属、faults规模入口保持；发现checker漏绝对路径。主代理补POSIX/Windows/file:三反例，独立原失败探针复核通过。

最后13项边界检查（含真实163生产文件图）及两端类型检查通过；这发生在1600项整体检查之后，没有伪称全套又跑一遍。git diff --check通过，当前文档链接复核通过。

真实原生侧栏已补齐：搜索、两次Escape、鼠标及键盘选择当前模型、正常发送与回复、页面零写入。见[原生记录](20260913-repository-maintainability/ui-result.md)及截图。DeepSeek原生执行者遭遇控制器焦点异常后由主代理接手；保留部分失败记录，不算产品失败或Worker全通过。

历史失败证据、用户WIP和个人资料均保留；未提交推送。
