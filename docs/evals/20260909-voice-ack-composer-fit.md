# 任务：新任务接收回应贴合本句要求，窄侧栏下全部底栏操作完整可见

派发前冻结。Boss 独立 Evaluator；执行者不得修改本文件、Boss验收脚本或断言。

## 完成标准
- [ ] 1. 单个 start 确已 accepted 时，回应是一句简短口语，提到本次委托的对象或目的；不同委托不能都只说“任务已收到”，不靠随机轮换同义模板。— 谁检查：Boss真实语音3个不同领域请求；人判断自然
- [x] 2. 回应只承认接收/即将处理，不能声称已点击、已核实、已完成；旧轮/重放不新增操作。rejected/failed/unknown、pause/resume/abort，以及复合计划逐步回执维持既有事实约束。— 谁检查：Boss独立回归与真实原始输出核查
- [x] 3. 320、360、400、440、520 CSS px 侧栏宽度，空闲/运行/接管状态，语音打开与关闭，使用长模型名及思考标签时：document.scrollWidth不超过clientWidth（1px容差），可见底栏按钮边界均在composer与视口内，无重叠、裁切。— 谁检查：Boss生产侧栏DOM/CSS的隔离Chrome几何验收与截图
- [ ] 4. 模型入口、附件、语音、接管、停止/发送仍能点击；模型名允许省略显示，但操作不可靠隐藏或overflow裁切来“修复”。保持现有配色、粒子球、层级与其它界面。— 谁检查：Boss实际按钮事件与截图；人判断
- [ ] 5. Boss在执行者停笔后统一跑typecheck/test/build/diff；所有既有语音控制与连续对话检查保持通过。— 谁检查：Boss

## 边界与不做
- 当前前轮修改全部保留；不提交、不推送、不代用户重载。
- 此次为既有UI溢出修复，无需另选设计或做样式原型；不重新设计底栏。
- 接收回应保持低延迟，不新增独立provider或任务执行器，不读取用户邮箱/YouTube，不评判截图中GPT图像产品的可用性。
- 执行者只做负责文件和聚焦自测，完成/阻塞直接cmux回报。Boss独占全量与最终真实验收，文档保留证据。

## Boss独立结果（15:46，整体验收尚未完成）

- 真正的ConversationManager单步计划测试先红后绿，同一requestId重放不重复执行。
- 真实Step语音三类委托通过，`ego-voice-recovery-THDPDg/result.json`：图像工具网页/API检查、活动邀请只看标题、文章主要观点先不发布。实际回答提到本次要求，只确认将要处理。第1项机器通过，听感待人。
- 旧CSS在30种几何组合中24种失败；400px运行态曾横向撑至475px。修后最终生产bundle30/30几何检查及8个按钮/恢复事件检查通过，`ego-composer-fit-hdjuXy/result.json`。320/400截图已审：操作可见，无新裁切或隐藏；第4项机器通过，观感待人。
- 完整回归尚有内容连续性/口语问题，不能把局部通过写成整体验收完成。
