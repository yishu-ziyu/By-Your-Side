# 任务：把通用浏览器决策循环接入现有任务链，并以陌生任务验收

依据：[已讨论的方案与运作标准](20260920-general-browser-contract.md)。用户现已明确“开始改吧”；主代理亲自实施，不派子任务，保留其他未提交改动。

## 完成标准

- [ ] 1. 通用动态观察/候选、页面身份、决定与执行回执有统一契约 — 谁检查：类型与反例单测
- [ ] 2. Jev 连续决策复用既有工具/授权/任务账本，不能绕过改口、停止、权限与过期对象保护 — 谁检查：定点单测和隔离生产浏览器
- [ ] 3. Realtime 3 结构化任务交接与重复事件处理接通，状态不冒充完成 — 谁检查：协议反例及真实语音链路
- [ ] 4. 冻结实现后再首次揭晓陌生任务，依原规范单列回归/开发/留出结果与失败 — 谁检查：版本指纹及验收运行器
- [ ] 5. 安全、任务质量、性能门槛通过后才加载日常；真人认可独立记录 — 谁检查：主代理实跑、用户

Change: 未专门适配的网页任务通过共同循环完成，用户可以中途纠正，最终结果可核对。
Not this: 新增模块但未接线、模拟成功、熟悉翻译演示、只检查模型返回正确 ID，均不算交付。
Evaluator: 当前主代理执行机器与真实隔离验证，用户体验裁决。
Evidence: `out/acceptance/20260920-general-browser/`；具体执行记录续填，不预填通过。

## 2026-09-20 本轮执行计划（用户已要求先规划、再照计划执行）

1. **页面与动作组织**：按 EGO/Browser Use 保留作用区域、只读状态、选项与对象身份；拆开正文截断和控件缺失。先重现“无关控件变化误拦”“正文截断误退出”，补上相关字段变化仍拦截的反例，再改共同观察/执行判断。
2. **实际任务入口**：把通用循环接到任务开始处，复用既有已注册工具、授权、改口/停止和结果账本；不依赖测试口令“请调用 browser_loop”，不让每项普通操作先请主模型决定是否调用 Jev。开放式任务交回原任务模型；任何已执行动作必须带入交接，不能从头重放。
3. **材料按需补充**：Jev 选中需要填写的真实字段时才请求文字；用户给出的值保留来源，不编造个人资料；生成后复查页面和要求，再执行、读回并继续。缺材料是一次可解释的交接，不是重启任务。
4. **真实验证与陌生任务**：先用开发任务验证自然入口确实先运行循环（不是主模型被提示后调用）、文字/语音都正确，再完成独立任务源、实际执行器和结果核对；实现固定后首次打开陌生任务。回归、开发、留出、真人分别报告。日常开关过门槛才开。

本轮前置材料：已核对官方 TypeSafe 接口/状态/函数调用指导、Browser Use Jev Ultrafast、Cua 和[EGO 对齐](../devlog/20260920-03-ego-jev-alignment.md)。不移植社区脚本模拟点击来绕开当前真实输入和授权。

机器检查顺序：定点反例 → 类型/架构 → 隔离真实扩展与模型 → 按新失败扩大回归。既有两万条记录规模检查超时单列保留，不靠放宽时限改绿；它不是通用任务调度未完成的理由。

## 边界与当前状态

进行中。新循环在隔离环境启用，日常开关默认关闭；不改用户日常网页、不启动用户麦克风、不安装 Cua/桌面依赖、不动 `.serena/`。本轮不是仅交设计文档；达不到整体验收时必须保留未完成项，不擅自加载或宣称完成。
