# 跨会话记忆原型检查

日期：2026-09-08。产品代码未改，未调用模型，未写真实记忆。

## 结果

- ego-browser task space 98 中打开实际 HTML。浏览器 DOM 控件事件路径 20 项通过，结果：`/tmp/sideagent-memory-prototype-evidence/result.json`。仅操作表单和点击处理器，没有直接修改内部记忆对象。
- 原生 click/fillInput 走通 A：记住 → 新会话三条要点 → 修改为一段话 → 新会话一段话 → 忘记 → 空列表 → 再新会话无使用回执。B 原生点击走通保存、新会话、使用详情；双案其他行为复用同一事件链并分别检查状态。
- 独立校验脚本复核 7 项反例通过：无关任务、两条格式、不支持格式、历史版本、删除取消重试、来源会话、单案重置隔离。
- 390px 深色检查无横向溢出，截图已目视；1440px 双栏浅色已目视。桌面截图 `comparison.png`、窄屏截图 `mobile-dark.png` 位于上述证据目录。
- `git diff --check` 作为文档收尾检查。未跑产品 typecheck/test/build，也未将已有 550 项测试称为本轮通过。

## 修正与失败记录

独立校验发现并修复：任意“整理”请求伪造使用；两条格式仍返回三条；历史使用详情展示当前版本；忘记后旧失败重试使记忆复活；来源会话固定为 1。

模拟回复现在仅覆盖页面提供的会议材料；格式支持三条、两条和一段话。其他输入明示未覆盖，不制造使用结果。历史回执保存当轮版本，只有版本已变化或条目已忘记时额外显示历史内容，避免默认重复展示。

首轮批量原生点击检查未能进入新会话，未记通过。原因是测试选择器 `[data-act=close]` 命中遮罩，点击其中心实际被抽屉盖住；改成 `button[data-act=close]` 后同一路径通过。没有靠隐藏失败或直接改内部数据通过。

## 人评待定

P6 未通过：选择 A 会话内抽屉或 B 独立管理；确认仅显式记忆是否适合第一版，以及全会话/站点范围的默认规则。原型通过不代表 Pi 记忆、重启持久化、并发更新或生产接管已验收。
