# 任务：用简易HTML判断侧栏改后的布局、反馈和结果展示

## 完成标准（实现前）
- [x] 同一模拟任务并排展示现有结构示意与推荐方案；明确原型，不调用模型或写入产品。
- [x] 可切换发起、执行、结果；推荐方案可发送示例、接管/交还、展开过程与依据、打开/关闭记忆。
- [x] 浏览器点击检查主路径后交付。用户只判断：主次是否清楚、状态是否可理解、结果是否方便阅读。

沿用已读 Apple Design 与 Will's S 指引/反馈、渐进揭示原则；不新增设计调研，不改正式产品，不打磨与上述判断无关的细节。

检查：在应用内浏览器查看整体截图，实际切换阶段、发送示例、接管/交还、打开/关闭记忆、展开评论依据；已回到默认结果对比。人评待用户选择。无正式产品代码改动。
