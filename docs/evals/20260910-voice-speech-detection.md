# 任务：只改善收音的人声检测，随后交给用户试用

用户明确批准第一处：区分人声与环境声；第二处回声防误触、第三处转写过滤暂不做，试用无问题则到此为止。

## 完成标准（实现前冻结）
- [ ] 100ms非人声噪声反例由原检测触发，新检测不触发；静音、连续噪声和短冲击声不提交语音。— 机器：真实检测器与实际音频输入对照
- [ ] 中文短句、轻声短句和真实说出的“嗯”“对”“停”仍能触发；保留开口前音频，输入顺序和24k PCM协议不变。— 机器：音频样本；人：实际麦克风声音
- [x] 检测本地运行、资产随扩展打包；启动/关闭/重开/失败不会漏发旧音频或退回纯音量判断。— 机器：生命周期测试与正式扩展加载
- [x] 定点、typecheck、test、build和diff检查通过；真实可见Chrome验证本地检测可加载和工作。— 机器：主代理
- [ ] 用户日常试用判断误触是否改善、轻声和短句是否自然；用户未反馈前不称真人通过。— 人

## 边界与不做
- 只改收音入口，不做播放回声专门策略，不过滤“嗯”等转写，不改人格、任务路由、语音供应商或页面布局。
- 不把合成样本当实际现场录音；上一轮现场原始PCM没有保留。
- 本轮加载新版供用户试用，不提交推送。

## 实现与证据

- 第一处采用随扩展打包的Silero V5，通过本地独立Worker运行。原24k PCM保持不变，模型副本转16k；连续96ms高人声概率确认开口，保留320ms前音，700ms无人声收尾。极低能量下限仅排除数字静音，不以音量单独触发。未做第二、第三处。
- 9类样本在当前日常Chrome的正式扩展页面运行生产SpeechClassifier/Worker：中文短句、0.12倍音量短句、“嗯”“对”“停”各开始/提交1次；静音、100ms白噪声、连续白噪声、短冲击声均0次。原始结果与PCM：`/var/folders/k6/7c96rbxd1r782myg_bnlqshw0000gn/T/ego-speech-detection-djzmAl/results.json`。
- 正式VoiceClient+AudioWorklet+Worker合成输入检查：开始1次、提交1次、84帧音频。远端消息由检查器接收确认，不调用ASR/任务服务。证据：同目录`0.pcm.capture.json`。输入非现场麦克风，真人体验仍待用户试用。
- 最新定点4文件29项、typecheck通过。全量130文件1050项通过。首次高并发全量中一个既有JS权限测试返回interrupted而非权限错误，单文件34项通过；降低测试并发后全量通过，未改该断言。
- 普通HTTP诊断页的Worker被报blob脚本fetch未定义，未确定来源；换到正式扩展页面验证通过。旧原生诊断脚本使用20ms页面定时器喂样本，切后台后未在100秒内完成；改为逐帧回执推动检查，保持样本与断言不变。旧失败不作产品通过证据。
- 依赖：@ricky0123/vad-web 0.0.30 / onnxruntime-web 1.29.0；资产约17MB，随扩展本地提供。上游API参考：https://docs.vad.ricky0123.com/user-guide/api/ 。许可证随构建分发。
- 经验筛选：关键原因、边界和失败均由代码、测试与本记录解释，不新增wiki或提案。

## 交付边界

机器侧检查完成。最终构建加载到日常Chrome后交给用户试用。真人未验收前不勾选包含真人的复合标准；若用户试用无问题则结束，不自动补第二、第三处。

最终版已重载，静态实测结果页已打开：同样本目录`review.html`。未提交推送，等待用户实际麦克风试用反馈。

## 用户最终反馈：体验未通过，停止当前修改

15:51用户表示识别变差、对近期工作不满意，要求全部commit/push后交由GPT 6 Pro指导。机器证据保留，但真人体验不通过。详见[交接与截图](../tasks/20260910-voice-review/handoff-gpt6-pro.md)。不自动补第二、第三处。
