# 任务：Agent能直接读取并等待目标状态，减少重复脚本探查

用户2026-09-10同意继续推进，并明确优先改善浏览器操控环境、向ego-lite学习，再决定是否减少轮次/上下文/推理配置。当前主代理独立完成。

## 完成标准（实现前冻结）
- [ ] 1. 现有read_element能对同一当前目标读取字段/控件/媒体状态，并按明确expected条件返回匹配证据；不依赖模型自行写JS读状态。— 机器：生产模块定点测试、隔离真实Chrome。
- [ ] 2. 需要等待的条件由同一只读调用有界检查，匹配才成功；超时、无此属性、多个目标、旧ref、换文档明确失败，不假称任务完成，不重复写页面。— 机器：固定反例和真实浏览器。
- [ ] 3. 单工具和browser_run使用同一工具契约；Agent得到可发现的参数、结果及恢复说明，工具禁用、共享页只读、页面归属和接管边界保持。— 机器：生产装配与契约测试。
- [ ] 4. 任务登记说明明确操作方法可以在执行前更新同一pending项，观察成功仅代表读取成功；禁止用方法完成状态冒充用户结果。不移除unknown/防重放闸门。— 机器：同结果改方法检查；模型真实路径观察。
- [ ] 5. 保持当前M3与medium配置，在隔离生产侧栏/工具环境跑真实自主任务，记录结果正确性、JS探查数、模型轮次和耗时；先比较旧环境样本，再判断收益。不能用强行指定工具的任务冒充自主使用，也不承诺单例P95。— 机器：真实模型；人：最终日常体验。
- [ ] 6. 定点通过后typecheck/test/build/diff；正式构建按既有授权加载并核对。若真实任务仍慢，明确失败点，不称整体流畅性已解决。— 机器：主代理；人：正式体验。

## 范围与边界
- 本轮是已批准方向中的工具环境改进。沿用Chrome扩展、Pi、现有read_element与browser_run，不替换为ego-lite浏览器，不新增代理，不改音色和UI布局。
- 不先降低推理级别，不设模型轮次硬上限，不盲目截断历史。
- 结果状态与改口/旧播报的完整重构仍需按研究顺序推进；这次工具能力与证据不代称这些问题已全部解决。R3旧unknown恢复反例继续保留。
- 参照ego-lite固定源码5ca3c36c的helpers/driver/locator/waits接口：直接读状态、代码等待、有边界的失败。未复制上游实现。

## 本轮验证与结果

- 实现前3个新状态断言失败，原7项读取检查通过。修复后定点状态/程序检查29项通过。
- 真实模型首次使用新properties/expect，未使用JS探查；同时暴露browser_run子调用登记晚于preflight，正确target也被拒。新增生产组合反例先红后绿，现将子步骤直接同步交给宿主，脱离Pi异步progress队列；没有放宽目标匹配。
- 模型又把paused.equals写为字符串"true"。改为按属性区分工具参数类型，并在浏览器检查前拒绝错误类型，避免无效等待。原失败轨迹保留。
- 最终typecheck、128文件1024项测试、build、diff检查通过。隔离真实Chrome7项通过：AX媒体状态、条件失败、单次调用等待成功、多目标拒绝、等待跨文档失效。
- 自主任务均使用同一M3/default medium，用户任务未指定新工具。旧环境样本10轮/21.762秒/1次JS，页面暂停和三个评论值已观察，但暂停事件计数因脚本读取了错误JS world而未验证，整份baseline不称全绿。此脚本问题已修复。
- 改动中间样本14轮/36.136秒/0次JS，随后样本13轮/104.298秒/0次JS（类型错误及调试器占用）。最终样本9轮/29.561秒/0次JS，暂停23.514秒发生、仅一次暂停、三个实际评论值均在正式回答中；使用了布尔expect。单例可见少一次模型轮次及不再手写JS，但总耗时没有改善证据，更不能称P95通过。

原完成标准1/2/3/4的工具与说明范围、5的真实观察记录、6的工程检查已完成。不是完成/改口/播报全链路重构，也不是日常流畅性验收通过。下一步仍需处理登记流程强制往返与当前要求接续；不会靠降低模型配置掩盖已知系统约束。

原始结果摘要及源码指纹见[结果JSON](20260910-browser-environment-state-results.json)。ego-lite参考版本与文件见同一JSON；公开源码提供helper/runtime，不能称复制了其闭源浏览器内核。未提交推送。

收尾：同一pending结果在执行前更新tool/target的生产模块小检查通过，保留一个结果身份。12:05重载正式扩展，native进程/面板重连，在线background.js与sidepanel.js均与构建SHA-256一致。核对wiki索引后，异步接线教训由回归测试、本记录及开发日志覆盖，不再重复建立经验或规则提案。
