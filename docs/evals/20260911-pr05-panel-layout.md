# 任务: 窄侧栏里仍能看见并点到发送、接管、输入；打字马上出现

用户能观察到：侧栏缩到 320 宽、或窗口很矮、或显示放大时，发送/接管/输入/新会话仍在画面里、没有被挡住、页面不会左右滑。面板里已经有很长一段对话时，键盘打字仍马上出现在输入框。Enter 发送，Escape 关掉菜单。

## 完成标准

- [x] 1. 320/400/520 × 600/900 × 100%/200% 共 12 档：文档不横溢；`#input` `#send-btn` `#takeover-btn` `#conversation-new` 可见且在视口内。 — 谁检查: `npx tsx scripts/eval/panel-layout.mts --headless=new` → `panel-layout-2026-09-11T14-55-37-941Z.json` 12/12
- [x] 2. 先放入 2000 条历史，再测 100 次输入到可见，p95 ≤100ms。 — 谁检查: 同上 p95=34ms（单次最大 231ms，发生在刚插入 2000 条之后）
- [x] 3. 输入框 Enter 发出任务；Escape 关掉会话菜单。 — 谁检查: 同上
- [x] 4. 打开面板仍是新会话；草稿不清空。 — 谁检查: 未改该逻辑
- [ ] 5. 人眼层级/接管好不好懂 — 谁检查: 人（本轮不拦机器项）

## 边界与不做

- 不连日常 Chrome。默认无头。
- 截图只作证据，不是唯一验收。
- 不打正式标签。
