# 任务：截图幕帘 + 新标签页跟随

用户 2026-09-11 指定「2+3 一起」。两件都来自 huashu-chrome 的对照（2026-09-11 审阅）。

## A. 截图幕帘

**目标**：agent 截的图里没有它自己的光标、标注和控制条——否则它会把发光箭头当成页面元素去理解甚至去点。

- [x] A1. `Page.captureScreenshot` 与 `captureVisibleTab` 两条捕获路径前隐藏全部产品画布，拍完恢复；`finally` 保证异常时也放下幕帘。 — 谁检查: 代码 + `npm run build`
- [x] A2. 幕帘按属性选择（`data-sideagent-overlay`）覆盖光标 / 标注 / 控制条三类 host，不依赖 cursor 脚本是否注入。 — 谁检查: 代码
- [x] A3. 隐藏后等两帧再拍（合成器要用上新样式），恢复不等待。 — 谁检查: 代码
- [x] A4. 幕帘失败（页面禁止注入、导航换文档）不弄失败截图本身。 — 谁检查: 代码（全路径 try/catch）
- [ ] A5. 真实页面上截图里看不到光标/标注 — 谁检查: 人（本轮未跑，见"未跑"）

## B. 新标签页跟随

**目标**：点击开出新标签页时，回执写明新页并把它变成工作页——不跟的话 agent 会对着"什么都没变"的原页反复重试。

- [x] B1. 点击前记录标签集合，点击后只认「新增 + `openerTabId` 是本次点击的页 + id 大于点击前最大 id」的新页。 — 谁检查: 代码
- [x] B2. 跟随走产品既有的 `switch_tab`（归属校验/借入规则照旧）；校验失败只报告不抢。 — 谁检查: 代码
- [x] B3. 回执带 `newTab:{tabId,url?}`，模型可见文案写明「新页已是你当前工作页，先观察再继续」。 — 谁检查: 代码 + `npm test`（既有 click 回执用例）
- [x] B4. 拿不到 `chrome.tabs`（测试 stub、受限页面）时静默跳过跟随，不影响点击。 — 谁检查: `extension/test/click-integrity.test.ts` 21 项（首版因同步访问 `chrome.tabs.query` 抛错，已改为整体 try/catch）
- [ ] B5. 真实页面上点 `target="_blank"` 链接：回执出现新页、工作页切过去 — 谁检查: 人（本轮未跑，验收 fixture 没有新页链接）

## 结果

- `npm test` 146 文件 1261 项、typecheck 0 错误、build 通过。
- 两个改动都是「有则生效、失败不影响主流程」的附加能力，没有改动执行事实与控制闸门。

## 未跑 / 未决

- A5、B5 都没有真实样本：验收 fixture 不带 `target="_blank"` 链接，本轮也没跑截图场景。两条都只有代码与既有回归证据，不据此宣布可用。
- 幕帘对 `captureVisibleTab` 路径同样生效，但该路径只在 CDP 不可用时走，本轮未构造该场景。
- 本轮未提交、未推送、未重载扩展。
