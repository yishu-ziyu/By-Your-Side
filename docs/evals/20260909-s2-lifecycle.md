# 任务：执行中改对象、暂停后补充，继续完成剩余步骤且不重复写页面

2026-09-09，由接手主代理在产品实现前冻结。用户已授权S2；S1人工检查状态不改写。

## 完成标准
- [ ] S2-01. 多步任务登记观察和操作两项结果；只有观察回执时操作仍未完成。助手说完成、agent_end、正式交付、播放完成都不能补足操作证据；失败保留受阻项。— 谁检查：主代理独立 `npx vitest run agent/test/harness-s2-lifecycle-evaluator.test.ts`，隔离浏览器真实结果。
- [ ] S2-02. 运行中把X纠正成Y，原run与原约束保留；重新核对Y，纠正被接受后尚未执行的旧X标注不得落地，最终只标Y。不要求撤销接受纠正前已完成的写操作。— 谁检查：独立契约测试及真实模型/浏览器纠正路径；人检查自然纠正。
- [ ] S2-03. 导航或文档替换后旧目标引用失效；必须重新观察后操作或明确受阻，不得误用旧ref写到新页面。— 谁检查：独立失效测试及隔离浏览器导航路径。
- [ ] S2-04. 运行中补充、暂停中补充并恢复均保留原run、原话、约束与已完成结果；暂停确认后到明确恢复之前页面写入为0；恢复后只执行剩余操作。— 谁检查：独立manager/session/control测试及真实模型/浏览器暂停补充恢复路径；人检查操作体验。
- [ ] S2-05. 取消后的一般能力问句不恢复旧操作；明确新委托可建立新run；旧事件、旧回执不得污染新run。— 谁检查：独立测试与真实浏览器取消后问能力路径。
- [ ] S2-06. 重复请求、重复交付、播放完成和进程恢复不重放已发生写入；持久回执unknown只查询或请求决策，写入计数不增加。— 谁检查：持久化故障注入与旧恢复回归。
- [ ] S2-07. 消息已显示而操作证据缺失、迟到或失败时，任务结果仍未验证；合法结果证据可推进已登记结果，不以永远false替代结果判定。不同run、对象/文档和调用的证据不能串用。— 谁检查：独立契约测试。
- [ ] S2-08. 主代理独立运行 `npm run typecheck`、`npm test`、`npm run build`、`git diff --check` 全部通过；重跑受影响S1正常圈画、禁用写能力及旧控制/恢复测试。真实S2入口 `npx tsx scripts/acceptance/harness-s2-run.mts` 覆盖纠正、导航、暂停补充恢复、取消后问能力，保存每次失败、源码hash、真实回执与DOM结果。— 谁检查：主代理。
- [ ] S2-09. 用户检查对象纠正、暂停恢复后裁决体验；机器结果不代称真人已加载扩展验收。— 谁检查：人。

## 判定与实现边界
- 承接H2/H4完整生命周期；语义登记使用现有执行会话与工具元数据，不为圈画词语增加voice关键词分流。不宣称任意自然语言意图已被完备提取。
- 复用Pi 0.84.4 prompt/steer/followUp、工具结果与context/session钩子；复用现有TaskProgress、TaskReceipt、执行闸门。结果登记必须有目标与执行证据，不能照搬todo.toggle或文本DONE即完成。
- 主代理拥有本标准和独立评估器。worker不得改其断言。先保存当前失败，再实现；已通过的既有防线按回归记录，不强求每条先红。
- 真实路径使用隔离Chrome、生产侧栏/执行器、真实M3及必要的合成麦克风/CDP桥接；明确其与用户扩展/native传输的差别。
- 保留全部未提交改动；不重载用户扩展、不提交推送、不进入TTS、不改主代理provider。Broker显式选路由，暂不使用DeepSeek。

## 实现前补齐的独立结果评估入口

`npx vitest run agent/test/harness-s2-results-evaluator.test.ts`：8条登记结果断言于实现前创建，首跑8红，日志`/tmp/ego-s2-results-red.log`。登记意图不接受模型直接设置完成；完整工具回执使`resultState`到达`satisfied`，保留证据来源。既有`successVerified:false`继续表示不宣称独立环境核验，不以它代替新增的结果判定。

首组生命周期红测日志`/tmp/ego-s2-red.log`：4失败/2通过。后续只按已冻结用户结果修复，不修改断言迁就产品。


## 收尾结果（2026-09-09）

机器检查完成，人工体验尚未验收。主代理最后独立检查：124个测试文件、989项测试、typecheck、build、diff检查通过。最后的交还观察修复另有39项session定点回归。

| 结果 | 证据与状态 |
|---|---|
| 多步登记、消息与执行结果分离 | 独立results/report评估器通过；真实文字纠正11项通过，观察后标注仍待办，正确执行后才满足 |
| 文字/语音运行中纠正 | 文字m1Cn6l、语音G7tgDw通过，实际只圈Y；保留原任务；旧事件隔离与旧程序写入失效独立测试通过 |
| 导航后旧目标 | u0W90J通过，旧JS/mark在文档检查被拒，重新读页后报告目标缺失；原akCb04曾圈错再清除，保留为失败 |
| 暂停中补充再恢复 | 1blklj 16项通过；暂停零DOM变动、控制闸门拒写、原run保留、交还真实快照入账、恢复只圈Y、结果同步满足 |
| 取消后问能力 | h12JvP通过，任务仍取消，没有页面写入 |
| 重复交付/播放/恢复 | 真实重复原请求和播放不产生新浏览器调用；已完成mark在RPC前拒绝；Pi实际会话文件重开后confirmed/unknown均不重放；晚到的匹配回执可以解开unknown |
| S1受影响回归 | disabled 9项、正常JS 11项、保留措辞圈画11项通过，手绘抖动/锚定/清除保留 |
| 人工 | 仍待用户在实际扩展检查自然改口与暂停恢复，不改写成已通过 |

原整组5场景为3通过/2超时，后续按失败原因定点修复，阶段与hash全部保留。最后源码hash `67fe51e3ce1ec3689d01226f853e478c19efb9162c4ae50d6d4bcf3443a855e0`。文字纠正与暂停恢复在最终源码验证；其他未受最后交还接线影响的场景沿用对应阶段证据，未重新宣称最终源码整套5/5。

详见[结果与失败台账](20260909-harness-s2-results.json)。[可点击行为演示](../previews/show-me-s2.html)仅用于解释，不是新UI方案。用户认为演示抽象，下一步转实际扩展试用，不继续打磨HTML。

本轮复用Pi工具与会话custom entry，没有新增依赖或换provider。结果登记带来额外内部步骤；没有验证提速。S2人工判断前停下，不进入TTS、不重载扩展、不提交推送。全部既有未提交改动保留。

## 用户追加授权：进入真实试用

用户明确要求每次产品改动后负责重载并确认生效，覆盖此前交接中的不重载限制。已重载ChromeMain内的扩展，Debugger读取的实际background脚本hash与构建相同，native host为新进程。真实试用页http://127.0.0.1:61344/已准备。先验“执行中改成标准模型”一条，用户只判断是否自然、是否圈对、是否等得住；主代理负责后续步骤，不把总体安排交回用户。人工结果待收集。
