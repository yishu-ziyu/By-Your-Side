# 任务：开发规范保留结果约束，减少重复确认、记录和强制流程

## 授权与范围

用户在审阅本轮规范复盘后明确要求“按照你的建议来修改”。本次修改项目 AGENTS.md、按需读取的收尾流程及必要文档接线；局部修正状态入口中已被最新反馈取代的描述。工作区为 `/Users/mahaoxuan/Desktop/ego`，main，开始时无未提交改动。

## 完成标准

- [x] 1. 请求目标和范围明确时说明理解后推进；仅在产品取舍、实质性范围变化或必要授权缺口时确认。保留当前要求优先、实际使用入口、主代理独立开发与既有授权边界。— 主代理逐条审阅
- [x] 2. 实现前确认问题及检查方法；允许有证据地修正错误测量或测试并保留反例、理由，禁止降低用户要求或掩盖失败；改变成功定义仍由用户裁决。“太慢”等反馈先诊断，只有影响改动方向的歧义才追问。— 主代理逐条审阅
- [x] 3. STATUS 是当前进度唯一入口；NOTES 按关键节点更新，开发日志按重要决定触发。修正人声检测“待试用”与最新失败裁决冲突的状态描述，保留原始验收和失败证据。— 主代理检查规范与状态入口
- [x] 4. 删除三角色解释、每日写日志及指定文风格式、通用自动归档保证；设计参考与预览形式按实际判断需要选择。保留真实路径验收、适量验证和用户对主观设计的裁决。— 主代理逐条审阅
- [x] 5. 收尾细节移到单一按需读取页面，AGENTS 保留触发条件、证据边界和入口；已有经验、提案及状态不被改判。— 主代理检查流程与引用
- [x] 6. 修改文件的本地 Markdown 链接与标题锚点有效，最终 diff 无空白错误且仅涉及授权文档。— 本地链接检查、`git diff --check`、diff 审阅

## 边界与不做

- 不修改全局规范、产品代码、宿主配置或 hooks，不重载扩展、不提交推送。
- 不批量迁移历史 NOTES、验收或日志；不把已授权的规范修改再变成待审提案。
- 纯文档修改，不运行产品测试、类型检查或构建。文档验证只能证明规则及引用已更新，不能证明未来任务必然遵守或产品体验已经改善。

## 验收结果

- 逐条审阅通过：请求明确时连续推进，成功定义由用户裁决，测量纠错须保留依据；真实路径、证据边界和主代理独立开发保留。核对项目检查命令与 package.json 一致。
- 收尾细节移至 `.kimi-code/wiki/closeout.md`，索引增加入口；验收模板保留，未修改既有 pattern、提案或其裁决状态。旧 METHODOLOGY 标为历史说明，避免其模板覆盖新规则。
- STATUS 将人声检测记为体验未通过、连续语音记为整体验收未通过；NOTES 更新维护说明并链接状态入口。原始验收、交接、失败证据和历史技术记录保留。
- 本地 Markdown 链接、标题锚点及 `git diff --check` 检查通过；最终 diff 仅涉及本轮规范和必要文档。未运行产品测试、类型检查或构建，未重载、提交或推送。
- 经验筛选：本次理由和边界已由现行规范、原验收与本记录解释，不新增 pattern 或待审提案。此次检查不证明未来执行效果，仍需后续实际任务观察。
