# P5：指定会话、连续纠正与复合指令有明确归属和执行顺序

## 完成标准（实现前冻结）
- [ ] 说出唯一会话名称后只调度那个会话，原语音仍归开启它的会话；目标原话和回执在来源/目标都能重载。— 双会话协议/磁盘/Chrome
- [ ] 重名、不存在、只有“另一个”的目标先澄清，零任务写入；目标在讲话/分类期间被替换则拒绝旧操作。— 身份/目标解析测试
- [ ] 未明确“用当前页面/所选资料”时不把来源会话的页面或附件带给目标会话。— 上下文隔离测试
- [ ] 运行中新建无关任务需明确答复才另开会话；否认、超时、换语音连接、改说别的不会触发旧待确认任务。— 澄清状态测试
- [ ] 连续“800→600”最终采用600；尚未执行的旧轮作废，已经接收的指令按序保留，不能假装撤回。— 竞态/真实页面
- [ ] “暂停→改600→等我继续”和“改600→继续”按序逐项回执；中途失败不执行后续项；部分完成如实呈现。— broker/真实页面
- [ ] 48句固定分类集仍三轮通过；P0/P2真实路径回归；typecheck、test、build、diff通过。

## 边界
- 不自动切换侧栏或让声音串会话；语音显式目标由应用解析真实会话，不接受模型编造ID。
- 不新增定时调度或后台常驻麦克风。真人耳麦验收保留。


## 2026-09-09 验收补记

目标目录冻结、任务身份复核、来源资料隔离、双归属回执和另开确认已实现，相关管理器测试通过。真实“暂停指定会话→保存600→等我继续”通过并保持页面稳定。

下一句“改800再600然后继续”只生成steer，页面没有恢复，整轮失败。已增加不允许吞掉显式resume的结构检查和分类修复，尚未真实复验。ASR同音词、名称写法变化和分类超时的失败保留在台账。双侧重载、三轮路径和分类门槛仍未关闭。

原始条件保持不变。检查范围、失败和可保存证据见[验收台账](20260909-voice-dispatch-results.md)及[结果摘要](20260909-voice-dispatch-evidence.json)。未勾选表示整条要求尚未全部满足，不表示没有做过任何实现。

推送前协议补验：来源侧回执曾因外层conversationId校验被拒；新增测试复现后允许目标或显式originConversationId接收，无关会话仍拒绝。实际双侧重载仍待跑，不提前勾选该项。
