# 任务：没识别出语音时继续聆听，不误报为操作意图失败

## 完成标准
- [x] 空/空白最终转写不调用意图模型、不发送任务、不生成回答、不显示空“你：”。— 单测
- [x] 空输入提示“没听清，请再说一次”并恢复聆听，同一连接下一句有效语音仍可处理。— 单测与真实上游
- [x] 后台日志区分输入提交、转写长度、路由结果/失败和服务错误，记录时间与轮次；不记原始录音、转写内容或Key。— 日志检查
- [ ] 分类失败仍不执行任何操作，提示不能混同空输入。— 单测
- [x] typecheck、相关测试、build、diff检查通过并重载。— 机器

## 当前证据
用户两张截图均显示空“你：”和通用意图错误。现有agent.log/wrapper-err.log无逐轮语音记录，不能还原这两次具体的输入和失败原因。代码的空转写路径能够解释该现象，需要聚焦复现与修复。


## 验证记录
- 聚焦测试先失败：空字符串确实调用了route。修复后空输入不会进入分类/任务通道，不生成空转写气泡，保持就绪。
- 真实Step先输入静音得到空ASR，再输入本机合成“你好，十加七等于几”，同一连接回答17。证据 `20260908-voice-empty-recovery.json`。这证明恢复路径，不证明两次历史失败的采音成因。
- 新日志记录voiceId/会话ID/轮次、PCM字节数/RMS/峰值、转写字数、route阶段/耗时/结果、分类失败类别。原音频、原转写、Key均不记录。
- 已通过78文件/657测试、类型检查、build、diff检查。

- ChromeMain扩展重载成功，新诊断日志从下一次语音开始生效。
