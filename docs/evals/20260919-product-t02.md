# 任务: T02 由真实状态生成统一任务视图

验收标准版本：docs/tasks/product-experience-v1 v1.0（A02-01—A02-08）。责任 Agent：主代理（本会话）。基线：fb46d7b（含 T01 合入），分支 t02/task-view，工作树 ../bys-worktrees/t02-task-view。

## 完成标准

- [x] 1. 只读投影模块 `shared/task-view.ts`：视图含任务身份（conversationId/runId/controlVersion）、目标原文与已接受修订、作用页面绑定（tabId+urlHash，不跟随当前 tab）、当前活动、等待/阻塞原因（直接取自 state/nextStep.reason，不合并成含糊状态）、成果引用、未完成项、数据时间；无「业务成功」字段 — 谁检查: agent/test/task-view.test.ts
- [x] 2. A02-01 同一事实序列实时 observe 与 restoreResults 重放投影一致（固定时钟下逐字段相等）— 单测
- [x] 3. A02-02 A 页任务投影视图不含 B 页/B 会话事实 — 单测
- [x] 4. A02-03 取消后旧 running/tool_end 不复活任务 — 单测
- [x] 5. A02-04 running/接管/中断/错误四态与真实下一步一致，无一伪装成完成 — 单测
- [x] 6. A02-05 工具成功但义务未尽时 outstanding 保留、无成功表述 — 单测
- [x] 7. A02-06 回执 unknown/缺核验 → waiting 明确未知，无百分比/成功说明 — 单测
- [x] 8. A02-07 旧快照缺字段兼容读取、损坏字段明确失败（isTaskView 拒绝）— 单测
- [x] 9. A02-08 重连后 replayState 重发视图；投影纯函数无副作用、不触发模型调用 — 单测 + manager 级测试
- [x] 10. 协议：`task_view` 消息进出有结构校验；旧快照/旧回执读取兼容 — 单测 + npm run typecheck + check:architecture
- [x] 11. 真实扩展路径：A 任务运行→切 B→回 A→接管→交还，面板收到 task_view 且与状态一致 — 受控真实扩展脚本证据 + 人看截图

## 边界与不做

- 不做界面重设计（T03 消费本视图的 UI 由 T03 负责，本票只把视图送上发送/重放路径）
- 不新增语义判断模型；不自动拆解自然语言为结构化任务
- 不改恢复成功定义；successVerified 语义不动

## 交付记录（READY_FOR_REVIEW）

结束 commit：本工作树最新提交。修改文件：shared/task-view.ts（新）、shared/protocol.ts（task_view 消息 + 校验）、agent/src/conversation-manager.ts（TaskProgress 突变方法的通知包裹 + replayState 重发）、agent/test/task-view.test.ts（新）、agent/test/conversation-manager.test.ts（一处计数断言过滤 task_view，原意图保留）、本文档。

实测证据：
- 单测：task-view.test.ts 13 项（A02-01—08 + 协议校验 + manager 集成），全量 217 文件 2125 项通过；typecheck 0；check:architecture 191 文件；build 通过；npm run check 退出 0。
- 真实扩展路径：用 T01 运行器在本工作树跑 A03（真实面板、真实模型 MiniMax-M3、真实接管/交还）：task_view 消息 179 条，状态序列 none→running→paused(human_control)→idle 与真实事件一致；页面绑定 tabId 正确；接管窗口内无 running 伪装。证据：eval/runs/journeys-case-A03-*/（本工作树）。
- 模型调用：0 次新增（投影为纯函数，manager 集成测试用桩运行时）；真实扩展那次运行使用的是 T01 已授权的验收调用。

仍需 reviewer 注意：
- task_view 是新增下行流量（微任务合并 + 内容去重后幂等），面板旧版本收到会因校验/未知类型而忽略——协议校验已加，旧面板无 task_view 分支时默认忽略，不回退。
- 每事件一次的 snapshot+投影开销与现有 per-message snapshot 同量级；未做性能采样（T03 的 50 次时序在 T03 票）。
- A02-08 的「修改后重连再查」由 replayState 强制重发覆盖；T03/T05 消费时才验证真实面板渲染。

裁决：READY_FOR_REVIEW（终裁留 ChatGPT，降级安排见 CLAIMS.md）。
