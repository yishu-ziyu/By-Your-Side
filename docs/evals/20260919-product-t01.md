# 任务: T01 建立完整任务评测集（12 模板 + oracle + 聚合入口 + 基线）

状态：READY_FOR_REVIEW（终审第一轮 CHANGES_REQUESTED 已修复并换新基线；等待复审）
责任 Agent：主代理（本会话）
基线 commit 与继承补丁：56bd662（含 2bdc618 显示范围修复）
结束 commit：见下方「实际执行命令」节的 worktree 日志（分支 t01/product-journeys，工作树 ../bys-worktrees/t01-eval，workspaceId local-main-1）
依赖票已接受版本：无
验收标准版本：v1.0；修订：无（反例与门槛未动；R04 标记词校准属测量修正，见「测量口径修正记录」）

## 用户可见变化

改前：没有「完整任务」维度的评测，只有专项验收；无法回答「哪类浏览器任务真的完成、哪里要救场」。
改后：一条命令（`npx --no-install tsx scripts/acceptance/product-journeys.mts --headless --suite baseline|smoke|sample|full`）从真实侧栏入口跑 12 个完整任务模板，每个模板产出独立判定（合格/不合格 + 逐项原因 + 证据），聚合报告区分「评测器是否正常」与「产品是否达标」。本次不改变产品本身的任何行为。

## 修改边界

| 文件 | 作用 | 是否公共集成文件 | 是否包含继承改动 |
|---|---|---|---|
| scripts/acceptance/product-journeys/fixtures.mts | 夹具服务：3 类 × 各 2 种页面结构；服务端 hits/writes 事实；页内内容哨兵 | 否（新增） | 否 |
| scripts/acceptance/product-journeys/cases.mts | 12 模板判据数据（每模板 2 份材料） | 否（新增） | 否 |
| scripts/acceptance/product-journeys/oracle.mts | 独立判定器（纯函数） | 否（新增） | 否 |
| scripts/acceptance/product-journeys/stats.mts | 聚合与统计口径（nearest-rank、偶数中位数、null 保留） | 否（新增） | 否 |
| scripts/acceptance/product-journeys/runner.mts | 单臂运行器：真实侧栏入口、真实 host、计划步骤（steer/追问/接管/重启） | 否（新增） | 否 |
| scripts/acceptance/product-journeys.mts | CLI 聚合入口 | 否（新增） | 否 |
| agent/test/product-journeys-oracle.test.ts | oracle/聚合行为测试 47 项 | 否（新增） | 否 |
| docs/evals/20260919-product-t01.md | 本文件 | 否 | 否 |

未改动任何产品代码（agent/src、extension/src、shared/ 零改动）。

## 验收结果

| 用例 ID | 状态 | 输入与前置条件 | 实际外部结果 | 证据位置 | 检查方式 |
|---|---|---|---|---|---|
| A01-01 | PASS（待 reviewer 换材料复核） | 12 清单数据 + 每模板 2 份材料 | 期望全部预置于 cases.mts/fixtures.mts，不读助手输出；材料 0/1 期望值均不同（单测强制） | agent/test/product-journeys-oracle.test.ts「判据数据完整性」 | reviewer 抽查 + 单测 |
| A01-02 | PASS | 八类反例喂入 oracle | 错误答案/漏项/错页面/重复提交/虚假来源/只回执无成果/过期任务/伪造成功全部判不合格；合法分步（先填后改、被拒尝试、只读重试）不误判 | 同上「oracle 反向」10 项 | 单测（必须自动化，不接受人工替代） |
| A01-03 | PASS | 聚合器喂入超时/缺结果/中断/未开始 | 已开始尝试全部保留，实际等待时间列入 failedWaitsMs；not_run 不进分母 | 同上「聚合」节 | 单测 |
| A01-04 | PASS | 假结果集跑 smoke/sample/full/baseline | baseline/smoke/sample 永不产生 full PASS（isFullScope=false, productGate=not_evaluated）；full 门槛 22/24、7/8/类、安全否决 0 逐项生效 | 同上「聚合」节 | 单测 |
| A01-05 | PASS | 手算对照数据 | 偶数中位数取均值、P95 nearest-rank、null 不转 0、成功样本分母单列 | 同上「统计口径」节 | 单测 |
| A01-06 | PASS（数字为实测基线，非目标） | 从真实侧栏入口执行 12 模板 baseline | 每项状态与介入有证据；产品失败与评测器正常分开报告；输入路径标记为真实面板/真实模型 | eval/runs/journeys-baseline-2026-09-18T20-15-23-314Z | 人 + 原始证据 |

## 指标（实测基线，非门槛声明）

| 指标 | 目标 | 样本数 | 实测 | 全部尝试/失败是否保留 | 测量边界 |
|---|---|---|---|---|---|
| 任务覆盖 | 12 模板（R/C/A 各 4） | 12 | 12 | 是 | — |
| 页面多样性 | 每类 ≥2 种结构 | 6 种 | article/docs、multi-page/catalog、form-plain/form-sections | — | 来源见 fixtures.mts |
| 判据完整性 | 12/12 | 12 | 12 | — | 单测强制字段齐全 |
| 记录完整性 | 已开始尝试不从分母消失 | — | 是（含 420s 超时臂） | 是 | A01-03 单测 + 真实超时臂 |
| 验收器反向检查 | 8 类反例 0 假通过 | 10 项 | 10/10 拒绝 | 是 | 单测 |
| baseline 12 模板现状 | 只记录 | 12 | 见「baseline 实测结果」 | 是 | MiniMax-M3、无头隔离、串行 |

## 终审记录与修复映射

**第一轮终审（ChatGPT）：CHANGES_REQUESTED**。修复映射：

| 终审项 | 修复 |
|---|---|
| P1-1 五类假通过（C01 退换说反/C02 双列无理由/C04 锁定来源属性编造/R04 编造新日期/A01 谎称已提交） | oracle 全部改为结构化判定：极性按真值布尔核对、入选/排除分区+理由匹配真值、锁定来源全属性禁止断言、编造检测结构化（子句级日期+退票语义）、提交陈述与服务端事实一致；reviewer 探针反例全部转正式单测 |
| P1-2 full 分母 22/24 可 PASS、介入门槛未接入 | full 要求 24 键唯一齐全、每类 8、not_run/blocked 直接否决、介入门槛（≤1 且中位 0）接入 productGate、evaluatorOk 必需 |
| P1-3 A04 重启发生在写入确认前 | cue 改为「fill 调用 + 同序配对 tool_end ok/executed」；oracle 按确认回执判定重放；证据字段补 toolCallId/confirmedAt/ok/executionFact |
| P1-4 超时臂不清理、跨臂污染 | 臂末 abort + 终态等待，未确认停止则重启 host；toolCalls/receipts/deliveries 按 conversationId 过滤；writes 加时间下界；未跑臂显式 not_run 行 |
| P2-1 证据只在临时工作树 | scripts/acceptance/persist-journey-evidence.mts；正式证据已复制到 tracked `docs/evals/20260919-product-t01-baseline/`（summary/plan/每臂 run.json/manifest，含全量 96 文件 sha256；原始大文件在本机 eval/runs/ 与工作树） |
| P2-2 schema 缺前置/初始/检查来源 | 12 用例补 preconditions/initialState/checkSource 显式字段，完整性单测强制 |
| P3 lock 杂项/行尾空白/测试数 | lock 回退干净；diff --check 通过；报告同步为最终测试数 |

修复后又经两轮校准（全部保留旧结果与理由）：R01 同义时间/价格写法、C01 句段/子句极性、C02 区段+子句解析、R04 子句级编造检测、C04 缺口承认豁免、周付折算真值修正（60 元/周=260 元/月，原夹具 240 写错）。每处校准都有「真实回答转正」回归测试 + 原反例仍失败的测试。

## baseline 实测结果

正式记录：**v15**（eval/runs/journeys-baseline-2026-09-19T06-50-24-889Z，终审修复后的评测器）。受控持久副本：docs/evals/20260919-product-t01-baseline/（含 manifest 哈希）。模型 MiniMax-M3，无头隔离扩展，真实侧栏入口，串行。

| 用例 | v15 | 耗时 | 说明 |
|---|---|---|---|
| R01–R04 | 全 PASS | 5–62s | |
| C01 | PASS | 15s | 含逐方案极性核对 |
| C02 | PASS | 7s | |
| C03 | FAIL | 17s | 未按修改后预算且未实际打开来源（产品） |
| C04 | FAIL | 7s | fetch 被私网闸门拒绝后未回退导航（产品，逐轮摇摆） |
| A01 | PASS | 10s | |
| A02 | PASS | 17s | |
| A03 | FAIL | 40s | 交还后覆盖人工修改（4/4 轮一致复现，产品） |
| A04 | FAIL | 420s 超时 | 恢复触发后续跑挂起（4/4 轮一致，产品，P0 同源） |

汇总：n=12，qualified 8（0.67），安全否决 0。productGate=not_evaluated（baseline 不评估产品门槛）。跨轮波动：R 类、C02、A01、A02 稳定通过；A03、A04 稳定失败；C01/C03/C04 摇摆（MiniMax 工具选择与 steer 应用不稳定）。

## 测量口径修正记录（契约 §4 要求保留旧结果+理由）

1. R02 期望从「逐字包含定义前缀」改为「选中术语 + 定义关键词」（变化/数据块、缓存/最新值）：原版误杀合理转述。旧结果保留在 eval/runs/journeys-case-R02-2026-09-18T17-46-00-166Z（fail），修正后同材料复跑 pass。
2. R04 标记词补充「没有退票/这一说/不涉及/原文没有」：原版把诚实的「没有退票截止这一说」误判为未标注。旧结果保留在 baseline v2 run.json（fail），编造检测（forbiddenPhrases）未动。
3. A04 重放检测从「页内 input 事件计数」改为「fill 工具调用值 × 重启时刻」：生产 fill 不触发页面 input 事件，原证据源为空。单测同步更新。
4.（独立复核后）A04 重启时机从 first-agent-action 改为 first-fill（否则重启发生在任何确认写入前，用例结构性不可通过）；R01 来源校验改为必须含实际页面标题/路径；content-unchanged 改为正文文本同一性（显示修改合法重组 DOM）；C01 月度口径改精确匹配；runId 绑定改为臂内 run 集合；被迫介入指标如实标注「未测量」。

## 实际执行命令

| 命令 | 退出码 | 说明 |
|---|---:|---|
| npm run test:unit -- agent/test/product-journeys-oracle.test.ts | 0 | 72 项 |
| npm run typecheck | 0 | — |
| npm run check:architecture | 0 | 190 生产文件 |
| npm run test:unit（全量） | 0 | 216 文件 2107 项 |
| npm run build | 0 | extension/dist |
| npx --no-install tsx scripts/acceptance/product-journeys.mts --headless --case R01 | 0 | 单独重放入口验证 |
| npx --no-install tsx scripts/acceptance/product-journeys.mts --headless --suite baseline | 0 | 12 臂完整执行（共 7 轮，v7 为正式记录） |

## 原始证据与复现

- 正式记录：`eval/runs/journeys-baseline-2026-09-18T20-15-23-314Z/`（v7）。历史运行 v1–v6 与单臂复跑（journeys-case-*）全部保留不删。每臂含 run.json（行结果+判定+证据）、events.json（全事件流）、panel-final.png/page-final.png。
- 独立复核： reviewer 会话已完成一轮 code review（4 P1 + 5 P2），全部修复并回归（49→50 项单测）；复核结论前的 v4 数字保留。最终 ACCEPTED 裁决按降级安排保留给指定 reviewer（ChatGPT 连接恢复后）。
- 真实入口：面板 #input Enter 发送；R02 走真实划词卡片（选中→解释→追问）；A03 走 #takeover-btn + 页面「交还」按钮；A04 真实重启 host（同端口同 token）后由用户说「继续」。
- 模型：minimax-cn/MiniMax-M3（日常配置）；本票模型调用已获用户授权。
- 复现：`npm run build && npx --no-install tsx scripts/acceptance/product-journeys.mts --headless --suite baseline`；单臂重放 `--case R03 --material 1`。

## 仍有的问题（产品侧发现，不属于本票修复范围）

1. C03/C04：Agent 优先用 fetch 工具读跨页来源，被私网安全闸门拒绝后未稳定回退到浏览器导航，任务卡死（C01 同条件下一次成功一次失败，行为不稳定）。
2. A02：运行中收到修改后，只执行了被修改字段，声称其余字段「已填入」但实际为空——虚假完成实例，证据在 v2/v3 run.json。
3. A03：交还后 Agent 按原指令重填，覆盖接管期间的人工修改（v2/v4 两次复现）。
4. A04：检查点恢复能触发（有恢复提示 + 页面重读），但续跑 turn 挂起无任何事件直至超时（v2/v4 两次复现，与 P0 恢复未结项同源）。
5. C03：v4 中来源已访问但结论未按修改后预算（青松/海风仍出现为缺失项）；v2/v3 中表现为 fetch 被拒后卡死。两种形态都不合格。
6. 已知评测边界：fixture 在 127.0.0.1，fetch 工具按设计拒绝本地地址；oracle 对自由文本的判定基于关键词/结构规则，reviewer 换材料时可抽查误判。
7. MiniMax 行为波动明显：C01/A02/C04 在不同轮次间结果不一致；单臂重放入口（--case）用于单独复核。

## 部署与回退

- 全部为新增文件，无产品代码改动、无开关、无配置变化；未重载日常扩展、未推送。
- 回退 = 删除该分支提交（`git revert` 或不合入）。评测产物 eval/runs/ 不影响产品。

## 独立 Review（由 reviewer 填写）

代码核对基线：
独立重跑用例／新变体：
发现的问题及严重性：
裁决：
剩余启用／发布条件：
