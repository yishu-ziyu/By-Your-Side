# P1：文字和语音共用有身份、可查询、不会重复执行的修改通路

## 完成标准（实现前冻结）
- [x] 每个实际任务有独立runId；同毫秒启动的新任务不复用身份；暂停／恢复保留原任务身份，重新启动生成新身份。— TaskProgress测试
- [x] 文字和语音修改共用执行入口，并携带requestId、expectedRunId及原话；两者实际调用同一Pi steer。过期任务、非运行状态拒绝，不退化为新任务。— manager/协议测试
- [x] 同一会话requestId并发、完成后或重启后重放均不重复执行；同ID不同内容拒绝；不同会话相同ID互不覆盖。— 执行次数断言和真实磁盘重载测试
- [x] 执行前持久写入pending，执行器接收后写accepted；持久化失败不先执行；崩溃窗口pending重载为unknown，不能重做。— 故障注入测试
- [x] 侧栏可查询或重载回执，显示原话、目标和送达/拒绝/未知状态；重放不产生重复记录。— 协议、历史记录和浏览器检查
- [x] 新语音轮／关闭语音使尚未执行的旧判定失效；已接收的请求仍可查，不把“取消播报”当成撤销已送达操作。— 竞态测试
- [x] P0成对真实Chrome页面断言继续通过；合成音频不替代真人听感。— scripts/acceptance/voice-steer-run.mts
- [x] npm run typecheck、npm test、npm run build、git diff --check通过。— 机器

## 边界
- 不修改P0判断标准；原legacy消息兼容留在适配层，新侧栏和语音走正式动作请求。
- 回执accepted只表示执行器接收，不表示网页任务完成。
- 回执保存在本机私有目录，暂不自动清理，避免清理后旧请求重新执行。普通诊断日志不保存原话或附件内容。
- P2–P5另立完成标准，用户已授权继续无需真人测试的部分，不因P1完成结束整体工作。

## 自动验收记录
- 81文件667项测试、typecheck、build、diff检查通过。磁盘重载、损坏记录、写盘失败、并发重放及原话冲突均有执行次数断言。
- 真实文字／Step原生合成音频对照：`/tmp/ego-voice-p0-2026-09-08T14-48-54-962Z/`，两路均通过P0的预算、结果列表、排序及单次启动断言。
- 安装中原生服务／侧栏回执查询与刷新：`/tmp/ego-receipt-ui-1788879284365/result.json`，原话和拒绝原因可见，查询两次与刷新后仍一条；截图已查看。
- 本阶段没有真人录音。回执accepted仍不代表网页任务完成；后续P2–P5自动工作继续。
