# 任务: 查清 Step Plan 全双工连接契约及其与 By Your Side 的接合方式

## 完成标准
- [x] 1. 用当前官方文档核对 Step Plan 专用端点、模型、鉴权、音频格式、会话初始化和基本收发事件，给出可复查来源。— 谁检查: 主代理对照一手文档
- [x] 2. 核对工具调用、插话与取消、上下文、音色及会话限制；模型专属能力、通用协议能力和待实测项分开。— 谁检查: 主代理对照模型页、API、开发指南与官方示例
- [x] 3. 检查现有音频探针与本产品控制/任务链，提出最小接法并解释用户可见变化；识别两套上下文、异步结果与控制权之间的关键竞态。— 谁检查: 主代理定点读代码
- [x] 4. 留下 Key 到位后的具体探针顺序、成功条件与失败分支；不使用未提供的凭据、不把历史证据或模拟结果写成当前服务已通过。— 谁检查: 主代理检查研究交付

## 边界与不做
- 用户已确认供应商为阶跃星辰，入口为 Step Plan。本轮只做调研与接入方案，不重新选择供应商。
- 初始阶段用户将稍后提供 Key，不查找或调用本机已有密钥。用户随后已提供本次 Key：只在探针进程内存中使用，不写入源码、文件、命令行参数或日志；允许下列与调研直接相关的真实服务探针。
- 不实现产品语音功能，不修改现有产品代码、界面或配置，不部署、不提交。
- 文档、代码检查和已授权的协议探针构成本轮调研；耳麦听感、完整延迟和网页闭环保留待实测。

## 用户提供 Key 后增加的真实探针标准（运行前冻结）
- [x] P1. 专用 Step Plan WSS 建连成功，确认 session.created 中的模型，并收到明确 session.updated；短文本输入返回非空音频及对应文本。— 谁检查: 真实服务事件
- [ ] P2. 模型生成完整自定义工具参数；客户端按 call_id 仅执行一次；返回随机生成、未提前透露的结果后，模型能准确回读。— 谁检查: 真实服务事件与本地调用记录
- [x] P3. 工具只返回 accepted/task_id，后台延迟任务未完成时能完成一次独立追问；任务完成后通过会话事件注入随机结果并被模型准确回读；不得把 accepted 当完成。— 谁检查: 真实服务时间线
- [ ] P4. 长音频生成时发送取消，记录服务器终态与取消后残留事件；单独探测 conversation.item.truncate 是否获确认。— 谁检查: 真实服务事件，不等同于耳麦停声验收
- [x] P5. 将 P1 的合成输出音频重新作为流式输入，显式开启 server_vad；观察说话开始/结束、转写和自动语音响应。— 谁检查: 真实服务事件，不等同于真人麦克风体验
- [ ] P6. 用自然中文合成语音询问网页任务进度，触发状态查询工具并回读随机结果；同一会话的简单算术问题不触发工具。— 谁检查: 真实服务事件与本地工具执行记录
- P1–P5 各自报告 passed / failed / not_run。事件形状适配可以修正探针；不得降低随机回读、先后顺序和实际应答的判据。
- 不打开麦克风、不播出声音、不操作网页；音频仅作协议验证，不将服务端事件或合成音频验证称为真实全双工体验。

## 结果与限制

- 调研四项完成；详细交付 `docs/research/20260908-step-plan-full-duplex-integration.md`。
- P2 有两次完整随机工具回读成功，也有未触发工具的变体和重复请求；不按全通过勾选。P6 的自动/手动模式均出现过成功和失败，最后同一 PCM 的成对对照为自动两次成功、手动两次失败，不能据此给出普遍可靠性结论。
- P4 取消后收到 incomplete 终态且有残留音频块，严格终态检查失败；conversation.item.truncate 两次得到确认，单独记通过。
- P3 最终通过：后台任务在接收后 30.001 秒完成；等待期间的独立问题在 3.101 秒回答完成；任务结束后准确回读随机词。system 角色被 400 拒绝的旧记录保留，换成文档支持的 user 消息并标明应用数据后才通过。
- 证据 `/tmp/ego-step-plan-research/results-index.json`，保留六轮报告和各自事件；最后一轮另保存探针源码。测试范围不包括真实耳麦、页面操作、连续可靠性、跨会话恢复或 30 分钟重连。
- Key 只在进程内使用；隐藏输入包装进程已退出。未写产品配置、未提交代码。文档 JSON 示例/证据路径与 diff 检查见本轮工具结果；未运行 npm 测试和构建。
