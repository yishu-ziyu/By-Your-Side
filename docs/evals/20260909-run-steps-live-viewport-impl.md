# 任务: 执行中过程锁在一块，步骤还在，对话不被顶走

用户已点头：侧栏执行中看到「结合」那一档——做过的步骤还在，整块过程限高、上面淡出，底下是芯片和正在写的思考，用户自己的话不被顶走。

```
Change:     任务执行中，展开的「查看执行过程」不再把用户消息顶出画面；步骤芯片还在，当前思考锁在约 175px 并跟最新；内容超出时上沿淡出。完成后卡片仍收成一行。
Not this:   不改顶栏、输入区、配色、完成后的折叠标题；不把整段过程改成只有思考字。
Evaluator:  机器：扩展测试含 CSS 契约与限高数值；npm test / typecheck / build。
            人：真实侧栏跑一条会思考的任务，确认对话还在、步骤还在、思考跟最新。
Evidence:   本卡 + 测试绿 + 侧栏样式与跟随滚动。
```

## 完成标准

- [x] 1. 进行中且展开的 `details.run-steps` 其 `.run-body` 限高并可滚动，完成后（`.done`）展开能看到全文。 — 谁检查: extension/test/steps.test.ts
- [x] 2. 流式思考 `details.thinking.streaming pre` 限高 175px 并可滚动。 — 谁检查: extension/test/steps.test.ts
- [x] 3. 新内容到来时，若视窗钉在底部则跟最新；用户在过程区内往上滚则停跟。卡片本身 `flex-shrink: 0`，不被消息列表压扁。 — 谁检查: steps.test.ts 契约 + 人
- [x] 4. typecheck / 相关测试 / build 绿。 — 谁检查: npm run typecheck && npm test && npm run build
- [x] 5. 用户确认按结合档：步骤还在、过程锁一块、对话不被顶走。代码已在侧栏。真机跑一条任务的最后观感仍可由人扫一眼。 — 谁检查: 人

## 边界与不做

- 不改完成后的折叠态与「思考过程 Ns」文案
- 不改顶栏、输入区、配色、工具芯片样式
- 不做「回到最新」按钮（过程区内滚回底部即恢复跟随）
