# 任务：一句话只经一次主 Agent 推理就得到回答

用户可观察的结果：说"嗨，晚上好"，首声从约 6.9 秒降到约 4 秒；且无论内部算了几份候选，只有一份能获得交付权，被丢弃的候选不产生任何用户可见输出或副作用。

起点：外部独立复核（基于提交 `90d796a`）指出——"必须有判定"是产品约束，"判定必须由前置独立分类器完成"只是当前实现选择。它给出的首选是：把判定并入主 Agent 的第一次推理，一次输出"意图计划 + 回答或首个工具意图"，执行权限仍由程序掌握。本文件是该复核落地前的冻结标准。

主代理已核对复核提出的两处代码事实，**均属实**：

- `session.ts:506` `sendUserMessage()` 在发模型请求前就改状态：`activeGoal`、`rpc.setPageTarget()`（换任务缺省页面）、`runTrace.begin()`；会话在跑时自动转成 steer。它不是"纯开始算答案"。
- `session.ts:1010` 在 `toolcall_delta` 阶段从**尚未执行**的 `send_user_message` 参数取正文，发 `user_delivery_stream`。交付流能在工具执行前漏出去。

## 完成标准

- [ ] 1. 闲置态内容请求只发生**一次**主 Agent 模型请求即可交付正式回答；不再有独立的分类模型请求。 — 谁检查: 机器，`scripts/acceptance/voice-greeting-live.mts` 的事件时间线（`route_start`/`route_result` 诊断消失，交付在首个可用输出后直接到达）
- [ ] 2. 一次性提案的校验覆盖现有语义：整句、分界、否定、引用、复合动作、目标名称、页面归属。 — 谁检查: npm test（`agent/test/voice-*.test.ts` 现有用例不删）
- [ ] 3. 控制类句子仍走原控制闸门，未因合并绕过授权、任务身份、执行版本。 — 谁检查: npm test + 定点反例
- [ ] 4. 下面六条反例全过。 — 谁检查: npm test
- [ ] 5. 首声：一句问候的中位数 ≤ 4.0 秒（见"目标修订"）。 — 谁检查: 机器，同一验收脚本的 `firstAudioMs`
- [ ] 6. 一轮一份回答、说停能停、不编事实三条不退化。 — 谁检查: 机器 + 人（真人麦克风）

## 必须过的反例（来自复核第 3 节）

| 情境 | 必须观察到的结果 |
|---|---|
| 校验很慢、模型很快，已产生完整回答甚至工具参数前缀 | 批准前没有正式文字、音频、工具执行、任务状态污染 |
| 生成中说"停"，随后注入旧 token、旧交付、旧 `agent_end`、重连事件 | 旧声音不恢复，旧候选不复活，不补说第二份回答 |
| 原任务执行中问进度或闲聊 | 原任务不被隐式 steer，不换工作页面，不被推测分支取消 |
| 页面导航／任务替换／控制版本变化后，旧提案才返回 | 旧提案不能对新任务或新页面执行；需要时重新读取，不用旧事实 |
| 只有页面标题没有正文；读页失败；操作回执 unknown | 不编正文、不宣称已看到或已完成、不自动重做未知写入 |
| 否定、引用、复合控制、重复 ASR 终稿 | "不要停""把'暂停任务'读一遍"不误停任务；复合动作保序；同一语音输入不重复执行 |

代码层不变量（进测试）：

```text
同一语义轮次，正式回答提交次数 ≤ 1
被丢弃的候选，产品可见输出与业务副作用 = 0
每次实际工具执行，身份、权限、执行版本仍有效
```

约束按**语义轮次**而不是 `runId` 或工具调用 ID 约束。已有空白输入恢复逻辑（空白候选恢复上一句）不得兑换成第二份回答。

## 实现范围

1. `agent/src/session.ts`：新增**无副作用**的提案入口（暂名 `prepareVoiceTurn`）。用当前主 Agent 上下文跑一次模型请求，要求只产出提案；期间不改 `activeGoal`、不调 `setPageTarget`、不 `runTrace.begin`、不派发浏览器 RPC、不发布任何交付。
2. 提案结构（放 `shared/` 或 `voice-intent.ts`）：`plan.steps[]` 沿用现有动作域；`next` 为互斥分支 `reply | tool_intent | control`。
3. `conversation-manager.ts`：`routeVoiceInput` 拆成 **准备 → 校验 → 提交** 三段。校验不通过或超时返回确定的失败回执，**不自动放行**。
4. 提交分支：`reply` 走现有正式交付通道；`tool_intent` 用现有工具链执行首个工具并把真实结果接回同一轮（不要批准后再把原话从头规划一遍）；`control` 走现有控制链。
5. 轮次协议：`PREPARING → COMMITTED → COMPLETED`，另有 `DISCARDED` / `INTERRUPTED` 两个终态。`PREPARING` 阶段的输出（含 `user_delivery_stream` 前缀）进缓冲区，只有 `COMMITTED` 之后才对外发。取消顺序：先失效凭证 → 被扣住的调用以取消结束 → 请求取消模型/TTS → 清空播放器待播。
6. 第二版才允许流式解析未完成 JSON。第一版等完整短提案通过校验再开放正文。

## 目标修订（已确认）

原目标"首声 ≤2 秒"在当前链路上不可达：识别收尾 0.7s + 一次模型往返 1.4s（`classifier-floor.json` 里最快的一次极小请求）+ 合成首帧 0.68s ≈ **2.8 秒地板**。

修订为：一句问候首声 **≤4.0 秒**，且不再随分类耗时波动。**用户 2026-09-14 同意此修订。** 账面上"拿掉分类往返"是 6.825 − 2.676 = 4.149 秒，合并后的请求负载与生成方式都会变，**必须重测，不能按账面宣布达成**。≤2 秒需要动识别或合成两端，另议。

## 边界与不做

- 不换 Step ASR/TTS，不换现有模型生态。
- 本轮**不做推测式并行**。复核结论：并行只接受"隔离候选"版本，作为合并路线之外的过渡；不接受把原话提前送进 `sendUserMessage` 再拦工具与声音。
- 裸"停"的语义不变（先停声音，再澄清任务范围）。若要让裸"停"同时冻结任务，那是产品语义变更，不在本轮引入。
- 分类超时不得自动放行。
- 已发出的网页写操作不能靠 `abort()` 假装回滚；结果未知走原有未知结果处理。

## 第一轮实测结果（2026-09-14，`out/acceptance/voice-greeting-live-2026-09-13T16-50-35-477Z`）

架构改动生效，但**只对了一半**，不通过验收：

| 轮次 | 分支 | 识别 | 提案 | 提案→首音频 | 首声合计 | 旧基线 |
|---|---|---|---|---|---|---|
| 嗨，晚上好。 | reply | 703 | 2009 | 873 | **3586** | 6824 |
| 你好。 | reply | 559 | 3185 | 746 | **4489** | 6953 |
| 十加七等于多少？ | reply | 627 | 2441 | 936 | **4004** | 5411 |
| 当前招聘页面要求几年经验？ | tool_intent | 642 | 4041 | 8217 | **12899** | 4907 |

- 闲聊/知识问答（reply 分支）：6.8→3.6、7.0→4.5、5.4→4.0，中位 4004ms，**基本达成 ≤4 秒**。
- 页面问答（tool_intent 分支）：4.9→12.9 秒，**2.6 倍回退**。逐段看：提案后主 Agent 先"想"3.6 秒才调 `snapshot`，工具返回后又"想"2.9 秒才开口。旧路径带页面预观察，不需要这两段。根因是 tool_intent 走了一条合成的续跑文案，既丢了预观察，又让模型在"我的工作页还没认领"上反复权衡。
- 单元层 1689 项、边界 167 文件、类型、构建全过；但**单元层过不了这一条**——回退只在真实链路上出现。

## 第二轮要求（未完成，必须满足才验收）

- [ ] 7. 页面问答分支（`observe`/tool_intent）首声回到旧基线以内（≤5 秒）：复用既有派发与页面预观察，不要给主 Agent 塞合成的"已批准的工具意图"续跑文案。
- [ ] 8. 控制类句子的首声不得比旧基线更慢（提案耗时不得叠加在控制链之前成为新的固定成本）。
- [ ] 9. **reply 分支只在不需要任何外部事实时使用**：凡依赖页面内容、任务结果、记忆的追问，一律退回原路径（先派发、由主 Agent 带上下文回答），宁可慢也不能靠精简上下文编事实。
- [ ] 10. 用同一个脚本复测四轮，逐分支给出前后对比，reply 与 observe 两条都要达标才算通过。

## 第二轮实测结果（2026-09-14，`out/acceptance/voice-greeting-live-2026-09-13T17-1*-*`）

| 轮次 | 分支 | 第二轮首声 | 第一轮 | 改前基线 |
|---|---|---|---|---|
| 嗨，晚上好。 | reply | **3497** | 3586 | 6824 |
| 你好。 | reply | **3779** | 4489 | 6953 |
| 十加七等于多少？ | reply | **3440** | 4004 | 5411 |
| 当前招聘页面要求几年经验？ | 精简协议→旧派发 | **5202** | 12899 | 4907 |

- reply 分支 3.44–3.78 秒，**达成 ≤4 秒**。
- 页面问答回到基线波动带（基线两次为 4907 / 5751，本次 5202），上一轮 2.6 倍回退已消除。**但未到我定的 ≤5 秒线**，记部分达成。
- 事件轨迹确认修复机制：该轮不再出现 `snapshot` 工具调用，页面正文由派发时的预观察直接交给主 Agent。

**一次被排除的波动**：同代码的另一次运行四轮提案耗时 4.7/5.1/6.0/6.0 秒（两次撞上 6 秒单次预算并重试），首声 6.3/6.4/16.0/24.0 秒。同一时刻单独测供应商往返正常（分类 1.06–1.23 秒），因此判定为供应商/网络偶发，不计入改动效果；原始记录 `out/acceptance/voice-greeting-live-2026-09-13T17-12-23-756Z` 保留。**代价是暴露了一条真实风险：单次 6 秒预算 + 一次重试，供应商打嗝时用户要等 15 秒。**

## 最终结果（2026-09-14，第四轮后）

**最终实现不是我最初写的"一次提案输出计划+正文"，而是两条协议**，原因见下节的失败记录：

- `free_reply`（白名单：问候/寒暄/致谢/告别/应答 + 纯算术）：一条最小请求直接产出正文，无 JSON、无分支规则、单次 8 秒上限不重试；拿到正文后走与其它分支完全相同的轮次闸门与唯一交付权。
- `plan`（其余全部：控制、页面问答、依赖事实的问询、动手委托）：与改前的分类调用**逐字同形**的请求 + 既有派发/控制链。

协议选择由程序判定（`isFactFreeClosedUtterance`，失败关闭），提交层用同一判据再守一道，模型自律与否都不影响是否直答。

### 首声实测（同一个脚本 `voice-greeting-live.mts --headless`）

| 输入 | 协议 | 首声 | 改前基线 |
|---|---|---|---|
| 嗨，晚上好。 | free_reply | **3799** | 6824 / 6579 |
| 你好。 | free_reply | **2760** | 6953 / 7653 |
| 十加七等于多少？ | free_reply | **3365** | 5411 / 5595 |
| 当前招聘页面要求几年经验？ | plan→原派发 | 6381 | 4907 / 5751 |
| 暂停任务 | plan→控制链 | **3356** | 改动前未测过 |
| 继续 | plan→控制链 | **3321** | 改动前未测过 |

- reply 分支跨 4 次运行共 9 个样本：2760–4250，中位 **3432ms**，**达成 ≤4 秒**。
- 页面问答：改动后 5202 / 5230 / 5251 / 5470 / 6381，中位约 5.4 秒；改前 4907 / 5751，中位约 5.3 秒 → **与基线持平，未改善**。按契约自定的 ≤5 秒线记为**未达成**，是否接受"回到基线波动带"由用户裁决。
- 控制句首次拿到真实链路数字 3.3–3.4 秒，与结构论证一致（控制链之前的固定成本未增加）。

### 这套实现是怎么被打回来两次的（失败记录）

1. **第一轮**：把判定并进主 Agent 首次推理，一次输出"计划+正文"。闲聊 6.8→3.6 秒，但**页面问答 4.9→12.9 秒**——`tool_intent` 走了一条合成的续跑文案，丢掉页面预观察，模型还多两段思考。教训：不要把已有快路径换成看起来更统一的慢路径。
2. **第二轮**：页面问答回到基线，但"防编事实"的守卫是关键词黑名单，**"工资多少？""它要求几年经验？"这类依赖页面事实却不含"页面"字样的问句全部漏过**（独立验收官实测 13 句）。改为白名单失败关闭。
3. **第三轮**：白名单命中了，但快不快取决于**模型愿不愿意直接写正文**——"十加七等于多少？"第二轮给正文（3.4 秒），第三轮不给（5.75 秒）。靠提示词维持的"快"不算数，改成独立最小请求。
4. **第四轮**：白名单本身还有个漏洞——识别实际把"十加七"转成 **"10+7"**，而匹配前会删掉所有 Unicode 标点，`+` 正好是标点，于是"10+7"变成"107"、算子消失，整句掉回慢路径。单测用的是文字输入，从来测不到。修完算数 5753→3365。

**给后续的提醒**：这条路径两次"看起来对了"都是假的——一次是页面问答被拖慢，一次是快慢取决于模型心情。判断标准只能用真实链路数字，不能用单测通过。

## 起点证据

- 外部复核原文（用户 2026-09-14 提供，基于 `90d796a`）。
- 代码事实核对：`agent/src/session.ts:506`、`agent/src/session.ts:1010`。
- 延迟分解：`docs/evals/20260913-voice-first-audio-latency.md` 及其四份 JSON。
