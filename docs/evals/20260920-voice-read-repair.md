# 任务：日常语音能读出当前页面，遇到短暂变化真实恢复，不空口承诺重试

## 完成标准

- [x] 1. 文字读页不依赖截图、不因同文档可见文字/视口变化而整体失败；短暂无文字有界重读 — 谁检查：定点单测、生产扩展无头检查
- [x] 2. 换文档、换标签页、授权失效或会话结束仍拒绝，不借重试读错页 — 谁检查：反例单测
- [x] 3. 原“看到页面吗→有看到吗→读到什么”三轮经真实 Realtime 3 和生产扩展读出可核对内容；失败不得只有口头重试 — 谁检查：无头合成语音真实链路
- [x] 4. 加载日常 Native/扩展并核对运行版本 — 谁检查：主代理运行核对
- [ ] 5. 原日常真人语音体验已解决 — 谁检查：用户（待体验，不以合成检查代替）

Change: 在原日常侧栏继续语音询问页面，拿到页面事实而非反复“再试一次”。
Not this: 只改提示词、只测模拟工具成功、只改源码未加载，均不算完成。
Evaluator: 主代理跑定点反例和无头真实语音链路，核对日常运行版本；用户最终体验。
Evidence: 定点日志、真实调用/结果与页面读回、日常加载记录。机器通过不冒充真人认可。

Goal: 修复并加载日常版本，让用户直接体验。
Hard bar: 三轮真实语音询问有可核对的页面内容且不出现口头重试无执行；页面身份保护反例仍成立。
Improve: none。

## 边界与不做

只修语音页面观察、失败恢复和必要诊断；不重开旧票组、不替换语音模型、不改用户页面、不动其他并发文件。不启动用户麦克风；真实模型测试使用隔离无头浏览器与合成声音。日常加载先检查任务/语音空闲。用户本轮明确要求从定位连续推进到解决、加载和体验，不再止于诊断文档。

## 实施与验证结果

- `read_page` 使用独立的 text 观察，不再截屏。两次同文档读取返回较新文字，容许文字/标题/尺寸/滚动变化；URL、documentId、timeOrigin、活动标签和授权仍检查。短暂空白最多三次读取（含初读），最后一次间隔 150ms，仍为空明确报错。原图文观察保留严格检查，并只记录变化字段名、不记录页面内容。
- Native 与验收器共用 `readVoicePage`，拒绝空文字冒充成功。语音工具日志增加成功/失败事实与去重原因，失败可见；提示明确据读数回答、失败不空口承诺重试。这是程序分路/有界恢复加响应约束，不是只补提示词；响应约束仍由真实模型验证，不承诺模型永不犯错。
- 先跑新增反例：旧实现 **7 FAIL / 5 PASS**，`out/acceptance/20260920-voice-read-repair/before.log`。实现后定点 **32 PASS**；`npm run check` 退出 0，边界 204 文件、类型、2501 普通测试 + 2 规模测试、构建通过。对应 `targeted.log`、`types.log`、`check.log`、`check.exit`。

### 真实语音链路

命令：`node --import tsx scripts/acceptance/voice-read-repair.mts --headless`。

隔离生产扩展/VoiceClient/VoiceService/真实 Realtime 3，合成声音输入、播放静音、不用物理麦克风。页面可见文字每 10ms 变化。

1. “可以看到我当前这个页面吗？”→ 实际读取成功，13ms，回答开源离线笔记、Markdown、版本 4.2。
2. “嗯，有看到吗？”→ 从上一条真实观察回答星河笔记及版本。
3. “读到了什么呢？”→ 报出标题、维护者林晓、4.2、Markdown 和本地全文搜索。
4. 将测试页清空，明确“再读一下当前页面”→ 确实重新读取，159ms 后报空文字；最终语音说明读取失败，没有只说再试一次。

`live-read.json` / `live.log` passed=true。上述耗时是工具读取耗时，不是说完到听见的延迟；四轮完整收音到播放完分别约 18/14/29/21 秒，没有把长答案播完时间冒充首声指标。

**检查器修订，保留原失败：** 首轮脚本错误要求每条同页追问必须重新读一次，第二轮超时（`live-read-first.json` / `live-first.log`）；用户与既定标准要求的是有真实依据的回答，并非每句都重复读同一页。修订为首次请求和明确再读必须有新执行，同页追问可引用真实观察，但每轮必须给页面事实；新增空白页明确再读反例防止永远复用旧内容。没有把空口重试放行，也没有删除失败记录。最终全部四轮通过。

### 日常加载

`node out/acceptance/20260920-voice-read-repair/load-daily.mjs` 首次确认 running=0、voiceExpanded=false 后重载；原生侧栏随扩展关闭，等待重开超时，保留 `daily-load-panel-closed.json`。Chrome 拒绝脚本 `sidePanel.open`，未绕过。

随后 `node out/acceptance/20260920-voice-read-repair/load-daily.mjs --verify-only`：在同一个日常 ChromeMain 中用临时非活动扩展标签核验，结束后已关闭。Native PID 79356/79357 → 93987/93988；实际加载的后台与侧栏脚本经 Debugger 取源，SHA-256 与构建完全一致；真实语音连接确认 ready + server_vad，未发送音频、未开用户麦克风、未操作用户网页。`daily-load.json` passed=true。Native 重启初始化了新的空会话，未删除原会话。

日常修复已生效。用户点击工具栏 By Your Side 重开侧栏，再点语音即可体验；原生侧栏无法自动打开不是代码未加载。用户最终真人认可待回执。未提交、推送或发布，`.serena/` 保留未动。
