# 任务：侧栏顶部入口清晰，菜单有克制的小动效

## 完成标准

- [x] 顶部保留品牌、小连接状态、更多；会话标题与新建独立一行；操作模式用文字选择。— 谁检查：主代理，真实构建侧栏截图
- [x] 更多中的示范、技能与记忆、阅读外观可操作；示范可结束；键盘关闭后焦点可继续操作。— 谁检查：浏览器交互验收
- [x] 空闲不占状态行；断连、运行、接管和结果继续有真实提示。— 谁检查：状态场景验收
- [x] 菜单轻量展开/收起、按钮按压反馈，支持减少动态；窄侧栏不横向溢出。— 谁检查：浏览器与主代理；最终观感由用户判断
- [x] 类型、构建及受影响检查通过，既有并发改动保留。— 谁检查：主代理

## 边界与不做

- 根据用户认可的静态预览实现，只改侧栏顶部及相关入口，不修改 Agent 执行语义。
- 使用现有动效 token 与浏览器原生控件，不新增 UI 框架或依赖。
- 隔离无头浏览器验证；不主动重载日常扩展，不提交或发布。


## 验证与边界

- 真实生产 bundle/CSS 在隔离无头 Chrome 中运行，传输和存储为受控替身；[交互结果](20260916-sidebar-header/result.json)通过，无浏览器脚本异常。
- 已验证：菜单实际 opacity/transform 过渡、快速反向开关、外部点击、Escape 焦点恢复、键盘打开无动画；阅读外观打开/修改/关闭；技能与记忆打开/关闭；示范开始/结束派发；模式选择及后台模式回写；历史打开、新会话派发及等待状态。
- 覆盖 320/360/400/520px 的空闲、运行和接管状态，均无横向溢出；断连文字保持可见，减少动态不发生位移。
- `npm run typecheck -w @sideagent/extension`、`npm run build`、3 个受影响测试文件（37 项）、`git diff --check`通过。
- [日常状态](20260916-sidebar-header/01-idle.png)、[更多菜单](20260916-sidebar-header/02-more.png)、[窄屏断连](20260916-sidebar-header/03-disconnected-narrow.png)、[深色](20260916-sidebar-header/04-dark.png)已由主代理逐图检查。
- Codex 内置浏览器另打开生产构建的本地交互预览，实际点击更多并核对截图；使用模拟连接，不运行网页任务。
- 没有重测真实 Agent 技能生成、真人语音；没有重载日常扩展、提交或发布。最终动效观感待用户判断。

## 测量修正与维护性

- 旧验收脚本写死的 Chrome 缓存版本已不存在；本脚本改为发现当前已安装版本。
- 初次键盘探针缺少 CDP Enter 的文本输入，未触发浏览器默认点击；补齐 Enter 输入后，立即 Escape→Enter 和焦点检查通过，没有为测试改产品逻辑。
- 相对本轮开始前工作区：main.ts +60/-20，styles.css +97/-107；阅读外观仅增加已有菜单入口和返回焦点注入。主要增长是明确菜单入口与焦点衔接，删除旧胶囊/教学按钮和持续脉冲样式，无新增依赖或并行状态管理。
- 后续增加菜单项可直接复用原生 popover 与原按钮行为；边界场景用示范进行中从菜单外结束、连续开关、外观弹层返回焦点检验。
