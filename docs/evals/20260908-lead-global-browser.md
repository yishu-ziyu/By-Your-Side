# 任务：主 Agent 能查看整个浏览器，操作被占用页面时自动协调接手

## 完成标准
- [x] 主 Agent 的 list_tabs 返回浏览器现有标签（含用户自己打开、其他会话的页）；worker 只能列出和操作分配给自己的页。— 谁检查：聚焦测试、真实浏览器
- [x] 主 Agent 可按 tabId 读取其他会话页面，不转移页面归属、不停止原成员。— 谁检查：聚焦测试、真实浏览器
- [x] 主 Agent 操作其他会话的页时，停止使用该页的原成员，等待已开始操作结束后移交；其他页的 worker 不受影响，旧 worker 不再写入。— 谁检查：协调与竞态测试、真实浏览器
- [x] 用户接管优先；并发接手不能覆盖已发生的归属变更。— 谁检查：聚焦测试
- [x] 针对改动的类型检查与构建通过；不跑全量测试。— 谁检查：机器

## 边界与不做
- 不合并会话内容，不重新设计侧栏，不启动 Codex 子代理。
- 保留上一轮页面回收实现与其他工作区改动。


## 结果

- 新目标4项先红后绿；相关权限/读取/协调文件6组43项通过，随后补充主Agent旧页不误停与用户闸门验证，相关5组46项通过。部分用例重叠，不合并计数。
- `npm run typecheck`、`npm run build`、`git diff --check` 通过。按用户要求未跑全量测试。
- 真实 Chrome + 生产 ConversationManager/Fleet/RPC/执行器 10项通过：`/tmp/sideagent-lead-global-evidence/result.json`。验证全局列表、无归属变化的跨会话读取、排空旧操作、移交和关闭，无关worker仍能读取。
- 本轮不测试模型自主派工；固定本地页面和工具请求验证执行能力，不伪造模型回复。只清理本轮创建的页面。扩展已重载。
- 此标准取代 `20260908-parent-tab-control.md` 中“主Agent不能跨会话接手”的旧边界；子Agent分配边界、结束回收及用户优先仍保留。
