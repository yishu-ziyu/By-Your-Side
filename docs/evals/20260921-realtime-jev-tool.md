# 任务：Realtime按需调用Jev判断当前页面的动作与对象，再使用现有浏览器工具执行

用户已认可“Realtime调用判断工具 → 宿主提供真实候选 → Jev返回建议 → Realtime再调用执行工具”，并要求接受建议后实施。保留20:31多轮修复，不提交；只做简单检查和一个隔离路径，加载日常给用户试用。

## 完成标准
- [x] Realtime注册judge_browser_action，参数只含局部要求和可选页面；候选由宿主读取，不由模型编造 — 机器：注册和参数检查。
- [x] 复用现有Jev动作/对象判断，一次调用只返回建议或不确定；不得自动执行，不把done当完成 — 机器：定点检查。
- [ ] 采纳建议后原有click/fill/press_key/scroll/tabs工具携带原观察校验，页面过期由现有执行器拒绝 — 机器：参数传递和真实隔离路径。
- [x] 多轮修复A/B不回退，类型通过，加载日常 — 机器和主代理。
- [ ] 真人自然对话中按需调用合适、体验可接受 — 用户。

## 范围

新增一个只读判断工具；没有新增Agent循环、路由或账本。Jev按现有0.85候选门槛选择，门槛不是任务成功率。材料缺失时返回需要补充value，不让Jev生成正文。候选、页面身份与执行前校验复用已有browser-decision实现。复杂自定义下拉选项沿已有选择逻辑转换为click。

页面候选目前包括可访问性角色、名称、值、状态和分组，不含每个元素的屏幕矩形；不能据此承诺可靠理解纯空间指代。本轮不扩展感知实现。


## 结果（20:59加载，未提交）

- Agent类型检查、扩展构建通过；Jev新工具2条定点检查与多轮修复A/B共4条通过。检查了宿主提供候选、判断不写页面、采纳后原工具完整传递guard、不确定/取消不返回动作。初版测试直接导入扩展运行模块导致chrome环境与类型失败，已改成Agent边界检查；没有将该失败计入产品故障。
- 真实Realtime合成麦克风第一条试跑因句间VAD分轮导致旧调用取消，未操作；记录out/acceptance/realtime-jev-tool-1789995325500/result.json。
- 单句重试仍失败：仅snapshot，没有judge_browser_action、没有click，模型口头宣称已点击。记录out/acceptance/realtime-jev-tool-1789995410375/result.json。**完整语音自动调用链未通过**，不靠修改探针判为通过，不追加循环或大量试验。
- 分开检查真实Jev工具与真实扩展：out/acceptance/jev-tool-only-1789995543498/result.json。宿主提供同名“中文”按钮及正文/导航分组，Jev-1.13.0返回正文目标@114、click及原观察guard；判断前后页面为en且未操作导航，明确执行返回的原click工具后为zh且导航未改。这个调用由测试程序发起，不能冒充Realtime主动调用成功。
- 现有扩展guard仍负责执行前有效性，当前真实探针验证正确guard可执行；本轮未另跑真人改页导致失效的路径。
- 20:59 npm run reload:ext成功；Native新进程74466，agent.log在12:59:57.679Z记录面板已连接。真人待试用。此前多轮修复保留，所有接入仍未commit。

## 修改面与下一步

新增62行Jev判断适配，复用现有候选生成、模型调用和页面身份校验；session增加只读调用事件，Realtime参数表增加一个工具与原执行工具guard参数，进度和侧栏显示“判断页面操作”。无新依赖、循环、账本或模型配置。只对必要判断调用Jev，原旁路影子记录仍独立存在。

下一步由用户试用；若要求继续修“Realtime不调用而虚报”，应定位工具选择和播报事实绑定，不将Jev选对对象写成整体语音可用。恢复点仍是94b1782，回退只撤回指定改动并重新加载。
