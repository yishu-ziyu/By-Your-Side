# 任务: 全量单元测试的 30 个旧失败逐个定性：产品坏了就修代码，测试过时就按现有设计更新

## 背景

2026-09-25 全量单元测试（`npx vitest run --exclude "agent/test/performance/**"`，3059 项）有 30 个失败，切回开工前的 `6f660ef` 同样失败。记录里仍写着只有 4 个已知失败（[09-24 开发日志](../devlog/20260924-02-回答不再被核验扣下.md)）。另有 `voice-audio.test.ts` 1 个失败，早上的[插话验收](20260925-voice-barge-in.md)已记为改前就失败。

## 完成标准

- [x] 1. 每个失败找到引入它的提交，判定是产品回归还是测试过时 — 谁检查: `git bisect` / `git log -S`，本文件逐条记录
- [x] 2. 测试过时的，按现在的有意设计更新；保留它原本保护的对外行为，并写明更新理由 — 谁检查: 更新后通过，且撤掉对应设计时会失败
- [ ] 3. 产品回归的修代码 — 唯一一条涉及安全定义冲突，已撤回改动，待用户裁决（见下）
- [x] 4. 记录里的已知失败清单与实际一致 — 谁检查: [STATUS](../STATUS.md)

## 结果

全量 3059 项：3058 通过、1 失败（待裁决那条）。

| 测试 | 数量 | 引入提交 | 定性 | 处理 |
|---|---|---|---|---|
| `click-integrity` / `pointer-input` / `hover-recovery` | 14 / 8 / 4 | `73260b6`（09-25，跨标签页 ref 改走 `axBackendNodeFor`） | 测试过时：模拟的 `axstate` 模块缺新函数，用例没走到点击逻辑 | 模拟函数按单标签页下的原行为补上（`ref` 是 AX ref 才用），跨标签页拒绝不在这几个文件范围；55 项通过 |
| `resume-entry` 重连两条 | 2 | `de381b0`（09-24，扩展内 agent；`Uplink.start` 监听设置变化） | 测试过时：模拟的 `chrome.storage` 缺 `onChanged`。本机程序不可用时仍走被测的 WebSocket 通道，前提成立 | 补上入口；16 项通过 |
| `task-recovery-matrix` 断线脚本恢复 | 1 | `ce4ecee`（09-22，有副作用的 `js` 也自动记账） | 测试过时：脚本开始时已记一条待定结果，「未记账写入」内部标记不再置位；对外结果（恢复后问用户、禁写、可读）不变，实测 `nextStep = ask_user, allowWrites:false` | 去掉内部标记断言，改名，保留对外断言 |
| `voice-audio` 就绪后才发送 | 1 | `ee3229d`（开口不丢字）与 `3b53de4`（插话停声） | 测试过时：就绪前的声音现在先留下再补发；服务端报开口时旧回答停播 | 用可控时钟等补发完成；期望改为 10 帧留存 + 40 帧、按到达顺序；开口后旧回答停一次 |
| `task-goals` 通用脚本审计 | 1 | `ce4ecee` | **定义冲突，待用户裁决** | 未改 |

## 待裁决：执行过通用脚本后，「执行账本覆盖全部变更」还算不算数

- 09-21 的测试（A）：成功执行过 `js` 后，`executionAuditComplete` 必须为否——脚本改了什么说不清，核对「不要保存」这类约束时不能拿账本当证据，只能重读页面。
- `ce4ecee` 同时加了两条相反的断言（B，`task-result-turn-economy`、`realtime-multiturn-repair`）：有副作用的 `js` 会自动记一条结果，所以账本仍算覆盖全部改动；结果未知的脚本靠「未知记录」拦住后续写入。
- [浏览器能力 v2 记录](20260922-browser-capability-integration-v2.md)当时写明这两条失败「未改其业务语义或删断言」，即没有做过决定。
- 按 A 修代码（`js`、`cdp` 执行过即审计不完整）后，A 通过，B 的两条失败；已撤回，保持现状。今天起页面脚本在所有模式下可用（[Sitegeist 对照](20260925-sitegeist-parity.md)），这条的影响面比以前大。
