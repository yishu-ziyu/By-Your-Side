# 任务：验证逐次指定原音色是否消除连续回复变声

## 完成标准

- [x] 1. 固定 Realtime 3、原 voice、句子和会话设置，仅比较 response.create 是否显式携带 voice/modalities；保存请求方式、服务事件和每轮原始 WAV — 谁检查：实验脚本、主代理
- [ ] 2. 验证协议是否接受逐次音色；比较多轮输出。音高仅作为筛查，稳定听感由用户判定 — 谁检查：主代理、用户
- [ ] 3. 候选有效后覆盖聊天、页面工具续答、打断与自动回复，再接入日常；失败则记录反例，不修改生产链路 — 谁检查：主代理

## 边界与不做

用户批准此前方案的对照实验，继续由主代理直接执行。保持 Realtime 3 和原音色，不调用 TTS、不重克隆、不使用麦克风或操作日常页面。保留已有工作树改动。

## 基线

9/21 14:01 日常会话配置回显原音色，但用户确认男声及多种女声切换。9/20 直连实验只在 session.update 设置 voice，后续 response.create 为空。本次增加逐次配置对照；没有预设其有效。

实验入口：scripts/experiments/voice-per-response.mts（复用已有直连脚本，默认无 VAD、无工具、固定四轮同句）。

## 实测结果（2026-09-21）

原始证据目录：`out/acceptance/20260921-voice-per-response/`。12 条同句原始音频均成功生成；没有生产代码改动、宿主重载或浏览器操作。

| 组 | 目录 | 四轮音高中位数 Hz |
|---|---|---|
| 会话指定音色、空 response.create | baseline-1789970949766 | 237.6 / 187.5 / 195.1 / 172.7 |
| 每次 response.create 显式 voice + modalities | explicit-1789970940612 | 141.2 / 231.9 / 272.7 / 147.2 |
| 同一候选独立新会话；增加完整请求日志与响应归属校验 | explicit-1789971069480 | 180.5 / 244.9 / 160.0 / 226.4 |

两组都在 session.update 设置相同的模型、原 voice、PCM16、无 VAD/工具和固定朗读提示。候选组同时显式 voice 与 modalities，不能分别归因两个字段。服务端接受请求并产出音频，但 response.created 不回显实际采用的 voice；接受字段不等于确认逐次覆盖生效。

候选两次实验仍有明显音高变化，未达到进入生产的筛查条件。以上数字不是说话人性别或身份判定；用户尚未试听本轮样本。每目录 comparison.wav 按轮次拼接原始音频，中间加一秒静音，不变速、不变调。分析使用 24kHz PCM、40ms 自相关、10ms 步长、相关系数 >0.65；脚本 analyze.py 保存在证据目录。

早期两个 result.json 的 passed=true 只代表四轮协议完成，不是音色通过。最终实验脚本改名为 transportCompleted，并保留 speakerConsistency 为需试听；旧原始记录不覆盖。

完成标准 2：协议检查完成、声音一致性待用户听感判断。标准 3：因候选未通过筛查，未启动页面续答、打断和自动回复改造。失败在无 VAD 的手动逐次请求中已出现，因此关闭自动生成不能单独解决这个反例。

## 检查与后续

最终脚本已真实运行四轮，完成响应状态、非空音频与 response ID 归属检查；git diff --check 通过。未改产品代码，不跑产品测试或构建。

下一研究方向：核实当前自定义 voice 在 Realtime 3 的实际支持与兼容性，区别配置回显和实际发声条件。不得把逐次 voice 请求已被接受写成稳定修复。

## 用户裁决补充

用户在官方 Demo 对照前已试听 explicit-1789970940612/comparison.wav，明确确认“连续的四个不同音色”。因此逐次指定原 voice 的修复候选判为失败，不再标作仅有音高变化或待试听。后续官方客户端对照见 20260921-official-demo-voice.md。
