# 任务：跨页面复制复用已有原文，按用户要求核验结果

## 用户授权与完成标准

用户于 2026-09-21 同意对目标账本、证据材料链、核验交付与调度进行有边界重构；主代理直接执行，不派子代理。保留原有未提交工作。本任务允许必要实现、回归测试与项目记录，替代此前代码简洁任务的仅结构改动约束。

- [x] 1. 执行回执与用户目标分离；切标签、点击成功不能消除“取得评论、粘贴正文”的未完成目标。— 谁检查：回放测试、主代理
- [x] 2. 已有页面观察能生成带来源、范围与完整性检查的原文材料；换页、handoff、恢复后可引用，不经模型重写。— 谁检查：单测、集成回放
- [x] 3. 正确编辑器为空、内容不符、对象错误或证据不完整时不得完成；内容一致才满足粘贴要求。保存是另一个要求，不擅自增加。— 谁检查：真实浏览器路径、单测
- [x] 4. 未知写入、取消、用户接管、旧文档引用、任务修订、迟到回执仍受原有保护；不得因目标未满足而重放不确定副作用。— 谁检查：回归测试
- [x] 5. 文字交付、语音事实与侧栏来自同一目标投影；idle 不等于成功；需要 Agent 核对不写成需要用户读回。— 谁检查：协议与界面检查、主代理；真人听感由用户
- [x] 6. Jev 保留操作/目标置信度，结合已有证据选下一步；重复探测有明确缺口和有界预算，不随意改阈值。— 谁检查：回放与模型实验
- [x] 7. 连续语音要求与独立新任务正确区分，资源竞争不能丢失输入或错误另开；保留暂停/重启须明确继续的边界。— 谁检查：语音路由回归，真人语音另列
- [x] 8. 本例及陌生跨页任务分别验收；通过工程检查不冒充真人或正式发布通过。— 谁检查：主代理

## 原始失败证据

`~/.sideagent/conversations/8eec1c21-014c-4eff-8eb0-2e87736726ad/2026-09-20T15-19-32-007Z_01a0bf66-aba7-7ce3-af40-da6090c21fd7.jsonl`：第 10 行首次 handoff 已含置顶评论末尾；第 175 行 Flomo 编辑器 textContent 为空；第 179 行仅两条机械动作 satisfied，目标 remaining 为空，运行已 idle。

不复制用户其他笔记和完整浏览记录到测试工件。回归材料只使用最小合成内容与必要的结构。

## 边界与不做

- 不整体重写 Agent、不增加网站专用脚本，不替换用户当前模型或音色。
- 不降低未知写入与权限门槛，不默认保存/发送笔记。
- 浏览器检查使用无头隔离环境；日常加载和真实账户写入单独留证，不自动发布。
- 核验源码与运行构建分别记录；保留原标准、失败与修订依据。

## 验证记录

源码与隔离验收已完成。以下通过范围是程序、真实模型与隔离浏览器；真人听感、日常账户和发布不包含在通过结论中。

- 定点回放先复现“两个动作成功，用户要求被清空”，现在新请求仍保留未完成目标。执行回执状态单独保留为 executionState，旧检查点继续兼容；没有把历史动作测试改成用户目标证明。
- 来源存储保留原始 AX StaticText 与 LineBreak，使用一次观察内的片段范围复制。紧凑定位文本仍保留原格式；不依靠模型重写或字符计数还原原文。任务修订后保留已捕获文本，但新来源目标须重新匹配旧证书。
- 模型比较先暴露了通用审核提示的混淆：计划审核被当成结果验收。当前分为计划覆盖、来源范围、目标对象和一般页面条件。主模型继续做规划；Jev 的不确定结果只触发一次对相同证据的主模型复核，不作为重复提取的理由。
- 阈值实验记录在 `out/experiments/goal-evidence-*`，数据集为 `eval/goal-evidence/cases.json`。首次生产判断问题集的 24 例中有一次请求超时、一个保守拒绝（checkbox=true 为 0.87）；其余符合预期，没有观察到错误接受。该小样本不是通用概率保证；后续修改后还需按最终状态验证。
- 浏览器开发验收：首个试次因测试 HTML 未正确初始化而无效，已加发起任务前的页面检查。后续试次真实暴露了需求编号歧义、作者/置顶被并入正文、来源核验未通过仍尝试填写，以及正确来源落入不确定区间。原失败工件保留；未删除失败、未据已填字段宣称完成。
- 需求引用改为 `requirement-1` 等明确编号；首次 plan 替换占位项，不能把总目标和子目标重复登记。复制目的地须绑定一个真实来源材料目标。来源未核验或值不一致时阻止自行生成的填入；未知写入仍由原闸门阻止重做。
- 跨工具提取尝试与目标核验预算写入原有私有会话日志，恢复不会重新获得同一缺口的预算。空字段是有效读数，目标未满足与缺少读数分别处理。
- 日常扩展、真实账户写入、真人语音和正式发布未在本次验收中执行。


## 最终结果与保留边界

- `npm run check`：模块边界、类型、2638 个单测、2 个规模测试、构建通过。编辑器诊断检查未发现错误/警告；最终差异无空白错误。
- 真实模型判断：[28 例结果](20260921-goal-evidence/semantic-results.json)。全部通过，包含漏目标、错误来源、正文截断、相同值但错误位置、来源复用与改口、以及“已完成”不能代替回答。原先的留出样本已经曝光，现为回归集，不宣传独立泛化结论。
- 真实分类模型：[7 句接续结果](20260921-goal-evidence/voice-results.json)全部通过。连续要求保持原任务，独立新任务另行调度；暂停/重启后的补充不隐式继续。SDK 尚未收尾但进度已 idle 的竞争由真实会话占用判断处理。
- 浏览器：[完整试次清单](20260921-goal-evidence/browser-runs.json)保留所有失败与通过。普通置顶评论→textarea（47.6 秒）、视口外论文引用→contenteditable（37.8 秒）、带诱导保存指令的正文→编辑器（34.3 秒）均有通过记录。通过试次正文逐字符一致，搜索框未变，Agent 未保存/提交；无 JS、网络或程序脚本重新提取。耗时来自不同合成页面，不与原日常任务作倍数比较。
- 最终构建再次运行视口外引用→contenteditable，23.6 秒通过全部检查，正文准确、未保存、侧栏已结束；记录在试次清单最后一项。
- 附加浏览器检查实际读取侧栏“本轮已结束”，等待提示为空。测试脚本在 Agent 结束后单独点击夹具保存按钮，确认状态由草稿变为“已保存”；该动作不是 Agent 的任务操作，也没有真实账户。
- 初始测试页只有静态说明文字，不能证明实时保存状态。该判据缺口与后续复杂候选快照的节点失效均保留原失败，分别补动态状态/保存反例，以及普通快照文档身份核验。没有将失败改写为通过。
- 语义复核曾出现一次错误接受：主模型把“内容相同”误当成“位于正确表单”。现在目标身份判断不接收重复的期望文本，补实际祖先范围；目标身份低概率不交由主模型翻为成功。文本一致仍由代码判定。旧来源复用则只判断来源关系，不重新猜测正文内容。
- 旧版本任务仅在明确继续时、且存在完整原要求时建立新目标。执行回执与未知写入锁保留。恢复保留原文，改口取得新来源时使用新材料身份；不会覆盖旧正文或删除未满足目标。已完成历史任务重开不变回待办。
- 纯回答目标在内容核对和正式交付后完成，普通插话或空泛“已完成”不能销账。缺少正式答复时仍可继续原任务。语音进度、侧栏与交付消费同一目标含义。

## 维护性与后续变化检查

相对开始本任务时的工作区基线（不是相对含有并发改动的 HEAD）统计见 [增删行数](20260921-goal-evidence/change-size.json)。增长主要用于独立目标、原始文本材料、核验和回归；没有新增依赖或第二个数据库。普通动作仍由原执行器负责，目标核验不能解除未知写入、取消或接管限制。

检查了“第一条改成第二条”“同名材料但来源改变”“相同文字落入错误表单”“无效答复不提前播出”和“恢复后不能重放未知填写”等后续场景。历史执行测试仍验证执行账本，新目标测试与真实页面分别验证用户结果；没有用模型一致意见代替页面读回。

**未运行：** 日常 B站/Flomo 真账号粘贴、真人语音听感、正式发布全套门槛。本次没有主动重载日常运行版、提交、推送或发布；模型和音色配置未改。后续日常验收需加载成套新宿主/扩展，不能混用缺少原文/文档身份字段的旧扩展。
