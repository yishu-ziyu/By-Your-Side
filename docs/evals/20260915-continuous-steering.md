# 任务：同一会话连续补充要求时，指令不丢失、过期操作被阻止、结果与反馈一致

## 完成标准

- [x] 1. 思考中连续补充三条要求，全部进入后续处理，保留未撤销的旧要求；队列中的补充一起进入下一模型轮。— 谁检查：真实Pi定点测试及真实模型/页面
- [x] 2. 执行中改变目标，已接受修改后的过期写入被阻止；已执行动作不冒充撤销。— 谁检查：可控时序测试、浏览器程序epoch检查、页面最终值
- [x] 3. 停止/接管不被普通插话等待阻塞，停止后的旧动作不复活。— 谁检查：调度定点测试及慢观察期间真实停止按钮
- [x] 4. 暂停期间补充、交还后恢复，补充保留且不重复执行。— 谁检查：会话及浏览器验收；未消费图片另有定点测试
- [x] 5. 并行助手工作时改变共同要求，旧助手不能继续按旧要求写入，更新后可继续。— 谁检查：成员定点测试及两个真实助手等待中纠正的浏览器验收
- [x] 6. 任务结束竞态、模型失败和重连有真实回执，未处理指令可追踪，不自动重复未知写入。— 谁检查：异常注入、真实重连与侧栏回执
- [x] 7. 相关旧行为、类型、模块边界、构建通过，独立复核完成。— 谁检查：自动检查、独立 DeepSeek、主代理最终修复/验收

## 边界与不做

- 用户授权来源：2026-09-15 同意先做连续插话专项验收，再按实测失败修复。
- 保留 Pi、现有调度与执行闸门；不用新框架替代现有实现。新增状态必须说明现有结构不足。
- 文字侧栏是本轮主要入口；真人语音声学验收保持单独待验，不以合成输入代替。
- 浏览器验证使用本地测试页，默认无头，不向站外提交，不覆盖用户标签页内容。
- 已有未提交改动保留；不提交、推送或发布。代码基线指纹和差异保存在本机临时验收目录。
- 接收、带入模型处理、实际结果分别留证；不得把模型读到消息称为理解或完成。

## 诊断与证据

本轮完成于2026-09-16凌晨。以下保留原失败、测量修订及最终证据；历史测试不冒充当前通过。

### 改前真实模型基线

- `out/acceptance/continuous-steering-2026-09-15T15-32-14-585Z/result.json`：真实生产侧栏、Port、扩展后台、WS 调试通道、Pi/deepseek-flash、实际浏览器工具。隔离无头 Chrome；不是日常 native host 或真人语音。
- 已通过：连续三条修改最终得到姓名李四、城市上海、备注地铁附近；邮箱 keep@example.com 保留，提交次数 0，无旧目标写入。
- 已失败：三个已接收的修改逐轮进入模型，没有一起进入下一次模型请求；中间旧步骤被执行闸门拒绝。原始标准“全部进入后续处理”虽满足，但已批准方案要求一起送入，新增该可执行检查保留为失败，不以最终字段正确掩盖额外往返。
- 较早两次测试台问题：包名引用错误、辅助 Port 名写错；第三次发现停止按钮选择器写错。均属验收脚本问题，不算产品失败，原日志保留在临时目录。

### 改前控制与时序

- `out/acceptance/continuous-steering-2026-09-15T15-34-51-029Z/result.json`：停止期间无迟到写、暂停补充后交还正确且邮箱保留、断线重连后同请求不重做、完成后迟到修改明确拒绝，四项通过。没有据此扩大修改这些已成立的行为。
- `out/acceptance/steering-races-2026-09-15T15-40-24-028Z/result.json`：真实 Session 包装层加可控 Pi/传输时序。三项失败：预观察 await 时旧写入仍允许；相同文字两条消费一条后提前放行；预观察期间停止后旧插话仍入队。这是控制逻辑证据，不冒充真实模型/浏览器。
- 团队基线 `out/acceptance/continuous-steering-2026-09-15T15-37-39-765Z/events.json`：主任务的共同修改于 1789486672350 收到；两个真实助手之后分别向标签页 32251486、32251485 填入旧姓名张三，后续主任务才安排纠正为李四。原脚本最终因读取了隔离世界的页面变量而断言失败（undefined !== 0）；该测试台错误已修正为 MAIN 世界读取，不把它算产品故障。旧要求写入由工具请求和成功回执独立证实。

## 修复后证据（首个候选；后续控制修复尚待复验）

- [三个时序探针](20260915-continuous-steering/fixed-races.json)：预观察先挡写、重复输入分别消费、停止后不再入队均通过。
- [真实主任务](20260915-continuous-steering/fixed-main.json)：三条补充进入同一模型轮且最终值正确、真正 thinking_delta 后插话成功、停止/暂停恢复/重连去重/迟到拒绝通过。该次整轮仍为失败：团队由测试台直接 fleet.spawn 装配，未把两个具体助手的 id/tab 提供给主模型，汇总阶段检查其他测试页并超时。保留原失败，不修改其结果文件。
- [团队定点复验](20260915-continuous-steering/fixed-team.json)：补齐上述手动装配上下文后，两个真实模型助手仅写入李四、邮箱保留、提交0；主任务完成汇总，无旧姓名写入。是guided团队控制验证，不是自主拆分质量评测。与上条的核心源码指纹一致。
- 普通单测1762项、类型和模块边界通过；Stagehand真实隔离浏览器兼容、停止后无迟到写、纠正与原字段保留、运行中切页及epoch变化拦截通过，原始证据 `out/acceptance/stagehand-control-2026-09-15T15-50-58-129Z/result.json`。

### 追加的真实失败：停止排在慢纠正后面

`out/acceptance/continuous-steering-2026-09-15T15-53-07-781Z/result.json`：在实际侧栏发纠正，测试台延迟其页面预观察，再点击实际停止按钮；后台于1789487595594发出abort请求，但与steer共用TaskDispatcher串行尾部，3秒内没有执行停止。等待阈值用于证明停止依赖未释放的观察，不是对普通网络速度作产品承诺。需要给控制独立且自身有序的接收通道，同时保持请求去重与任务身份检查。

## 最终结果（2026-09-16）

**已修复并重载日常扩展。** [完整真实验收](20260915-continuous-steering/final.json)的9项全部通过，记录的生产源码指纹与最终代码一致：

- 三条补充同轮交付且顺序保留，姓名/城市/备注正确，邮箱未变、提交0；另有[真正thinking期间连续三条](20260915-continuous-steering/thinking-three-final.json)的加强验收：三条输入均早于首个模型工具调用，姓名陈七/城市成都/备注安静房间全部正确，邮箱保留、提交0。
- [慢观察期间停止](20260915-continuous-steering/stop-observation-final.json)：停止先完成，迟到纠正明确rejected，写入0；控制同通道顺序与重放去重另有单测。
- 暂停补充后交还、重连重放不重复写、任务结束后的迟到纠正拒绝均通过。
- 两个真实助手都只写李四，无先写张三再补救；邮箱保留、提交0，主页面完全未变。成员已暂停/已完成/送达失败的混合状态由定点测试验证；失败成员停止并交回页面，不继续旧分工。
- 清理Pi队列失败仍能停止，保留挡写记录，拒绝新任务或交还，清理成功才恢复。主代理先复现两项失败，再修复，见`agent/test/continuous-steering.test.ts`。未消费图片暂停恢复不丢失。

### 检查与发布状态

- 最终普通单测：192文件、1772项通过；类型、模块边界通过，`git diff --check`通过。扩展构建通过；本轮未改扩展生产代码，最终真实验收使用该构建的原始指纹。
- Stagehand浏览器执行回归见[记录](20260915-continuous-steering/stagehand-regression.json)。执行分支误把规模测试与普通测试并行后超时，按专用单worker入口复跑2项通过；不把那次并发超时当成本轮产品缺陷。
- [重载记录](20260915-continuous-steering/reload.json)：核实安装路径为本仓库`extension/dist`、native wrapper指向本仓库Agent；182个会话与控制快照均idle且无接管事务后重载，确认新SW和新native进程，随后进程仍存活。日常扩展未新开测试任务；完整操作验证是隔离WS路径，不冒充native全链路重跑。
- 未提交、推送或发布；其他已有改动保留。真人麦克风、声学中断和自主拆分质量不属于本轮文字路径通过的结论。

### 独立复核与主代理处置

独立DeepSeek复核只读完成；因只读沙箱临时目录限制，复核者不能自行跑Vitest，复用了已核对指纹的实测。它确认控制被阻塞、成员反馈不完整与清理静默失败；控制修复由一个有界执行会话完成主要代码，达到600秒时限后停止。用户随后要求不再委派，主代理独自修完剩余问题并完成最终验收，没有再启动子Agent。

| 复核项 | 最终处置 |
| --- | --- |
| 控制被普通输入阻塞 | 控制独立且自身有序，真实慢观察停止通过，原请求身份/去重保留 |
| 成员被静默跳过 | 暂停成员保存，结束成员明确未重启，失败成员停止；人类可读名字进入回执。已结束成员不会自行复活，因此未另建“全局共同要求”存储 |
| 清理异常吞掉 | 保留未清理记录、禁止继续；停止本身仍执行；两项反例先失败后通过 |
| 主页面锚点可能误导成员 | 加入主页面完整状态未变断言，最终真实双助手通过；不把此样本当任意任务的语义保证 |
| Slash模板改写文本 | 未确认为本次侧栏缺陷：侧栏消息有页锚点，skillsOverride为空，当前用户prompt目录不存在；未为假想配置替换已验证的精确身份机制 |

### 复杂度与边界

相对本轮开始时的工作树，4个生产文件共增加223行、删除22行，详见[统计](20260915-continuous-steering/change-size.json)。增长用于控制优先、逐条补充身份、暂停/故障保留及成员反馈；沿用Pi、原回执与执行闸门，没有新依赖或新协议。新增测试和验收脚本不计入生产代码数字。主代理检查了最终增量，没有回退原有未提交修改。

本轮场景是有限、可复现的文字路径验证。已经开始的外部动作不能被倒退；程序保证的是之后的过期步骤受控，模型理解仍以实际字段/结果验收。

### 并发修改边界

最终检查发现00:16另有阅读功能的并发改动（shared/reading.ts与shared/protocol.ts等），不是本轮或本轮子Agent写入，已保留。本轮普通检查完成于该改动之前，日常重载也在该改动之前；不把本轮通过扩大为阅读功能的验收。连续插话4个生产文件与最终真实验收指纹一致。
