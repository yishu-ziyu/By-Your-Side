# 任务：只调整回复正文的结构、排版和加粗

用户纠正范围：只保留输出文字的调整，其他设计全部恢复。随后明确认可“查看执行过程”展示，但必须位于正式回答上方。回退节点为7a92d5f；不回退同目录另一组页面接管功能改动。

## 完成标准
- [x] 1. 顶栏、配色、输入框、模型入口、小伙伴、动效、会话/记忆/接管与7a92d5f一致；main.ts仅保留“查看执行过程”的标题/列表图标，过程保持正式回答上方。— 机器：diff
- [x] 2. CSS新增规则只作用于.msg.assistant.markdown；调整正文、标题、段落、列表、表格及加粗，不改变周围界面。— 机器：选择器检查与构建
- [ ] 3. 新回复先结论后依据，短语少量加粗，不减少用户要求的信息；不改变任务执行及过程展示。— 机器：提示词diff；人：实际文字效果
- [x] 4. 类型、测试、构建通过。— 机器

取消之前扩大到整个侧栏的方案。变更保持未提交，待用户确定。

实际正式侧栏页面检查：原渐变背景存在，新工具栏不存在，“查看执行过程”在正式回答前（true）；证据 /tmp/sideagent-reply-layout/check.json 与 restored-layout.png。类型、616项测试通过，最终构建通过。用户最终观感待裁决；未新提交。
