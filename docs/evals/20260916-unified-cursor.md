# 任务：正式网页使用统一右上角状态，目标旁仅保留授权确认

- [x] 读取、等待、动作结果与跨页信息合并在右上角，保留角色身份。
- [x] 光标动作不带信息卡，确认/取消保留在操作目标旁，键盘可用。
- [x] 同页/跨页多角色不互相覆盖；清理、失败终态与迟到事件保护有效。
- [x] 生产bundle浏览器检查、类型和相关测试通过；检查日常加载状态。

主代理检查实际构建与状态测试，用户最终裁决观感。沿用现有角色/控制协议，等待原因未确定时使用“处理中”，不虚构模型状态。

## 验证
- [生产bundle受控浏览器结果](20260916-unified-cursor/result.json)：20项通过。本页/跨页共存、清远程保留本页、未知结果持续可见、授权按钮键盘触发、结束自动收起与停止清理均已执行。跳转与授权消息由测试runtime接收，没有发送真实外部操作。
- 后台状态/竞态26项通过，操作路径、标注授权、overlay与点击完整性56项通过；扩展类型、构建、架构边界通过。
- [读取状态](20260916-unified-cursor/reading.png)和[确认操作](20260916-unified-cursor/confirmation.png)已检查。第一张确认截图暴露旧布局收缩，修正固定最小空间后重新截图及验证。
- 日常侧栏预检无运行任务、草稿或语音后重载扩展；最终加载证据另见同目录native-loaded.json。本轮没有额外调用真实模型；原任务调度与授权协议未改。
- 历史cursor-status-check入口改为调用新版验收：用户明确将光标名牌改为右上角状态，旧名牌字号/色条断言已被新行为标准取代，并保留确认、生命周期与跨页检查。

## 实现取舍
共享既有右上角组件，本页数据从现有动作/状态实例读取，跨页仍来自后台成员表。没有新增状态总线或依赖。等待改为“处理中”，移除无法证明模型等待的秒数；任务结束显示“本轮已结束”，失败后的结束不覆盖失败。结果未知保留到下一动作或显式清理。原型已选定并退役。

未提交或发布到商店；本次“正式版接入”指项目生产代码与本地已安装开发扩展。
