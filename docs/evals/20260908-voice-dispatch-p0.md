# P0：固定语音／文字预算修改基线

## 完成标准（执行前冻结）
- [x] 文字、语音各从独立干净页面启动真实任务；先实际筛选1000，再运行中提交“预算改成八百，只看八百元以内的书桌，再按价格从低到高排序。”；最终DOM必须为budget=800、prices=[699,799]、sort=asc。两条路径都保存原始结果，无论通过或失败。— 机器：`node --import tsx scripts/acceptance/voice-steer-run.mts`
- [x] 使用真实执行模型、生产ConversationManager/StepVoiceSession和Chrome扩展；语音输入为固定TTS原生PCM，不注入转写。报告不称真人麦克风或侧栏端到端验收。— 机器：事件和输入来源
- [x] 同一conversationId、一次agent_start；语音修改送达回执恰好一次，回执先于确认播报。缺少正式runId/requestId标为未支持，不能伪造字段声称完成A2/A12。— 机器：事件断言
- [x] A11已有空输入、重复/旧轮、晚到分类、暂停/结束拒绝和发送失败测试跑出基线；未覆盖的断线结果未知和重启恢复单列债务。— 机器：聚焦测试及矩阵
- [x] 判定器必须拒绝旧预算1000、结果含899、未排序、只有口头成功、重复回执及重复启动。— 机器：判定器反例测试
- [x] 前置失败记blocked并非零退出；运行证据使用独立目录，不覆盖历史结果；仅清理本次创建的验收标签。— 机器：脚本检查及前置故障测试
- [x] `npm run typecheck`、`npm test`、`npm run build`、`git diff --check`通过。— 机器

## 边界与不做
- P0建立可信基线，不修改生产调度代码，不开始P1。
- 历史失败可在当前版本消失；必须报告当前结果，不为复现强行恢复旧实现。
- 测试通过不等于A2/A11/A12完整能力通过；记录缺失的产品能力。
- 等待门只让运行中修改有确定投递窗口，不替模型填写、筛选或排序。释放等待门后由模型继续完成页面操作。

## A11／A12基线矩阵

| 检查 | 当前证据 | 尚缺什么 |
|---|---|---|
| 空转写不调度，下一句可处理 | voice-session.test.ts通过 | 真人采音 |
| 原生工具重复调用不重复提交 | voice-session.test.ts通过 | 应用路由完整requestId去重 |
| 新语音轮使旧分类失效 | voice-session.test.ts、conversation-manager.test.ts通过 | 真实连续讲话 |
| 暂停、结束、任务标记不匹配拒绝修改 | conversation-manager.test.ts通过 | 正式runId与执行边界竞态 |
| 投递失败不确认成功，回执先于生成回答 | voice-session.test.ts通过 | 历史重载后的结构化回执 |
| 断线／重启后知道是否已执行 | 无完整合同 | P1接收记录与unknown语义 |
| 分类超时、非法结果可恢复 | 本轮未做真实故障注入 | 后续faults套件 |

## 执行记录

- 执行前4文件23项相关测试通过；新增判定器反例后，全量79文件658项测试、typecheck、build通过。
- 首轮证据 `/tmp/ego-voice-p0-2026-09-08T13-43-28-276Z/` 保留。文字遭遇真实MiniMax-M3 HTTP 529，零网页工具调用，属于上游阻塞。语音任务到达页面，但模型将等待表达式改成异步发起后立即返回，导致任务在ASR完成前结束；该轮不作为运行中修改判定。语音正确拒绝，没有成功回执，页面仍1000。
- 修正测试同步：只延迟等待工具的真实返回结果，不代替网页动作、不注入转写、不修改判断断言。开始第二轮对照。

## P0结果

第二轮文字／语音均通过，证据摘要见 `20260908-voice-dispatch-p0-results.json`，原始目录 `/tmp/ego-voice-p0-2026-09-08T13-47-32-153Z/`。

| 路径 | 初始结果 | 最终结果 | 启动次数 | 语音回执 |
|---|---|---|---|---|
| 文字 | 1000，899/699/799 | 800，699/799，升序 | 1 | 不适用 |
| 语音 | 1000，899/699/799 | 800，699/799，升序 | 1 | 1次，先回执后音频 |

- 语音最终转写准确，分类耗时1150ms；提交音频到首段输出3423ms。仅一个样本，不能称P95达标。
- Chrome 152.0.7977.82，扩展0.1.0，已读取的background.js哈希与当前构建一致。执行器为本次脚本加载的生产模块，不宣称在运行的侧栏服务／真人麦克风已验收。
- 正式前置故障命令：`node --import tsx --import ./scripts/acceptance/fixtures/chrome-unavailable-loader.mjs scripts/acceptance/voice-steer-run.mts`。已验证输出blocked、退出1、results为空；这条是明确注入的故障测试，不属于真实对照通过次数。
- 只删除本次open_tab成功返回的标签ID；结束后只读检查未发现残留的P0语音测试页。
- 新增判定器拒绝10类反例；79文件658项测试、typecheck、build、diff检查通过。
- P0完成：基线和判定器已固定。没有修改生产功能。历史“口头成功但页面1000”在有效重跑未复现；不能据此声称已消灭所有竞态。

## P1输入

保留此组DOM和时序断言。接下来补正式runId/requestId、共享调度接口、去重与结构化持久回执；断线结果未知和重启恢复仍缺合同。当前20／42次工具调用说明执行过程仍有试错，但单组样本不足以归因为语音路由；P0不为减少次数另改产品。
