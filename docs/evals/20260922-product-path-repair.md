# 任务：修复真实网页资料到草稿的交付与接续

用户在 [Computer Use 验收](20260922-computer-use-product-path.md) 后要求主代理直接修复，并明确要求呈现修前/修后优化指标。本轮沿用真实页面、原输入和完成标准，不以降低原文核验门槛换取通过。

## 完成标准

- [ ] 原文可准确捕获观察片段内的一句话；多余句子、错误对象或改写不能通过来源核验。— 机器：定点反例、真实 Jev；主代理：原页面对照。
- [ ] 已验证原文可以按已核验的用户要求附上其真实来源 URL；正文和附加内容都可精确核对，不允许任意追加内容。— 机器：填写闸门与落点反例；主代理：真实 Flomo 草稿读回。
- [ ] 明确“继续原任务；修改……”保留原 run、原要求、材料和未知操作限制，再应用修改。— 机器：真实入口回归；主代理：原改口路径。
- [ ] 已生成且满足回答要求的正文正式交付到当前侧栏；取消、过期回复和不完整答案不误交付。— 机器：交付回归；主代理：原问答路径。
- [ ] 构建重载后重测公开来源→Flomo 草稿、改口、接管/交还；不保存、不提交。— 主代理：Computer Use。
- [ ] 汇报同一输入的完成情况、来源范围/对象正确性、正式交付与总耗时、工具/模型回合和被迫介入次数；标明样本数及失败耗时，不把未测写为改善。— 主代理：前后证据对照。

## 边界

保留并发工作；不改模型、音色、凭据或语音功能开关，不整体重写 Agent。复用现有原文、目标、执行、交付账本，不新增服务。未授权提交或推送。此次修复不把语音连通提升为指定口令/听感通过。

## 修前基线

事实与输入保留在前述验收文件和 `out/acceptance/20260922-computer-use/`。问答后台有文本但未正式交付；切页单例40s成功；首次复制5m20s失败、草稿空；改口选错另一处 Note、仍未填写。完整资料→草稿成功数0/1；此失败样本不用于计算成功任务的提速百分比。

开始修复时的受影响文件副本和哈希保存在 `out/development/20260922-product-path-repair/baseline/`，用于区分本轮改动与已有未提交工作。


## 开发中复测与进一步修正（不是最终通过）

- 15:30 首次重载；15:36 二次重载。问答试跑曾因输入操作附带多余 `Jev` 后中止，该轮排除原输入对照但保留日志。原问题的干净会话 `97890bea-1991-4218-9fc4-bcfadb3743ec` 最终在 15:42:23 正式交付，UI 3m47s；它暴露了纯问答规划被错误要求捕获原文的问题，不能称为提速。
- 修正规划说明，信息问答可直接使用已读页面；复制任务仍需来源与落点。真实 Jev 三个对照 3/3：仅回答、附带多余材料引用仍可回答、不能以回答代替复制。证据 `out/development/20260922-product-path-repair/answer-plan-probe.json`。
- 15:45 重载后原复制输入在 `980a645e-b7a7-4958-af3e-fe165ab8774a` / `b377b7fc-651d-4126-84d2-03b07185aca2` 中，准确捕获第一句38字符，并保存完整来源容器；目标正确带 `appendSourceUrl:true`。原文范围问题已在真实路径修复。
- 本轮进一步暴露富文本核验缺陷：Flomo 正确的两段 DOM `<p>第一句</p><p>网址</p>` 的 `textContent` 没有换行，字段核验误拒绝，模型随后反复改写。15:51 主代理中止本次试跑；不将其计作完成。修正为新增只读 `editableText`，保留原始 `textContent`；段落/硬换行/真实空行分别保留，不靠删除空白放宽核验。
- 标准 fill 原本支持 contenteditable，但工具说明没有写明；补充说明，并在 capture/inspect 返回由主机组成的 `fieldValues`，明确一次填写完整文本，无需预先点焦点或拆成多次输入。
- 继续改口覆盖了中断、部分结束及已完成后明确提出修改；保留原 run 和要求。普通“继续”不复活已完成任务，已取消任务也不自动复活。
- 当前定点检查包含真实读回序列化的普通换行、硬换行、空段落、末尾空行、内联格式；保留未知写入读回的原身份和敏感字段限制。最终真实草稿复测仍待完成。

- 15:58 原输入复测 `3e79a2ea-6ad7-46b5-ad43-dc234bfa2b15`：模型采用标准 fill，但现有 contenteditable 实现直接赋 textContent，Flomo 将换行解析为空格。16:01 中止（已确认 aborted）。继续修复实际填写函数：DOM 与 AX 路径共用一个可序列化的原生编辑函数，一次 insertText 完整替换，不模拟 Enter、不自动回退重写。16:02 定点检查、类型、边界和构建通过；此试跑不计成功。

- 16:07 原输入复测 `b7333440-e9cb-4611-a7f2-9b8464835235` / `70dc57b5-fb31-4caa-b09e-0126a942c93c`：标准 fill 成功，独立 CUA 读回两行内容，`editableText` 与期望逐字一致。60s 截图超时、重复核验仍导致较慢；随后发现第2次同类 js 调用未新增执行回执，`executionAuditComplete` 从 true 变为 false（16:09:50），因此“未提交”条件无法被确认，仍不能算完整通过。16:14 左右中止，不以一次随机复核通过覆盖此失败。
- 修复自动执行记账：已完成回执不阻止后续已授权调用新增自己的回执；在途/未知项仍不可改绑，重复同一 toolCallId 不重复计数。两个定点反例在修改前分别只留下第1次调用，修改后均保留两次且审计完整；原重复执行拦截与未知写入检查保留。
- `task-result-turn-economy.test.ts` 还有两条在本轮记账修改前已失败的旧断言：把 js / worker_tabs 当成永不记账的探针，与当前基线源码已实施的真实执行记录冲突。更新为核对准确工具列表及成功状态，未删除调用、未放宽写入闸门；旧/新输出见本轮工具记录。
