# 任务：语音操作真实完成、及时交付，保持原说话人

## 完成标准

- [x] 1. 自然切标签请求通过真实 Realtime 3 派发，隔离浏览器实际切到所需页，任务结果独立核验 — 谁检查：主代理真实语音/浏览器回归
- [x] 2. 工具输出已就绪且服务端生成结束后，不因前导语未播完继续等待；主动通知与播放回执仍正确 — 谁检查：定点单测及真实事件时间
- [x] 3. 未核验/失败的任务不能经正文或语音冒充完成，已完成任务有对应结果事实；取消/过期回执不复活 — 谁检查：反例单测和真实交付回归
- [ ] 4. 原自定义 voice 配置保持一致，查验多轮普通回复与工具续答的输出音频；确认音色漂移原因后修复，最终稳定听感由用户裁决 — 谁检查：主代理链路/音频证据、用户试听
- [x] 5. 相关回归通过后仅在日常任务与通话空闲时加载；核实运行进程和浏览器资源，保留未通过项 — 谁检查：主代理加载验证

## 边界与不做

用户要求当前主代理接手直接执行，并从全栈判断；必要的架构重构须提出具体位置、原因与范围供用户决定。保留工作树全部既有改动，不提交/推送，不启动用户麦克风，不用真实用户页面做诊断，不更换语音模型/原音色或付费重克隆。不为 B 站等个例添加网站或关键词捷径。

## 基线与方法

main@3605347 加已有未提交改动；来源为 [22:01 交接](20260920-general-browser-handoff.md)。已有播放等待候选和 23 项单测不是本轮新修复，不覆盖真实切标签 FAIL。沿输入、路由、执行、核验、交付、播放检查状态与耗时；读回、任务完成、实际已播分别取证。

本轮产物：`out/acceptance/20260920-voice-repair/`；写入前源码快照位置与哈希存于其中 baseline.json。

## 中间结果与范围修订

- 切标签原失败已在当前树复现：`voice-dev-1789913707589`，actual task result timeout；23 项语音旧定点通过不覆盖此失败。
- 第一阶段修复了结果登记与不可逆副作用混用、未核验 complete 正文仍发送、语音丢失 facts 三处边界。正常表达与纠正表达的真实语音均单次派发、独立活动页读回与最终播放通过：`voice-dev-1789914098517`、`voice-dev-1789914407311`。工具结果就绪→送回均为 1ms，未再受前导语播放阻挡。
- 全栈计时显示仍慢：两轮服务端判停→切换调用 6010/5902ms，→正式交付 8358/8440ms。用户随后回复“继续”，同意本轮把已观察标签页接入公共 Jev 候选并核验；不增加网站关键词路由，不绕过页面归属/取消/授权。后续结果另填，不能用第一阶段通过冒充新架构通过。
- 音色：用户已试听并确认隔离样本复现变声。同 voice、同句话的文字输入与合成语音输入均有声学差异；完全绕开产品的最小 Realtime 3 直连也复现。证据在 `speaker-*/` 与 `speaker-direct-*/`。音高统计仅作佐证，不代替真人听感。用户明确不增加 TTS，只排查 Realtime 3；未换模型/音色、未调用 TTS。
- 第一轮完整 check 有 8 项失败：旧测试替身缺 tools 清单，另一个恢复测试在任务尚未开始时就暂停却期待它已开始。补齐替身并先等待真实 prompt，保持全部原行为断言；准备中取消不晚启动的独立反例保留。随后 check 通过（2595 普通 + 2 规模、类型、架构、构建）。第一次失败仍保留在 check.log。
- 原“空任务”记录核对发现：接收记录确实有 image.png 附件，模型实际消息也含图片，不是语音原话被此链路删空。原图无新明确文字要求；不凭此推断用户误操作，也不把它当语音派发丢字的证据。

## 最终结果（2026-09-20）

已加载日常的修复：

1. 切标签结果登记与不可逆写入分开；别名 tabs/switch 与原始 switch_tab 都保留结果与目标身份，换另一标签不复用旧结果。失败保留未完成，但不把焦点切换当未知远程写入永久锁死表单。
2. complete 请求与宿主 unverified 事实矛盾时，在交付工具边界拒绝；语音接收到 partial/unverified 时只播宿主未完成状态，保留任务归属，详细报告仍在侧栏。
3. 浏览器实际列出的标签纳入公共 Jev 候选；观察绑定成员、来源和目标，切换前复查标题/URL，切换后读真实活动标签。仍走原页面移交、权限和取消路径；混合任务继续在新页面观察，不增加站点捷径。
4. 工具结果只等生成结束，不再等前导语播完；accepted/queued 的异步任务写入工具结果后等待实际结果，不额外生成第二遍计划。播放超时按前面尚未播完的音频队列计算，仍有上限与未回执失败。

### 真实路径与计时

| 场景 | 结果 | 证据目录（均在 out/acceptance/20260920-general-browser/） |
|---|---|---|
| 初始切标签（修前） | FAIL，完成记录缺失 | voice-dev-1789913707589 |
| 仅修结果链的正常/纠正表达 | PASS；切换约 6 秒，交付约 8.4 秒 | voice-dev-1789914098517、voice-dev-1789914407311 |
| 公共候选正常表达 | PASS；判停→活动标签读回 1902ms，→交付 5417ms | voice-dev-1789916296561 |
| 公共候选纠正表达 | PASS；判停→活动标签读回 2668ms，→交付 6433ms | voice-dev-1789916395515 |
| 最终版正常表达 | PASS；判停→活动标签读回 1358ms，→交付 5669ms，实际播放通过 | voice-dev-1789916855991 |
| 填写/勾选（中间失败） | 页面正确，但播放超时；保留 FAIL | voice-dev-1789916477127 |
| 填写/勾选（播放修后） | PASS，原话单次派发、字段/勾选正确、未保存提交、结果实际播完 | voice-dev-1789916759536 |

时间从服务端 speech_stopped 起算，不包含用户说话和服务端判停前的延迟；浏览器活动读回是可执行验证，不是渲染帧/真人听觉计时。以上为小样本回归，不外推平均提速或陌生任务成功率。新循环实测操作都来自同一个 browser_loop，主模型只在最后核验/交付。

播放回归新增两类反例：前方 20 秒音频排队时，后方 2 秒音频不能在尚未轮到时误超时；真正不回 playback_done 仍必须失败。中间失败不是靠放宽真实检查器的等待判据改绿。

### 工程与加载

- `check-final.log`：类型、边界、2605 普通测试、2 规模测试、构建通过。最后仅补未核验的跨任务播报名称，29 项语音定点与类型再通过（final-voice-boundaries.log、final-types.log）。
- 日常空闲检查通过，Native 进程从 79115/79116 更新为 15517/15518；运行后台/侧栏代码与 dist 哈希一致，Realtime 3 ready。七份关键宿主源码哈希记录在 daily-load.json。未改本机开关、模型或音色配置，未开用户麦克风、未操作用户网页。
- 首次加载后的探针遇到面板初始化切换会话而关闭；保留 daily-load-first.json。没有再次重载，等会话稳定后仅 verify-only 通过，并恢复原选择会话。临时验证标签已关闭；原生侧栏需用户点工具栏重开。
- 未提交、推送、发布。原有未提交改动保留；本任务差异以 baseline.json 指定的写入前快照区分，不把相对 HEAD 的全部变更算作本轮。

### 未解决：音色

用户确认 speaker-comparison.wav 复现“忽男忽女”。进一步固定同一句话：产品连接的纯文字输入、连续合成语音输入、无产品逻辑的最小官方协议直连均出现明显声学差异；最小直连只配置原 voice、PCM16 与一句朗读指令，没有浏览器、任务工具、播放器或麦克风。

证据：speaker-1789914168246、speaker-text-1789914935267，以及 speaker-audio-repeat.log / speaker-direct.log 指向的目录，保存完整 WAV、服务事件与分析。基频分析是 40ms 自相关、高周期性帧统计，只作佐证，不能等同说话人身份判定。服务端输出前已发生变化；提供方内部原因和稳定修法未确认，未宣称修复。

用户明确拒绝增加 TTS，保持 Realtime 3；没有调用其他发声模型，没有重新克隆或改变原 voice。最小复现留在本机，未向任何第三方发送故障报告或私人日志。
