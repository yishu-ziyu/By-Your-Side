# 任务：Realtime 直接调用现有浏览器工具，加载日常供用户试用

基线：94b1782。用户批准先提交现状，再接工具、本人试用；通过后另一次提交，否则仅撤回本次改动并重新加载。

## 完成标准
- [x] Realtime 实际注册 tabs、navigate、snapshot、read_element、read_elements、hover、click、fill、type_text、press_key、scroll、mark、page_translation 共13个既有工具 — 机器：会话配置、真实服务握手。
- [x] 工具调用到既有浏览器执行器，不经过 Pi 规划或 Jev 选道；参数校验、接管、旧话轮、重复调用和未知写入保护有效 — 机器：定点协议与入口回归。
- [x] 隔离无头页面上，通过真实 Realtime 完成操作，独立读回页面变化；不以模型播报代替结果 — 机器：真实模型与浏览器探针。
- [x] 构建、加载当前日常入口，确认新 Native 进程 — 主代理。
- [ ] 真人试用确认体验 — 用户；满意后才提交接入改动。

## 接入范围
直接复用 tools.ts 的参数定义和执行代码。tabs 包含 list/active/open/switch/close；fill 包含原生下拉选择。page_translation 包含 translate/display/restore，翻译正文仍由现有翻译生成器产生，不宣称全程没有其他模型。

未接入 browser_loop/browser_run（另一层循环或脚本）、page_operation（多人共享字段）、fetch/network/js（网络与任意脚本）、screenshot（二进制图像返回，现有语音工具回传仅文字）。原四个对话/委派工具保留用于读取、状态及确需委派的工作。

执行仍沿用宿主工具权限、接管、执行事实和未知写入检查。直接工具调用不会自行把整个用户目标标记 verified；本轮通过真实页面读回和真人体验判断，不伪造正式完成证书。


## 实测与试用加载

用户随后明确要求只做简单检查，真人自行试用；不运行全量测试和泛化评测。

- 类型检查和模块边界通过；扩展构建通过。既有语音会话/时序回归40项通过；新增直接工具5项最终通过（参数错误、重复调用、串行、插话取消、关闭与诊断隔离）。并发工具结果曾提前触发续答，已改为本批结果齐备后再续答。
- 首次真实探针因测试接线漏传 conversationId 中止，修正探针并清理其隔离浏览器；没有改动用户浏览器。
- 随后发现空会话 executionAuditComplete 缺省值误触发旧任务保护，已限制为存在 runId 时检查。保留失败证据：out/acceptance/realtime-direct-tools-1789992416441/result.json。
- 最新真实服务探针：out/acceptance/realtime-direct-tools-1789992655449/result.json。合成麦克风经过真实侧栏、Realtime、工具适配、生产浏览器执行器。实际调用 snapshot → fill；独立页面探针读到 value=星河、saved=false；oldRoutes=0，无 Pi 任务规划派发。
- **探针整体仍为 FAIL**：额外要求模型主动再调用一次读取工具未发生，post-write readback timeout。实际字段改变通过，但模型未主动二次核验；保留原始失败，不改写为全部通过。本次按用户要求交付试用，不继续增加评测或核验框架。
- 2026-09-21 20:12 日常扩展 fnbjglhppbkgmjeehablkfilmmefjolo 重载成功；Native 新进程40571启动，agent.log 在12:12:44.408Z记录面板连接，源码路径仍为本仓库。
- 新增生产代码约170行，集中于工具适配、真实话轮绑定与既有工具执行入口；未另造浏览器实现、模型路由或任务账本。基线之后的接入改动保持未提交，等待用户试用。

## 已知边界

Realtime 自主选择工具并决定何时回答，目前提示词要求读回不等于程序强制核验；不承诺13个工具均已逐项实测。原有旧任务接管/等待恢复/未知写入状态会拒绝新直接操作。整页翻译仍调用既有内容生成器。语音旁路影子日志仍可能调用 Jev，但不控制本次直接执行。

## 回退

用户要求回退时，以94b1782为基线仅恢复本次修改的6个生产文件，移走本次新增适配文件、测试和探针；对文档保存必要试用结果。若其间出现其他改动，按差异撤回本次部分，不使用全仓 reset --hard。恢复后重新构建、重载扩展。网页已发生的输入和点击不由Git回退撤销。
