# 任务: Codex 开发任务收尾时沉淀已验证经验，形成待用户审核的规则或 skill 提案

## 已确认的目的与范围

用户在本次 Codex 会话明确指出“我们现在不在 Kimi 里面”，并确认目的：让本仓库在 Codex 中开发时积累经过验证的经验，值得复用的经验经用户审核后升级成规则或 skill。

用户随后批准的范围：将提炼、核对、记录、待审接成每个开发任务的收尾步骤；修改项目规范、现有经验库入口和提案流程。

## 完成标准
- [x] 1. AGENTS.md 明确由当前 Codex 主代理在开发任务验证后、最终交付前执行复盘；无新经验时不凑文档；流程不依赖 Kimi 进程或退出事件。 — 谁检查: 主代理逐条审阅
- [x] 2. 沿用 `.kimi-code/wiki/` 唯一经验库，入口说明名称是历史路径；明确 pattern 的证据与适用条件、提案状态及用户裁决后的处理。 — 谁检查: 主代理核对规范、入口和提案流程
- [x] 3. 用本次 Codex 工作中“验收入口与用户实际入口不一致”的真实教训完成提炼、核对、记录，并形成一条具体待审提案；不把提案自动写进规则或安装 skill。 — 谁检查: 主代理核对用户已确认目的、前轮验收记录与本轮产物
- [x] 4. 已获用户明确授权的目的对齐要求写入开发约定，保留已确认范围内连续推进规则。 — 谁检查: 主代理审阅规则一致性
- [x] 5. 本次 Markdown 本地链接可解析、diff 空白检查通过，原有经验和提案保留。 — 谁检查: 本地链接检查与 `git diff --check`

## 边界与不做
- 本次是当前主代理执行的任务收尾约定，不是新增宿主钩子、后台服务或退出后运行保证。
- 不启动其他代理或 Kimi，不扫描历史会话，不迁移或另建经验库，不修改旧提案的内容与待审状态。
- 仅修改项目文档；不修改产品、运行全量产品测试、重载扩展、提交或推送。
- 提案的采纳属于下一项用户决定；本次验收只到“具体提案已形成且保持待审”。

## 验收结果
+
+- 当前 Codex 主代理在本轮完成了筛选、依据核对、经验写入、提案待审和交付前检查，没有启动其他代理或 Kimi。
+- AGENTS.md 已明确收尾触发时机、无新经验时跳过、证据核对、提案不得自动生效，以及实质性改动前对齐目的与范围的要求。已确认范围内连续推进的约定保留。
+- `.kimi-code/wiki/index.md` 作为统一入口，提案规则在 `proposal-workflow.md`；原有两条经验和 probe-scripts 提案保留。
+- 新经验为 `patterns/acceptance-entry-mismatch.md`；新提案为 `proposals/20260909-acceptance-entry-field.md`，明确标注待审。已检查 AGENTS.md 的验收模板尚未增加该字段，`.kimi-code/skills/` 未创建。
+- 检查 8 份相关 Markdown 中的 21 个本地文件链接，全部可解析；本次范围的 `git diff --check` 通过。逐条审阅规范与提案的一致性通过。
+- 仅修改文档，未运行产品单测、类型检查或构建，未重载、提交或推送。本轮没有验证宿主退出后执行或未来会话必然遵守约定，不作这类保证。
+- 用户下一项决定是是否接受新增模板字段提案；它不影响本次已批准收尾流程的生效，也不被记成已接受。
