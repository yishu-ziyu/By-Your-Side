# 任务：简单完整任务核验后直接交付，减少重复等待

## 授权与基线

用户于 2026-09-21 明确委派 Sol 完成三项：简单任务核验后直接交付、补齐分段计时、减少串行判断与重复快照。主代理保留集成与最终验收。以目标/原文证据链已通过隔离验收的当前未提交工作区为起点，不能退回旧 HEAD。日常运行版不主动重载。

## 完成标准

- [ ] 1. “切换到已打开的指定标签”“调整已有译文显示”完整匹配时，先登记具体目标，再经现有执行器执行、独立读回、保存结果并交付；命中路径不调用主任务模型规划或重写完成说明。— 谁检查：定点测试、真实模型与无头扩展路径、主代理
- [ ] 2. “切过去再总结”“不要切换”“是否已打开”、歧义候选、改口、未完成旧要求不能被部分动作提前销账；接管/暂停/迟到决策/未知写入的原保护保留。— 谁检查：反例测试、主代理
- [ ] 3. 复用原日志记录观察、判断、执行、核验、保存/交付阶段，关联真实任务身份与目标版本；单进程耗时采用单调时钟，明确快速未命中的额外等待。— 谁检查：实际日志、定点测试
- [ ] 4. 不可能的分支由已有事实排除；独立语义选择尽量合并；同请求兼容范围的观察共用，写入后或文档变化不能复用过期执行凭证；普通流程接到已获得的有效观察和执行事实。— 谁检查：调用次数与边界测试、真实路径
- [ ] 5. 既有原文复制、技能复用与交付行为不退步，最终差异可维护；原始失败保留，工程通过与真人体验分开报告。— 谁检查：受影响回归、主代理 diff 审查、无头浏览器；真人感受由用户

## 边界与不做

- 只新增上述两类完整任务的快捷结束，不扩展任意点击、输入字段、自动保存或跨页复制的无模型规划。
- 不新增调度模型、第二队列/数据库/事件总线，不重写语音，不更换模型或音色。
- 不采用跨请求全局快照缓存，不缩短全部超时，不降低目标核验与未知写入保护。
- 性能必须报告实测起止点、成功与失败/回退样本；1 秒目标不是预先承诺，不用文字注入冒充真人语音端到端。
- 无头隔离浏览器可执行，真实账户写入、日常重载、提交推送与发布不在本轮范围。

## 证据

实现由 Sol 执行；验收脚本和最终判定由主代理完成，不能以子代理报告代替产品路径。

### 修前基线

[全部样本](20260921-fast-task/baseline.json)来自任务开始时的源码快照和当时构建的扩展。真实侧栏文字输入，隔离浏览器；计时从脚本向侧栏派发前开始，包含桥接和轮询开销，不是真人语音端到端。

- 切标签：正式交付 12.14、15.95、24.46 秒，分别 6、8、8 个主模型回合。第三例标签保留检查缺少对照数据，结果/交付通过但该检查不能定性为产品错误。补齐页面初始化等待与前后标签列表后重测。
- 译文显示：一次 17.28 秒/7 回合通过；一次页面已变化但 90 秒无 finding；一次 86.98 秒才给 finding，侧栏仍未显示完成。两个失败保留，不能只算成功样本中位数。
- 初次夹具在发任务前未等标签初始化，属无效设置，修正后才派发。
- 真实失败日志显示通用流程在已操作后反复被计划覆盖判断拒绝；本轮应在执行前绑定完整模板目标，不删核验或降低通用阈值。

### 开发中实测（不是最终验收）

- 首轮真实 Jev 选择器 12 例：8 例符合预期，4 个正例保守回退，没有反例被误放行。原始记录 `out/experiments/fast-task-live-1789968427618/results.json`。要求改进任务效果描述后复测，未为通过测试降低通用阈值。
- 首次新切标签路径实际通过：`out/acceptance/fast-task-current-tab-1789968888061/result.json`，正式交付 0.968 秒，主模型 0 回合；活动标签、其他标签保留、目标、交付与侧栏独立检查通过。分段耗时观察 39ms、判断 842ms、执行 47ms、核验 19ms、保存交付 6ms。此为单个施工中样本，不构成最终版本整体性能结论。

## 用户接手试用（范围修订）

用户随后明确要求停止过度测试：只完成必要检查后立即交给本人使用，后续以实际反馈推动开发。该要求优先于此前拟定的扩展回归矩阵。

- Sol 已交付并冻结源码；最后定点批次报告 67/67。主代理独立取得切标签、显示模式、字体、决策中暂停及原文复制的真实无头证据；通用选择器16例、有效技能5例通过。早期无有效输出认证的技能夹具已纠正，原记录仍保留，不用它定性产品失效。
- 最终源码类型检查和构建通过。未继续运行全套测试、性能重复采样或真人语音自动回放；最后的 proof 前控制检查、精确技能分段计时及手动技能失败 partial 交付只完成类型/构建检查。手动技能失败这一条旧失败已由 Sol 修正，未再次运行该用例，留作试用边界。
- 已在日常任务与通话空闲时重载成套扩展和 Native 宿主。新进程已启动，实际加载的 background/sidepanel 与构建一致，恢复原选择会话；没有发起网页任务、开麦或播报。加载记录：`out/trials/20260921-fast-task/daily-load.json`。
- 已知问题：复合“切换并总结”已保留切换已有标签的身份，但普通模型流程仍出现90秒内未正式交付。不能将本轮简单任务加速外推到复杂任务。
- 下一步由用户正常使用并反馈。本轮不再自行增加测试或扩大修复，不提交/推送/发布。
