# P3：语音暂停、继续和终止真正控制页面及团队

## 完成标准（实现前冻结）
- [ ] 语音控制经共享dispatcher发给扩展，调用与按钮相同的冻结/快照/交还流程；成功回执必须晚于扩展最终确认。— broker/扩展控制测试
- [ ] 暂停整个任务及其团队，等待已有页面写入完成，之后5秒零Agent写入；查询和停止播报不影响任务。— 真实Chrome计数页及合成音频
- [ ] 人工步骤用测试脚本在暂停期间修改受控页面；继续必须使用该新标记和原任务runId，不新建任务。— 真实页面、原生恢复事件
- [ ] 终止等待模型/团队停下与页面操作排空；旧runId和旧控制代次的迟到/排队写入不能在恢复或新任务中落地。— 队列/协议测试与Chrome
- [ ] 暂停中的修改只保存原话，继续时再交给原执行器；修改不隐式继续，失败或部分恢复如实返回。— manager与真实恢复测试
- [ ] 重复、过期、错误会话的控制回执不改变状态；断线/超时不能确认成功，过期控制帧不能执行。— 故障注入
- [ ] typecheck、test、build、diff通过；真人停声/听感保留为人验项。

## 边界
- 不把后台abort等同于页面已经停手。
- 复用原控制协议；voice pause作用于整个会话团队，现有单页接管入口语义保留。
- 增加执行代次元数据用于拒绝旧写操作，不改网页工具的能力范围。


## 2026-09-09 验收补记

真实完整双成员控制路径一轮通过，原报告12项检查；包含暂停5秒无写入、暂停保存600、带新人工标记恢复、原runId、终止5秒无写入和旧写拒绝。曾出现partial被提前当失败，修复后同路径通过。

单元/协议控制覆盖已补；总方案三轮连续真实路径仍欠两轮，真人暂停/改页/恢复/终止尚无通过证据。整组标准保持待完成。

原始条件保持不变。检查范围、失败和可保存证据见[验收台账](20260909-voice-dispatch-results.md)及[结果摘要](20260909-voice-dispatch-evidence.json)。未勾选表示整条要求尚未全部满足，不表示没有做过任何实现。
