# 任务：任务收尾可信、来源可点击、侧栏减少重复信息

用户批准规划后修改，并要求实际核实宣称已实现的功能。原生试用反例见20260913-native-dogfood.md，不覆盖原证据。

## 完成标准

- [x] 有效文字/工具交付后的空尾响应不再报“整轮空响应”；真正无输出、真实最终错误仍提示；接管/中止不残留误导性自动重试提示。— 机器定点事件测试、主代理真实模型原生侧栏
- [x] https://example.com，页面显示 的href为https://example.com；中文相邻句读、Markdown链接、带参数/片段/括号的合法URL仍正确，HTML安全过滤保留。— 回归测试与原生侧栏实际链接检查
- [x] 普通任务不重复整段答案和长接收通知；异常/拒绝/未知结果仍可见；任务身份、当前页面、接管及授权入口保留。— 主代理真实截图、定点行为测试；体验由用户裁决
- [x] 同一简单读页、打开网址、跨来源任务真实重跑，记录效果和面板耗时；未测项不标通过。— 主代理原生电脑控制
- [x] README/界面已实现承诺形成真实路径清单，标注已验证/失败/未验证，优先覆盖本轮影响路径。— 主代理及独立只读清单复核
- [x] 定点测试、类型检查、构建、独立复核通过（DeepSeek复核超时后由原生独立子代理补查），最终diff保留原WIP。— 机器、复核代理与主代理

## 边界

保留全部并发改动，不提交推送。不改模型和凭据，不扩大权限，不通过隐藏全部错误让测试变绿。已有插件可构建、重载以验证用户授权的修复；UI只用原生电脑控制。真声学及全部能力的穷尽验收不冒充本轮完成。当前旧任务应空闲后重载。

## 实现和机器证据

- 后端用当轮成功交付事件判断正式回答是否给出，读取/ack/失败预览均不算交付。真实最终错误保留；新提问/插话/交还时清除旧交付状态。接管或中止期间不发自动重试提示。
- 网址使用局部Markdown裸链接tokenizer，中文句读结束链接；明确Markdown链接、代码块和中文路径等保留；外层DOMPurify照常消毒。
- 侧栏普通接收/修改回执折叠，原话与目标可展开；拒绝/失败/未知理由直接显示。标题和页面保留在选择器/页面标签；顶部只用“回答已给出”表示已有交付，不复述全文、不宣称所有动作成功。
- 前端3文件28项通过，后端4文件80项通过，加交付/语音确认3文件21项，共129项；两端typecheck、build通过。[日志目录](20260913-dogfood-fixes/)。没有重复跑全仓suite。
- [独立复核](20260913-dogfood-fixes/review.md)发现的3个反例已红→绿；主代理还移除了流式参数预览提前标成功的路径。
- [功能承诺清单](20260913-feature-claims.md)已形成，逐项分开入口、操作、成功标准与实际证据。

## 原生复验状态

已通过原生Chrome扩展详情重载本项目 extension/dist。期间另一项目窗口同时切换，出现user changed及ScreenCaptureKit -3812；使用原生窗口菜单定位测试窗口，并在每次动作前读最新AX，最终完成复验。没有换用CDP/Playwright/注入RPC。

| 真实路径 | 修后结果 | 证据 |
|---|---|---|
| 与修前相同的岗位名/城市问题 | 正确，全文仅正文一次，接收回执折叠，空响应与重试提示均无；面板5.7s（修前该样本27s） | [截图](20260913-dogfood-fixes/native-simple.png)、[AX](20260913-dogfood-fixes/native-simple.txt) |
| 新标签打开example.com，并指定中文尾文回复 | 新标签真实打开，链接目标example.com/，尾文独立；无空响应；12s | [截图](20260913-dogfood-fixes/native-link.png)、[AX](20260913-dogfood-fixes/native-link.txt) |
| 实际点回执链接 | 新标签显示Example Domain，未去错误域名 | [AX](20260913-dogfood-fixes/native-link-click.txt) |
| 与修前相同的两页比较指令 | IANA新标签打开，3项事实与来源正确，无空响应/重试；11s（修前23s） | [截图](20260913-dogfood-fixes/native-multi-source.png)、[完整AX](20260913-dogfood-fixes/native-multi-source.txt) |
| 展开接收回执 | 可查看原目标与完整要求，默认仍一行 | 同上完整AX的“任务已接收”展开内容 |

以上耗时来自面板单次样本，页面与缓存状态不同，不作稳定提速或分位数承诺。打开网址的修后指令额外指定了中文相邻回复，因此不与旧样本比较性能。

接管/中止、真实失败保留由本轮事件回归检查；本轮没有再次完成真语音、交还恢复、成员协作、记忆管理全路径。不宣称产品所有已实现功能均已真实验过。

## 剩余体验与交付边界

- 模型仍偶尔添加用户未要求的信息、内部tab编号；本轮未修改模型提示词，不能把界面精简当作模型简洁性已解决。
- 当前部署为本机已重载的开发扩展；未提交推送。原有WIP全部保留。
- 视觉观感最终由用户裁决；机器和主代理已检查真实操作与可见反馈，无已知本轮阻断项。
