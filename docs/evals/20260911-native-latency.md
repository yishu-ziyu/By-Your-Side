# 任务: 点接管后立刻不能再改页；交付一到，面板马上能看见

用户能观察到：点「接管」后，Agent 不能继续填、点、跳；正式回答到达侧栏后马上出现，不用等网络前面那段。

计时从按钮按下或交付进入扩展开始，用同一只钟，不含模型往返。

## 完成标准

- [x] 1. 100 次：交付进入扩展 → 面板可见该正文，p95 ≤300ms。隔离无头，真 SW + 真面板。 — 谁检查: `native-latency-2026-09-11T15-39-12-922Z.json` **p95=18ms**
- [x] 2. 100 次：点接管 → 写闸门已关，p95 ≤100ms。 — 谁检查: 同上 **p95=15ms**（单次最大 170ms）
- [x] 3. Agent 未连接时，本地仍关写入口，不再把闸门撤回去。 — 谁检查: 接管后 `gate.user===true`；未连宿主不再 `failPendingControl`
- [ ] 4. 人眼接管手感 — 谁检查: 人（本轮不拦）

## 边界与不做

- 不连日常 Chrome。默认无头。
- 不计模型/网络前时间。
- 不打正式标签。
