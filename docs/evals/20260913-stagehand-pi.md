# 任务：当前网页填写、停止、纠正后继续，切页不串任务

## 完成标准（实施前固定）

- [ ] 在用户已登录的当前 Chrome 页面填写指定字段，不另开浏览器，不重新登录，不提交。检查者：主代理操作与页面证据，用户复验。
- [ ] 操作中发出停止要求后，不再派发新动作；记录请求停止、最后实际动作、停止回执三个时间。已派发但尚未结束的动作不得显示为已停止。检查者：事件轨迹与页面状态。
- [ ] 将姓名纠正为李明，其他已确认字段保留，原任务继续，不提交。检查者：真实输入路径与页面最终值；真人语音不得以合成输入替代。
- [ ] 执行中切到另一标签页，另一页不发生写入，操作始终属于原任务页面。检查者：两页值、页面身份与操作事件。
- [ ] 以上成立后，固定同模型、同起始页面、同任务，多次比较首次有效变化、全程耗时、失败次数及停止响应。未完成门槛不得宣称提速。

## 边界

保留现有执行方式。新接入必须经过同一任务身份、页面归属、授权和取消约束。独立探针不默认启用生产。当前阅读外观与语音交付未提交修改由原任务拥有，不改动。本轮不做视觉改版、依赖全面升级或浏览器重启。

隔离无头 Chrome、确定性脚本、模型工具调用、正常侧栏输入和真人语音分别标证据，不相互替代。工具调用返回成功不等于目标完成。

## 现场确认

- 2026-09-13：真实仓库为 `/Users/mahaoxuan/Desktop/AI 产品/By-Your-Side`，旧任务路径 `/Users/mahaoxuan/Desktop/ego` 已不存在。main 有阅读外观和语音相关并发修改。
- agent/package.json 已依赖 Pi 0.84.4；session.ts 调用 createAgentSession。browser-program.ts 已有 QuickJS 连续程序、逐步中止检查和现有 RPC。实验不是首次引入 Pi 或代码执行。
- 日常 ChromeMain 正在运行，已经启用调试端口 9222；只做只读探测，未重启、重载或修改它。此机器配置不能代表普通用户 Chrome 均可连接。
- 基线定点测试：browser-program、browser-program-binding、tab-bindings、control-gate 共 58 项通过。仅证明已有自动检查通过，不是新方案通过。

## 本轮结果

执行探针、独立复核、主代理修正及实际复跑已完成；Stagehand 原样接入未获接受，原始四项用户路径尚未完成端到端验收。详见 [技术结果](20260913-stagehand-pi-probe.md)。

### 现有执行层的浏览器基线

`node --import tsx scripts/acceptance/page-transfer.mts --headless` 已通过：页面移交期间旧会话写入被拒绝，另一页面仍可用，新会话接手后可填写。证据：`out/acceptance/page-transfer-2026-09-13T11-00-14-280Z/result.json`。这是隔离浏览器中真实已构建扩展的定点检查，无外部模型；不能代替本轮四项完整路径。

### 上游取证

主代理获取官方仓库至本机临时目录，固定提交 `b771930d2b4d858e5bd9670203c66260b385a8fa`，SDK package 标明 4.1.0。DeepSeek 沙箱 DNS 失败，改由主代理获取后交其离线核查，未切换模型提供方。
