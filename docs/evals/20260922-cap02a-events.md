# 任务: CAP-02A 事件 / 下载 / 动态文件选择 / JavaScript dialog

## 完成标准

- [x] 1. 提供 popup、download、filechooser、JS dialog 的正式观察/等待/处理路径；命令发出 ≠ 事件可见 — 谁检查: `npx vitest run agent/test/page-event-ledger.test.ts agent/test/cap02a-events.test.ts`
- [x] 2. 订阅先于触发：宿主签发 token（arm → 动作 → wait/consume）；不因串行队列先阻塞 wait 再排 click 死锁 — 谁检查: `agent/test/cap02a-events.test.ts` arm→click→wait 顺序反例
- [x] 3. 下载：页面/blob 路径经 Page.setDownloadBehavior（页会话级临时目录），非 fetch(GET)；两页同名不串；失败/取消分证；saveAs 宿主 fs — 谁检查: ledger 单测 + `download-artifacts` 单测
- [x] 4. 动态 chooser 走 TaskUploadLedger / `file_chooser_set_files` → `setFilesOnBackendNodeId`，禁止 raw CDP 绕过 — 谁检查: 代码路径 + 既有 `cdp-guard` / upload 授权测试
- [x] 5. JS dialog 正式 accept/dismiss/dialog_info；confirm≠危险业务授权；不伪装 OS 权限提示 — 谁检查: 协议/工具描述 + unit（dialog 缓冲）
- [x] 6. listener 在超时/取消/页面销毁/session stop 后清理；错误 token / 停止后迟到事件安全失败 — 谁检查: `page-event-ledger.test.ts`
- [ ] 7. 隔离无头 browser_run 真实路径（瞬时 popup、blob 下载、动态 chooser、上传后 dialog、两页同名）— 谁检查: `npm run accept:browser` 类入口 — **本轮未跑（NOT_RUN）**

## 边界与不做

- 不做 CAP-02B/C、SEL；不改 input.ts / keymap / pointer-input / click-integrity / hover-recovery
- 不改 STATUS、ego-fixed-sha-behavior 矩阵状态列
- 不加 `downloads` 权限：本实现用 debugger CDP `Page.setDownloadBehavior` 写任务临时目录，不经 `chrome.downloads`，不接管无关下载、不改全局下载目录
- 不把新原语塞进 Realtime 固定工具表

## 对标

固定 SHA：`citrolabs/ego-lite@dca7003349c5f7132189ba00547cbbd7ff8e597e`（MIT；实质性参考 `driver/downloads.ts` / fileChooser / dialog 生命周期处保留 CitroLabs 版权与许可声明）

矩阵行（`docs/evals/20260922-ego-fixed-sha-behavior.md` CAP-02A #1–#14、#16–#22）：本票**不改**该文件状态列（仍为 NOT_RUN）。下列为本票实现/单测结论：

| # | 项 | 本票结论 |
|---|-----|----------|
| 1 | waitForEvent popup + arm | **yes**（arm_event/wait_event + 单测顺序） |
| 2 | receipt popups | **partial**（wait 回执含 popup；click receipt 未扩 label/targetId 面，留给 CAP-02B 合并点见下） |
| 3–11 | download 族 | **yes**（单测/宿主链）；真实 blob 无头 **未跑** |
| 12–14 | filechooser | **yes**（协议+设文件路径）；真实动态 input **未跑** |
| 16–19 | dialog | **yes**（accept/dismiss/dialog_info/pageInfo.dialog）；真实页 **未跑** |
| 20 | events() | **yes**（consume_events） |
| 21 | listener 清理 | **yes**（ledger 单测） |
| 22 | ≠ OS 权限提示 | **yes**（仅 Page.javascriptDialog*；工具说明） |

## 命令与退出码

```text
npx vitest run agent/test/page-event-ledger.test.ts agent/test/cap02a-events.test.ts agent/test/fix02-wait.test.ts agent/test/tool-surface.test.ts agent/test/browser-program.test.ts agent/test/realtime-direct-tools.test.ts
→ exit 0（123 passed）

npm run check:architecture
→ exit 0
```

隔离无头 browser_run / `accept:browser`：**未跑**（记 NOT_RUN，不写成通过）。

## 留给 CAP-02B 合并的接口

- `ToolContract.click` / `double_click` 的 `newTab` 仍为 `{ tabId, url? }`；若 CAP-02B 要统一成 EGO `popups: [{ label, targetId }]`，可复用 `wait_event` popup 回执的 `label`/`targetId` 形状，或在 input 回执上附加同一结构（本票未改 input.ts）。
- 页事件账本：`shared/page-event-ledger.ts` + `extension/src/background/page-events.ts`（`armEventForTab` / `stopSessionPageEvents` / `holdAttach`）。
- 正式 RPC：`arm_event` / `wait_event` / `disarm_event` / `consume_events` / `accept_dialog` / `dismiss_dialog` / `dialog_info` / `file_chooser_set_files` / `download_stat` / `download_cancel` / `download_delete`；宿主 helper：`armEvent` / `downloadSaveAs`。
