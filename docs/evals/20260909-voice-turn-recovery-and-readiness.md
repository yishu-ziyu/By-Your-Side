# 任务：一句没判断成功仍能继续说，纠正任务有上下文，打开页面及时返回真实就绪状态

## 完成标准（实现前冻结）
- [ ] 1. 模型不可用、意图超时、候选无效只结束该轮，语音socket与麦克风保持可用；下一句成功处理一次。旧轮结果/音频不串入新轮，已接受或未知结果不重做，不将未知声称为未执行。— 谁检查: voice-session、voice-client及协议测试；真人连续输入
- [x] 2. 模型不再负责覆盖每个分句下标；应用保留完整原话，跨句动作有顺序且不丢控制、否定、条件与指定目标。覆盖语气词、复合请求、纠正、未来条件、引用与缺目标，不为某个词建补丁。— 谁检查: voice-intent测试及真实模型诊断集
- [x] 3. 意图判断获得有界的当前任务目标；运行/暂停时的纠正与另开请求分开判断；上下文是数据，不代替本轮授权。模型重试保持总15秒边界，诊断区分调用失败与候选拒绝原因，不记录密钥或默认记录私人原话。— 谁检查: 分类调用契约测试、真实模型
- [x] 4. 打开标签页按当前文档可交互状态就绪，不强制等待全部子资源；导航/同URL重载不接受旧文档就绪。明确返回就绪或等待超时状态，不虚报加载完成；后续网页操作仍有自己的控制校验。— 谁检查: 可控Chrome API测试、实际浏览器慢资源页
- [x] 5. typecheck、test、build、diff检查通过；记录分阶段结果与未验证部分，不恢复三轮/20次个人试用前置门槛。— 谁检查: 主代理
- [ ] 6. 判断失败后直接说下一句、修改正在执行的任务、打开慢资源网页的实际体验由用户验收。— 谁检查: 人

## 边界与不做
- 保留当前工作目录全部既有修改；不顺带改订阅池、音色或界面布局，不提交或推送。
- 不靠删安全校验、缩短等待到虚假成功或把未知动作重放来提速。
- 不承诺消除外部ASR/模型服务延迟；本轮给出可归因诊断，修复已证实的程序放大机制。
- 用户浏览器仍由用户控制；先完成本地工程验证，再协调必要的扩展重载与真实路径验证。


## 本地验证结果（2026-09-09 13:43）
- 先红后绿：分类失败主动关闭socket；结果未知错误提示；分界分配、同任务合并、匿名目标澄清、未来条件归属。分类四类错误恢复与前端收音存续均通过测试。
- 最新全量91文件754项通过；typecheck/build/diff通过。第1条机器部分通过，真人连续输入仍未勾选；第6条留给用户实际试用。
- 固定48句真实M3一轮48/48，/tmp/ego-voice-intents-1788932319721；新增自然说法8/8，/tmp/ego-voice-natural-1788932500835。完整动作、目标和原文检查，没有恢复强制三轮门槛。自然说法中一次分类重试总6.388秒，不能宣称全链路已足够快。
- 独立无界面Chrome载入生产openTab/navigate源码，慢图片始终未完成时文档已interactive且实际输入框可读；同URL导航新documentId通过。4项通过，等待分别13ms/143ms，这是受控页面样本，不代表公网地图速度。浏览器进程已退出，未使用或修改用户Chrome配置。
- 前次45/48及两次独立浏览器脚本选错worker导致的启动失败保留；前者为真实候选问题，后者为测试工具选择问题。未放宽断言。
- 结果归档：[机器证据](../diagnostics/20260909-voice-engineering-results.json)。尚未重载用户扩展，当前安装版本的真人路径待用户接入。
