# 任务：能直接操作观察到的内容，定位失败后依据新证据恢复

## 需求依据
用户 21:14 明确否定逐案例修补，要求追查反复失败的机制。21:09 真人失败撤销 S2 的用户路径通过预期：快照有标题但无引用，猜测 CSS 两次失败，随后误向用户索要继续定位的授权。

## 完成标准（实现前冻结）
- [x] 1. 快照中有 DOM 节点的标题、正文、图片、控件提供可回查引用；不靠页面业务名、CSS 层级或位置猜测。裁剪掉的行不登记引用。— 谁检查：定点快照测试与隔离浏览器
- [x] 2. 真实浏览器分别在不同父级同名标签、无标题正文、图片、Shadow DOM 上，从观察引用完成读取与标注；文字引用圈文字范围，不能扩大为整页。— 谁检查：隔离浏览器几何断言和截图
- [x] 3. 定位未命中/多匹配属于本次未执行，不代表用户撤销授权。重新观察后可修正同一结果目标继续；未知执行结果、取消/接管不可自动重放，重复无变化失败仍终止。— 谁检查：现有结果/控制测试，真实模型恢复路径
- [x] 4. 原真人布局改口，以及不同内容、不同结构的新任务均通过真实模型到真实页面的可见结果检查；不能用指定选择器/给模型答案替代自主定位。保留所有失败，不能只汇报最后一次。— 谁检查：隔离模型浏览器验收
- [x] 5. 稳定后一次 typecheck / npm test / build / diff；重载真实扩展并确认生效。— 谁检查：主代理
- [ ] 6. 用户实际使用时圈对对象，等待可接受。— 谁检查：人；机器结果不代替体验验收

## 边界与不做
- 不增加无条件重试，不写模型名称/页面专属定位规则，不更换模型掩盖缺口。
- 不进入 TTS，不提交推送，不改其他用户工作。
- 本文记录标准与证据；当前进度只在 docs/STATUS.md。

## 实测证据

- 用户失败记录：21:05:38 快照有两标题、无标题引用；21:07:20 CSS 未命中；21:08:43 匹配两个对象；21:09:16 未画圈并索要继续授权。真实试用未通过。
- 新机制红测 2/2 失败：内容没有引用，输出被截断的 ref 仍被登记。修改后通过。
- 独立浏览器发现正文标注滚动后扩大到容器，修复为 AX backend node 直接传入扩展绘制环境，保留 Node/Range；不是用坐标重新认对象。
- 浏览器检查：同名标题分别定位、正文/图片/Shadow DOM 引用读取与圈画、滚动/resize 后精确范围、移除节点后不重定位其他对象，最终全部通过。
- 真实 M3：原并排卡片改口通过；更换正文、图片、嵌套内容，并注入一次真实选择器未命中回执，模型随后完成标注，未要求用户再次授权。
- 修复途中保留：正文范围失败；截图频率限制；测试图片编码错误。看到图片未加载后增加实际 decode 检查，没有把 alt 占位符的几何当成图片验收。
- 全量第一遍因新增模块在 import 时访问测试环境不存在的 debugger 导致 18 项失败；改为首次实际标注时启动监听。随后旧点击测试暴露序列化函数引用 Node 全局的模拟环境兼容问题，使用 nodeType 数值判断，原点击断言未放宽。定点 25/25，最终 125 文件 991 项、typecheck、build、diff 全绿。
- 扩展已重载。实际 background SHA256 `b1252367ffda7c072a3f57ec49cc670c8336d86146ab2120ffb059119a1cab63` 与最终构建一致。
- [完整分阶段记录](../diagnostics/20260909-observation-action/summary.json)、[并排改口结果](../diagnostics/20260909-observation-action/live.png)、[正文滚动结果](../diagnostics/20260909-observation-action/text-after-scroll.png)、[图片结果](../diagnostics/20260909-observation-action/image.png)。

本次内容引用验收覆盖默认 full_page AX 路径；DOM 降级快照仍保留其现有覆盖限制。真实模型测试使用生产运行时、工具与侧栏，传输为隔离 CDP，不代称真人麦克风/native messaging 验收。分阶段修改与失败保留，不宣称同一最终源码跑完所有场景。用户日常体验仍待真人判断。
