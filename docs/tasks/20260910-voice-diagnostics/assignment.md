你是用户授权的DeepSeek主要实现者。通过jcode运行，模型Go deepseek-flash/max。目标：完成docs/evals/20260910-voice-diagnostic-capture.md的第1步，不扩大成通用诊断平台。先读该验收契约、docs/tasks/20260910-voice-diagnostics/lessons.md、baseline-status.txt，以及受影响代码。你不是唯一工作者，不要撤回他人改动。主代理负责浏览器、真实服务、人声采样和最终验收；你只实现与跑定点/typecheck/build。
所有权：shared/voice.ts的诊断协议与校验；agent/src/voice-session.ts、voice-service.ts及必要新诊断helper；extension/src/sidepanel/voice-client.ts、voice-ui.ts及必要新诊断helper（独立样式小文件或局部styles.css追加）；必要voice-speech/worker观察字段；对应定点测试。不要改AGENTS/STATUS/NOTES/现有光标文件/任务执行模块/模型参数；这些由主代理维护。其它模块只有确实接线必要才最小改动并报告。
明确实现：普通语音保持原行为，diagnostic模式由start显式请求、backend明确确认后才能送音，服务端阻止route与steer。诊断仍经真实StepVoiceSession转写/旧轮过滤，以便取到原始ASR与被过滤的事实，不能旁路成独立ASR客户端。必须在socket.send成功之后捕获实际上游append/commit（包含event_id），绝不把客户端准备的音频当C1。保留单独的诊断item/turn关联，不改现有旧轮删除策略。记录filter前原始ASR、user-text转发、DOM最终question文字三处。C0 classifier之前复制原24kPCM，C1逐样本可与C0匹配。不要改VAD参数。diag控件用现有侧栏中的简短details区域，手动开始/结束录音、两份音频可播放、原始和显示文字可看、JSON导出和清空；没有完整上游回应要标未完成。禁自动mic/音频播放/下载，容量/时长有界（例如每次60秒）；失败和重连不假装完整。UI别变成复杂控制台。
实现节奏：最多15次定点读取后开始写最小实现，不反复扫全库。先1条样本机检，不跑全量1050项。每轮失败把检查项/失败值/根因追加lessons，同一检查连续3次失败或3轮就停报告，不能改断言粉饰。不得启动其他子代理、浏览器、服务、真实ASR或录音，不提交推送，不读凭据。完成后给文件/定点结果/尚未验证点。不要以单元通过宣称语音修复。