# GPT 6 Pro 交接：连续语音体验与识别退化

## 当前裁决

用户15:51明确反馈识别变差，对两天的工作越来越不满意，要求停止当前推进，将所有改动commit并push，交由GPT 6 Pro指导。当前是保存未通过真人验收的工作快照，不是发布已修复版本。不要沿用“测试绿了”作为体验成功的判断。

工作区：`/Users/mahaoxuan/Desktop/ego`，分支`main`，远端`yishu-ziyu/By-Your-Side`。本轮开始时HEAD为`03b7fe4`，本次提交可与该版本比较。主代理独立实施，无子代理。用户最新指令优先，后续产品修改需按接手会话中的指令决定。

## 用户真正要的结果与范围变化

1. 原问题：语音任务执行期间一直沉默，结束才说话。用户主要希望像真人一样接话、闲聊、改口，有一致人格；页面进度次要。
2. 用户要求测试可见，反对只用静音无头Chrome而看不到结果。后半段转到其日常Chrome扩展页面做可见检查。
3. 新问题：没说“嗯”却记录。日志和100ms非人声噪声探针指出纯音量检测可误触；具体现场声源无原始录音，未确定。
4. 用户只授权改善第一处人声检测，随后自行试用。第二处播放回声专门策略、第三处可疑转写过滤均未获本轮实施授权，未做。
5. 最新真人反馈：识别仍差且用户判断体验退化。停止修补，保存并交接。

## 两组主要改动

### 任务中接话

`agent/src/voice-session.ts`移除awaitDelivery沉默，后台结果排队，保留voiceTurn与delivery身份、取消晚包。先接话与分类并行（VoiceService开启earlyReplies），接话只能表达理解/意图；真实任务与控制仍依receipt。`conversation-manager.ts`增加待委托的输入判定门控，避免普通闲聊取消尚未接收的原委托；新动作使旧委托失效。`voice-intent.ts`合并符合条件的start+observe分类。`voice-personality.ts`统一即时语音与正式回答语气。

顺序接话较早真实四轮样本通过13个流程检查，但另一样本第二轮等11.93秒；于是改并行接话。随后一次首音2.324秒，但任务模型529过载，未完成最新全路径。不能拿早期版本的流程成功证明最终并行版本体验。

### 本地人声检测

`extension/src/sidepanel/voice-speech.ts`、`voice-vad-worker.ts`、`voice-vad-model.ts`：Silero V5/ONNX在本地Worker判断人声。输入原24k PCM保留，仅模型副本转16k。以32ms帧处理，连续96ms高置信触发；前音320ms，低人声700ms提交，极低能量下限避免数字静音延续。VoiceClient将worklet音频先经过该检测。

依赖@ricky0123/vad-web 0.0.30、onnxruntime-web 1.29.0；约17MB资产随扩展构建，CSP增加wasm-unsafe-eval。没有更换转写服务、音色或任务模型，没有屏蔽“嗯”。启动/推理失败显式报错，没有退回音量判断。

## 最重要的缺口

**人声检测成功不等于转写正确。** 9个样本检查验证触发/提交次数，未验证真实麦克风中文转写的字词准确率、首尾是否丢失、自然停顿是否错误分段。正式收音链路检查也用合成输入，远端消息由检查器接收确认，没有走真实ASR。此前对外虽保留“真人待验收”，但实际门槛仍不足以支撑放心交付。用户现在已经提供负反馈，必须记作体验失败。

截图显示用户转写为“切了不是治病那一栏。”，助手回答中又理解成“不是直聘那一栏”，并要求用户发页面链接。用户确切原话没有记录，不能自行补造ground truth。截图上方“已中止”及当前上下文是检测结果页也值得结合实际任务核查，但尚未确认因果。

现场日志对应15:50:56.770开始turn3，15:50:58.687提交107520字节（24k单声道16bit约2.24秒），15:50:59.349转写10字。日志没有原始PCM，不能判定漏音或ASR原因。不要直接断言是Silero导致，也不要排除本轮分帧/缓冲/阈值改动。

## 证据入口

- [用户失败截图](user-reported-recognition-regression.png)
- [该次真实会话诊断日志](reported-session.txt)：仅当前语音会话，不包含密钥和全库日志。
- [9类检测样本结果](speech-sample-results.json)、[合成收音链路结果](capture-result.json)：仅证明检测与提交，不证明识别准确。
- [连续对话冻结标准](../../evals/20260910-live-dialogue.md)、[历史样本摘要](../../evals/20260910-live-dialogue-results.json)
- [人声检测范围与验收](../../evals/20260910-voice-speech-detection.md)
- [状态](../../STATUS.md)、[技术笔记](../../NOTES.md)

较早录音和完整JSON仍在本机临时目录，验收记录有路径；未全部上传远端，远程接手者不能假定可访问。用户当前浏览器扩展已加载最终源码构建，sidepanel与检测worker的在线资产hash已比对一致；不是尚未加载的候选。

## 机器验证与已知失败

最终全量130文件1050项通过；最终定点4文件29项通过；typecheck/build/diff通过。一次高并发全量的既有JS权限测试收到interrupted而非权限错误，未改断言，定点34项及降低并发全量通过。

普通HTTP诊断页遇到blob Worker fetch未定义，来源未确定；正式扩展页面可运行。同一旧检查器以页面20ms定时器喂音，后台限速造成100秒超时；改按处理回执喂样本，未改音频与断言。都不能解释为真人体验通过。

## 供接手者判断的下一步（建议，不是用户已批准的实施方案）

优先判断应回退哪组变化，还是做受控对照；先取得用户实际说法与送进ASR的同一段声音，核对截断、分段、时间顺序、ASR结果与UI显示，再确定责任层。建立真实中文转写和自然停顿验收，不再仅靠“触发一次”证明整体改善。用户明确厌倦继续补丁式推进，不要未经指导自动扩第二、第三处。
