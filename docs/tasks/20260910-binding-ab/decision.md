# 选型结论：保留 A

2026-09-10。Codex主代理依据用户修订后的单一指标评审，DeepSeek实现并执行离线检查、分析真实结果；主代理经用户明确确认在宿主启动DeepSeek脚本。

指标：六次固定任务全部正确完成的平均totalMs（用户发出任务到任务结束，秒）。任一次真实任务失败即不达标；全部通过者中平均耗时最低者胜出。

| 方案 | 成功 | 指标值 | 判定 |
|---|---:|---:|---|
| A 当前工作区基线 | 6/6 | 83.701秒 | 胜出，保留 |
| B 唯一待办执行时绑定 | 5/6 | 不达标，不计算排名值 | 不接入 |
| C 动作携带待办编号 | 5/6 | 不达标，不计算排名值 | 不接入 |

原始有效批次：[20260910T045813-60319](runs/20260910T045813-60319/index.json)。18次、16成功、2真实失败；三副本运行期间源码与测试脚本均未漂移。每个block的三候选共享seed、任务、fixture，模型M3/medium不变。主代理从原始JSON复核成功数与A均值。

B最后一次等待任务没有提交：页面激活事件0、按钮已就绪，正式回答明确连续工具错误后未完成。C首次等待任务选择不存在元素后未完成，180秒任务上限触发。失败均保留，不用成功样本均值代替全任务指标。

首批因隔离目录无.git而启动失败，原始记录及更正保留，未计入性能或产品淘汰。见[失败更正](runs/20260910T045505-55523/reclassified.json)。

正式产品源码与A一致；本轮不回接B/C、不重载、不提交。原授权中的回接验证仅在新候选选中时适用。本轮结果为保留A，未取得符合标准的替代方案。

收尾核对：已检查相关经验索引和生产接线pattern。指标提取、快照启动问题已有对应脚本、定点测试和任务评审记录；不重复新增经验或规则提案。
